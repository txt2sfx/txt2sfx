# txt2sfx v0 — план работ (handoff-документ)

Дата создания: 2026-08-04. Обновлён: 2026-08-05.
Статус: **все фазы v0 завершены** — 1–5, 7 и обе половины фазы 6. Плейграунд закрывает
цикл: промпт-бар, выбор провайдера Gemini/Anthropic/mock, поле ключа в памяти вкладки,
галерея из банка и публикация рецепта в банк. Итоги остатка фазы 6 — §15.
Документ самодостаточен: его достаточно, чтобы продолжить работу на другой машине
без исходного диалога.

**Сверка с репозиторием на 2026-08-05 (после остатка фазы 6 и правок §15.6–15.7):** 617/617 тестов зелёные
(~19 с — сквозные тесты оптимизатора это тысячи рендеров), `tsc -b`,
`tsc -p tsconfig.test.json` и typecheck плейграунда чистые, `vite build` собирается
(330 КБ JS, 109 КБ gzip). На диске: `packages/{shared,core,analyzer,optimizer,agent}`,
`apps/{web,server}`, `bench/` (6 целей), `docs/` (6 документов), `test/` (репозиторные
проверки), `examples/` (10 рецептов), `.github/workflows/ci.yml`, `tsconfig.test.json`,
`README.md`, `CONTRIBUTING.md`, `ROADMAP.md`, `.env.example`, `assets/logo.svg`, `LICENSE`.
Итоги фазы 7 и отклонения — §14, остатка фазы 6 — §15. Что осталось за пределами v0 —
§15.9 и `ROADMAP.md`. Мост в devtools, которым цикл отлаживается вручную, и три дефекта,
которые он нашёл, — §15.6.

---

## 0. Что это за проект

Опенсорс-инструмент: текстовое описание звукового эффекта → **компактный процедурный
код Web Audio API** (300–1000 байт, zero-dependency), а не аудиофайл. Философия
скопирована с img2threejs — *reconstruction-by-code*.

Четыре принципа, обязательные для всех архитектурных решений:

1. **Scripts enforce, the model judges.** Парсинг, валидация, рендер, метрики, скоринг —
   детерминированный код без LLM. LLM участвует только в проектировании структуры звука
   и оценке перцептивного сходства.
2. **Spec-first, fail fast.** Кодогенерация не стартует, пока спека не прошла валидатор
   физических инвариантов. Никогда не чинить сгенерированный JS — чинится soundline,
   JS перегенерируется.
3. **Разделение структуры и чисел.** LLM проектирует топологию (слои, примитивы, роли);
   точные числа подбирает численный оптимизатор против целевого акустического профиля.
4. **Выход — читаемый текст.** Единица обмена — формат `soundline`, диффабельный.

Никаких облачных text-to-audio моделей (Stable Audio, ElevenLabs) в ядре. CLAP-судья —
опциональный плагин, в v0 достаточно интерфейса-заглушки.

### Требование заказчика по ключам и локальности

- Тестирование идёт через **Gemini по API-ключу, который вставляется прямо в плейграунде**,
  и через **Opus 5 из CLI / по API / через MCP**.
- Внешний агент пользователя должен воспользоваться библиотекой и накопленной базой
  рецептов **максимально быстро и без установки**.
- LLM всегда пользовательская; если привлекается локальная модель генерации звука —
  она тоже исполняется на машине пользователя. Ничего не проксируется через наш сервис.

---

## 1. Как поднять окружение на новой машине

```powershell
# требуется Node >= 20 (проверено на v24.15.0 и v25.2.1)
npm install -g pnpm@10     # см. примечание про corepack ниже
cd <repo>
pnpm install
pnpm vitest run            # 220 тестов должны быть зелёными
pnpm build                 # tsc -b packages/shared packages/core
pnpm dev                   # плейграунд на http://localhost:5173
```

Примечания:

- **Corepack удалён из Node 25**, поэтому описанный ранее обход `corepack pnpm <cmd>`
  больше не работает: бинарника `corepack` в `C:\Program Files\nodejs` просто нет.
  Рабочий способ — поставить pnpm глобально через npm (`npm install -g pnpm@10`),
  права администратора не нужны.
- В `package.json` уже прописан `pnpm.onlyBuiltDependencies: ["esbuild"]`;
  предупреждение «Ignored build scripts: esbuild» безвредно, vitest работает.
- `node-web-audio-api` — **devDependency воркспейса**, не зависимость `core`.
  Ядро получает фабрику `OfflineAudioContext` инжекцией (см. §5).
- `.gitattributes` форсит `eol=lf` — без него round-trip тесты на Windows падают.
- Типизация тестов проверяется отдельным вызовом: `tsc -b` покрывает только `src`.
  Постоянный `tsconfig.test.json` — задача фазы 7; до неё проверять одноразовым
  конфигом с `include: ["packages/*/src", "packages/*/test"]` и `paths` на
  `@txt2sfx/{shared,core}` → `src/index.ts`.

---

## 2. Текущее состояние: фазы 1–2 (готово)

```
txt2sfx/
├── package.json               # workspaces, скрипты build/test, pnpm.onlyBuiltDependencies
├── pnpm-workspace.yaml
├── tsconfig.base.json         # strict + noUncheckedIndexedAccess + exactOptionalPropertyTypes
├── vitest.config.ts           # алиасы @txt2sfx/* → src, тесты идут без сборки
├── .gitignore, .gitattributes, LICENSE, README.md
├── examples/*.soundline       # 10 файлов, все в канонической форме
└── packages/
    ├── shared/src/{types.ts,constants.ts,index.ts}
    └── core/
        ├── src/grammar/{signatures,units,errors,lexer,parser,serializer}.ts
        ├── src/compile/{ir,buffers,emit,envelope,duration,compile,graph,codegen}.ts
        ├── src/primitives/{types,signature,args,oscillator}.ts
        ├── src/primitives/{tone,noise,chirp,pluck,modal,fm,sub,click,index}.ts
        ├── src/primitives/effects/{filter,mix,lp,hp,bp,dist,delay,verb}.ts
        ├── src/render/{offline,wav}.ts
        ├── src/index.ts
        └── test/{roundtrip,lexer,parse-errors,suggestions}.test.ts
            test/{primitives,codegen,render}.test.ts, test/helpers/audio.ts
```

**Фаза 1 — критерии выполнены:** `serialize(parse(src))` побайтово равен исходнику на
всех 10 примерах; 34 теста на ошибки парсинга (требовалось ≥15); `tsc -b` под strict
чистый, тестовые файлы тоже типизируются без ошибок.

**Фаза 2 — критерии выполнены:** все 10 примеров рендерятся в непустой буфер без
клиппинга; рендер детерминирован при фиксированном seed; экспортированный JS даёт
**побайтово тот же рендер**, что живой граф. Подробности и отклонения — §5.

**Итого 220/220 тестов зелёные.**

### Файлы, добавленные сверх ТЗ-структуры (и почему)

- `core/src/grammar/signatures.ts` — синтаксические контракты примитивов/эффектов/огибающей.
  Единый источник правды для лексера, парсера, сериализатора и `primitives/*.ts`.
  Лежит в `grammar/`, чтобы парсер не зависел от DSP-кода: `parse()` работает где угодно.
- `core/src/grammar/units.ts` — конвертации Hz/kHz, ms/s, dB→linear, формат чисел.
- `core/src/grammar/errors.ts` — `SoundlineError`, словарь синонимов, Дамерау-Левенштейн.
- `core/src/compile/ir.ts` + `buffers.ts` + `emit.ts` — промежуточное представление и
  процедурные генераторы буферов; ключевое архитектурное решение фазы 2, см. §5.
- `core/src/compile/{envelope,duration,compile}.ts` — чтение огибающей, оценка хвостов
  и собственно AST → IR. `duration.ts` понадобится валидатору фазы 3 как есть.
- `core/src/primitives/{types,signature,args,oscillator}.ts` и `effects/{filter,mix}.ts` —
  контракт билдеров, строгие лукапы по таблице сигнатур, чтение аргументов с дефолтами,
  общие тела трёх осцилляторных примитивов и трёх биквадов, dry/wet-развязка.
- `vitest.config.ts`, `.gitattributes` — инфраструктура.

---

## 3. Замороженные решения (не переоткрывать без причины)

Все они уже отражены в коде; при написании `docs/SOUNDLINE_GRAMMAR.md` их надо перенести
в документацию.

1. **Заголовок:** `sound "<имя>" <длительность> [<категория>] [loop]`.
   Категория — из закрытого списка `pop|ui|laser|impact|explosion|pickup|foley|cycle|misc`;
   без неё валидатору фазы 3 не к чему привязать инварианты. `misc` — дефолт, при
   сериализации опускается. `loop` — отдельный флаг; категория зациклённых звуков `cycle`
   (валидатор фазы 3 требует для неё флаг `loop`).
2. **Сигнальный порядок:** `source → envelope → chain → mix`, хотя пишется
   `source >> chain | envelope`. Огибающая шейпит источник, эффекты идут после неё,
   поэтому хвосты `delay`/`verb` переживают `decay`. На этом построен helicopter
   (ротор = сабовый удар, рециркулированный delay-петлёй; LFO в v0 нет).
   **Компилятор фазы 2 обязан следовать этому порядку.**
3. **Числа хранятся как написаны** (`1.2kHz` = 1.2 + unit `kHz`), конвертация только
   через `toHz/toMs/toLinear`. Это и даёт lossless round-trip.
4. **Слитные безразмерные параметры** (`Q6`) — только для закрытого набора имён из
   сигнатур (kind `ratio`/`level`). Поэтому слой `snap2` остаётся одним идентификатором.
   Сериализатор склеивает только односимвольные имена в верхнем регистре (`Q6`),
   остальное через пробел (`gain 0.9`).
5. **Ошибки построчные:** одна сломанная строка не прячет остальные
   (`parseWithDiagnostics`). Лексические ошибки прерывают разбор целиком.
   Формат сообщения: `line L, col C: <detail> (<hint>)` — идёт в LLM как есть.
6. **Словарь синонимов в подсказках** (`lowpass→lp`, `reverb→verb`, `sweep→chirp`,
   `release→decay`, `volume→gain`, ...) + Дамерау-Левенштейн для опечаток
   (`nosie→noise`). Расстояние `lowpass`→`lp` = 5, чистая метрика такое не ловит,
   а LLM ошибается именно так. Каждый пойманный синоним экономит итерацию агента.
7. **В AST попадают только явно написанные аргументы**; дефолты применяет компилятор.
   Иначе round-trip перестаёт быть побайтовым.
8. **Канонизация:** 2 пробела отступа слоя, аргументы в порядке сигнатуры, огибающая
   в порядке `gain attack hold decay curve delay`, комментарии сохраняются
   (файловые, построчные над слоем и хвостовые), пустые строки съедаются.

---

## 4. Инварианты стиля (соблюдать во всех фазах)

- `@txt2sfx/core`, `shared`, `analyzer`, `optimizer` — **ноль runtime-зависимостей**
  (FFT, WAV, DE — свои). Зависимости только в `apps/` и в devDependencies.
- TypeScript strict, без `any` в публичных API, экспортируемые типы с TSDoc.
- В `localStorage` веба лежат настройки и **несохранённая работа**, но никогда секреты
  (состояние — в React state; рецепт, который решили сохранить, идёт в `examples/` или в
  банк; исключение — сгенерированные и ещё не сохранённые рецепты, см. §19).
- Секреты только из env (`ANTHROPIC_API_KEY`, `GEMINI_API_KEY`), плюс `.env.example`.
  Ключ Gemini, введённый в UI, живёт в памяти вкладки и уходит только в запросы,
  которые инициировал пользователь.
- Сообщения об ошибках рассчитаны на двух адресатов сразу: человека и LLM в цикле.
- Спорные решения (формула расстояния, дефолтные диапазоны) — принять, реализовать
  и обосновать комментарием/в docs, а не останавливаться с вопросом.
- В конце каждой фазы — работающие тесты.

---

## 5. Фаза 2 — примитивы, компиляция, рендер, кодоген (готово)

### 5.1. Главное решение: промежуточное представление вместо двух бэкендов

ТЗ предполагало, что `graph.ts` собирает живые узлы, а `codegen.ts` независимо печатает
строки. Это означало бы **две реализации одного DSP** — 8 примитивов и 6 эффектов,
написанных дважды и обязанных совпадать до сэмпла. Они бы разошлись.

Поэтому DSP живёт в одном месте. Введён `compile/ir.ts` — `AudioIR`, описание графа,
независимое от sample rate и от наличия аудиостека. Примитивы и эффекты эмитят IR;
оба бэкенда — тупые проигрыватели: `graph.ts` инстанцирует, `codegen.ts` печатает.
Тест паритета проверяет **два проигрывателя**, а не две копии синтеза, — и потому
может требовать не «совпадения RMS в пределах допуска», а **побайтового равенства
рендеров**. На всех 10 примерах `maxAbsDiff === 0`.

Следствия, важные для следующих фаз:

- `PrimitiveDef.build(args, ctx)` вместо `build(ctx, args, when, durationMs)`:
  `BuildContext` несёт `ir`, `seed`, `start`, `stop`. Никакого `AudioContext`.
- Времена в IR — секунды **относительно онсета звука**; `when` добавляет бэкенд,
  потому что экспортируемая функция принимает его аргументом.
- `quantizeIR` округляет все числа до 6 знаков после запятой. Это не косметика:
  без округления экспорт нёс бы `0.191891363534` и либо распухал, либо округлял —
  а округлённая длина буфера в экспорте сдвигает все сэмплы относительно эталона.
  Округление один раз, до обоих бэкендов, и есть причина, по которой паритет точный.

### 5.2. Отклонения от ТЗ и их причины

| Что | ТЗ | Сделано | Почему |
| --- | --- | --- | --- |
| `modal` | 2–5 биквад-резонаторов | аддитивный синтез в буфер | Уровень резонатора, возбуждённого бёрстом, не предсказуем из soundline, а непредсказуемый уровень источника ломает инвариант `peak ≤ 0.95`, под который откалиброваны примеры. Акустика та же: партиал звенит на `freq * ratio`, затухает с `tau = Q/(pi*f)`; сумма делится на сумму амплитуд, поэтому пик буфера **доказуемо ≤ 1**. |
| `click` | «одиночный импульс» | оконный шумовой бёрст | Дирак не несёт энергии при разумном gain, униполярный «бугор» — это DC-удар, смещающий все замеры пика ниже по конвейеру. Бёрст широкополосен, имеет нулевое среднее, и `width` делает ровно то, что обещает грамматика: шире = больше низа = глуше. |
| PRNG | «свой seeded PRNG» | MINSTD (`s*16807 % 2^31-1`) | Лучше 32-битного LCG (полный период, нет слабых младших битов) **и** короче в экспорте: не нужен `Math.imul`, так как `2^31 * 16807 < 2^53`. 17 байт экономии в каждом из четырёх генераторов. Состояние не должно достигать 0 — за это отвечает `normalizeSeed` в `IRBuilder.buffer()`. |
| `pluck` | KS-петля или предрасчёт | предрасчёт буфера | Как и разрешало ТЗ: экспорту не нужны `DelayNode`, петля обратной связи и её обвязка. |

Прочие принятые решения (обоснования — в TSDoc соответствующих файлов):

- Порядок сигнала `source → envelope → chain → mix` соблюдён; хвосты `delay`/`verb`
  переживают `decay` и учитываются в `soundDurationMs` (`compile/duration.ts`).
- Атака всегда линейная (экспонента не может начинаться с нуля, а старт не с нуля щёлкает);
  спад по умолчанию экспоненциальный до `GLOBAL_LIMITS.minExpTarget` — **абсолютного**
  порога -60 dBFS, а не относительного, иначе тихие слои затухали бы дольше громких.
- Все экспоненциальные рампы клампятся от нуля (`safeExpTarget`), в том числе когда
  модель пишет `-> 0Hz` или `index -> 0`: намерение слышимо, литеральный ноль — нет.
- `dist`/`delay`/`verb` при `mix 0` не строят wet-путь, при `mix 1` — не строят
  сумматор. Оба края встречаются (`mix 1` — это helicopter), каждый сэкономленный
  узел стоит байтов в экспорте.
- Сиды слоёв разведены умножающим хешем по индексу слоя: два слоя белого шума с общим
  сидом дали бы не более широкую текстуру, а на 6 дБ громче ту же.
- Рендер **сообщает** о клиппинге (`clipped`), но никогда не нормализует: нормализация
  спрятала бы проблему там, где её ещё можно починить правильно — в спеке (принцип 2).

### 5.3. Замеренные пики: правка примеров не потребовалась

ТЗ предупреждало, что в `explosion` сумма gain четырёх слоёв ≈ 1.63, и требовало
подкрутить gain по факту замера. Замер (seed по умолчанию, 44.1 kHz):

| пример | пик | пример | пик |
| --- | --- | --- | --- |
| bubble-pop | 0.859 | helicopter | 0.505 |
| coin | 0.498 | laser | 0.818 |
| door-creak | 0.283 | powerup | 0.418 |
| explosion | 0.744 | sword-clash | 0.436 |
| footstep-gravel | 0.355 | ui-click | 0.397 |

Клиппинга нет ни в одном примере: слои не выходят на пик одновременно — их разводят
разные атаки, спады и `delay`. Сумма gain — не верхняя граница пика; **и не нижняя**:
`hp` дифференцирует, поэтому фильтр может поднять пик выше входного (есть отдельный
тест, фиксирующий это). Отсюда практический вывод для фазы 3: инвариант `maxPeak`
проверяется только на рендере, аналитически из спеки он не выводится.

Осторожно при работе над фазой 4: у `explosion` запас всего ~2 дБ, и оптимизатор,
крутящий слоты, может его съесть. Пик стоит считать частью fitness или хотя бы
проверять после оптимизации.

### 5.4. Размер экспорта: 1 KB достигнут не для всех примеров

`GLOBAL_LIMITS.maxExportBytes` = 1024. Фактические размеры после оптимизации эмиттера:

| пример | слоёв | байт | | пример | слоёв | байт |
| --- | --- | --- | --- | --- | --- | --- |
| coin | 2 | **531** | | helicopter | 3 | 1093 |
| laser | 2 | **553** | | powerup | 3 | 1169 |
| ui-click | 2 | **811** | | door-creak | 3 | 1185 |
| bubble-pop | 2 | **824** | | explosion | 4 | 1458 |
| | | | | sword-clash | 3 | 1579 |
| | | | | footstep-gravel | 3 | 1708 |

**Все четыре двухслойных примера укладываются в 1 KB; трёх- и четырёхслойные — нет,
превышение от 7% до 67%.** Причина структурная, а не в небрежной генерации: фиксированную
стоимость составляют процедурные генераторы буферов, и это ровно та цена, за которую
проект не тащит ни одного ассета. `footstep-gravel` платит за генератор шума (два цвета),
модальный банк и кривую вейвшейпера сразу — ~640 байт до первого узла графа.

Что уже сделано для размера (снизило пики размеров на 15–25%): один общий `const` на всю
прелюдию (легально — список `const` инициализируется слева направо); генераторы шума
эмитятся **по фактически использованным цветам**; односимвольные алиасы для длинных имён
методов `AudioParam`, для `connect`, для пары `start`/`stop` и для повторяющихся фабрик
`ctx.createX()` — каждый только когда окупает собственное объявление; значения, равные
дефолтам Web Audio (`gain` 1, `Q` 1, синусный осциллятор), не пишутся вовсе; параметр
с единственным `set`-событием пишется как `.value=`.

Дальнейшие идеи исчерпаны: цепочки `a.connect(b).connect(c)` дают ~3 байта на звено,
общий envelope-хелпер сэкономил бы ~20 байт на слой, но закодировал бы форму огибающей
во втором месте и обрушил бы то самое свойство, из которого следует точный паритет.

**Решение:** `withinBudget` — это отчёт, а не запрет. `codegen` возвращает `bytes` и
`withinBudget`, но всегда отдаёт полный работающий код: молча урезать экспорт или
отказаться его выдавать было бы хуже, чем сказать вызывающему, сколько он стоит.
В тестах зафиксированы (а) потолок размера на каждый пример как защита от регрессий и
(б) требование `withinBudget` для всех сооружений в ≤ 2 слоя. Для README (фаза 7) честная
формулировка — «300–1000 байт для типичного SFX», а не «всегда меньше килобайта».

### 5.5. Ещё один вход для фазы 3

`helicopter` реально звучит **4046 мс** при заявленных в заголовке 1500: `delay 90ms`
с `feedback 0.88` спадает до -60 дБ за 54 повтора. Это не баг рендера, а именно тот
случай, который валидатор фазы 3 обязан поймать по контракту длительности заголовка.
`soundDurationMs(ast)` уже возвращает 4046 и готов к использованию как есть.
Оценка хвоста ограничена сверху 4000 мс на эффект (`delay.ts`), чтобы `feedback 0.95`
не просил минуту аудио; правду при этом сообщает валидатор, а не гигантский буфер.

---

## 6. Фаза 3 — валидатор и анализатор (готово)

**Файлы — все созданы:**

```
packages/core/src/validate/{invariants.ts,index.ts}
packages/core/test/validate.test.ts                     (46 тестов)
packages/analyzer/src/{fft.ts,profile.ts,distance.ts,diff.ts,index.ts}
packages/analyzer/package.json, tsconfig.json
packages/analyzer/test/{fft,profile,distance,diff}.test.ts, test/helpers/signals.ts
                                                        (73 теста)
```

**Итого 339/339 тестов зелёные** (было 220). `tsc -b shared core analyzer` чистый,
тесты типизируются одноразовым конфигом без ошибок, `apps/web` тоже.
Оба критерия приёмки выполнены: профиль `bubble-pop` даёт `durationMs = 42`,
и обязательный тест-кейс «пиу под видом pop» ловится как `pop.max-freq-ramp`.
Отклонения от ТЗ фазы и найденные по ходу проблемы — §6.1–6.4.

**Валидатор** — `validate(ast): ValidationIssue[]`, инварианты из
`shared/constants.ts` (`CATEGORY_LIMITS`, `GLOBAL_LIMITS`), минимум:

- pop/click/snap: суммарная длительность ≤ 60 ms, decay каждого слоя ≤ 40 ms,
  частотные рампы длиннее 20 ms запрещены (анти-«пиу»);
- laser 80–300 ms; explosion/impact 200–1500 ms; ui 30–120 ms;
  cycle 500–3000 ms и обязателен флаг `loop`;
- глобально: пиковая амплитуда ≤ 0.95, ни один exponential-рамп не заканчивается на 0,
  число слоёв ≤ 6;
- контракт длительности заголовка: всё, что выходит за него (включая хвосты
  delay/verb — считать по сигнальному порядку из §3.2), — issue.

Каждое нарушение — `ValidationIssue { severity, layer, rule, got, expected, hint, loc }`;
`hint` формулируется как инструкция для LLM.

**Анализатор** — своя radix-2 FFT без зависимостей; `extractProfile(buffer): SoundProfile`
(поля уже описаны в `shared/types.ts`); `profileDistance(a, b)` = взвешенная сумма
расстояний по полям + multi-resolution STFT loss (окна 256/1024/4096, log-magnitude, L1),
перед сравнением нормализация громкости и выравнивание по onset;
`humanReadableDiff(a, b): string[]` — директивы вида
`"attack is 45ms, target 8ms — shorten the gain ramp"`,
`"centroid 1.2kHz vs 3.4kHz — raise bp frequency in layer 'snap'"`.
Именно эти строки, а не сырые числа, идут в промпт LLM.

**Критерии приёмки:** профиль bubble-pop даёт `durationMs < 60`; валидатор ловит
подсунутый «пиу» под видом pop (обязательный тест-кейс: `sound "pop" 35ms pop` со слоем
`tone sine 2000Hz -> 200Hz in 120ms` → issue `pop.max-freq-ramp`).

### 6.1. Валидатор: правила и решения

11 правил в `INVARIANTS` (массив, чтобы фаза 7 могла добавлять свои), два входа:

- `validate(ast)` — всё, что выводится из текста;
- `validateRender({ peak })` — только то, что нельзя вывести (§5.3). Отдельный тип
  `RenderFacts`, а не `RenderResult`: у оптимизатора и плейграунда есть пик без
  `AudioBuffer`, а анализатору надо уметь давать те же числа по декодированному
  референсу.

Идентификаторы правил: `<cat>.{min,max}-duration`, `<cat>.max-layer-decay`,
`<cat>.max-freq-ramp`, `<cat>.max-layers`, `cycle.requires-loop`, `duration.contract`,
`envelope.attack-longer-than-voice`, `param.range`, `param.slot-range`,
`ramp.exp-to-zero`, `pop.rising-resonance`, `peak.max`, `peak.silent`.

Принятые решения:

1. **Severity по величине отклонения** — у контракта длительности и у
   attack-vs-voice. Оба сравнивают с *оценкой*, а не с замером: хвост — экспонента,
   приведённая к -60 дБ, хвост эффекта — формула с потолком 4 с. Пара процентов —
   это шум оценки, а не ложь; в два раза — уже другой звук. Порог: слack
   `max(10 мс, 5 %)` — чисто; до 25 % — `warn`; свыше — `error`.
2. **`param.range` — error, хотя границы в сигнатурах названы «soft».** «Soft»
   описывает происхождение чисел (вкус и практика), а не серьёзность нарушения.
   `Q 400` на биквадре звенит колоколом, осциллятор на 19 кГц не слышен половине
   аудитории, а warn, который цикл агента вправе проигнорировать, стоил бы итерации
   на то же самое открытие.
3. **Исключение: `-> 0` в конце рампы не считается выходом за границу.**
   `-> 0Hz` и `index -> 0` — поддержанная идиома «свести в ничто», компилятор
   специально клампит экспоненту от нуля (§5.2), и `ramp.exp-to-zero` уже говорит,
   что реально произойдёт, лучше, чем жалоба на диапазон. Голова рампы с нулём —
   по-прежнему error. Тест на отсутствие двойного отчёта есть.
4. **`param.slot-range` — warn.** Слот, выходящий за границу параметра, — это
   разрешение оптимизатору генерировать невалидный звук; узнать об этом лучше здесь,
   чем в фазе 4 через тысячу кандидатов. Но seed легален и звук рендерится, поэтому
   не error.
5. **`peak.silent` (warn, порог 0.02 ≈ -34 dBFS) добавлен сверх ТЗ.** Ровно тот
   класс провала, который §11 назвал самым дорогим: слой на -41 дБ без единой
   ошибки. Рецепт, рендерящийся в тишину, не бывает тем, что просили.
6. **Только перерасход контракта, не недобор.** Заголовок на 1500 мс при 40 мс
   звука — тоже ложь, но ТЗ просило ловить выход за контракт, а не недоиспользование;
   отдельного правила нет сознательно.

### 6.2. Два примера не проходят валидацию — и это правда, а не баг правил

8 из 10 рецептов чисты. Два — нет, оба по `duration.contract`:

| рецепт | заявлено | реально | перерасход | severity |
| --- | --- | --- | --- | --- |
| helicopter | 1500 мс | 4046 мс | 170 % | **error** |
| sword-clash | 700 мс | 821 мс | 17 % | warn |

`helicopter` — тот самый предсказанный §5.5 случай. Важно: **его нельзя привести в
контракт правкой одного заголовка** — 4046 мс выходит за собственный потолок категории
`cycle` (3000 мс). Находка зафиксирована тестом, а не заглажена: это решение звукового
дизайна, а не рефакторинг.

**Три рычага, решение за заказчиком:**

1. Принять как есть — тест это пинует, сюрприза не будет.
2. Менять DSP: `delay 90ms feedback 0.88` — это и есть ротор (§3.2); чтобы хвост
   уложился в 1450 мс, нужен `feedback ≤ 0.65`, то есть другой звук.
3. Поднять `CATEGORY_LIMITS.cycle.maxDurationMs` с 3000 до ~4500 и заголовок до 4046.
   Законный вариант: шапка `constants.ts` прямо называет эти числа мнением проекта, а
   обоснование «редко превышают три секунды» — про период петли, не про хвост
   рециркуляции. Если 4-секундный разгон ротора и есть правильный звук, сдвинуть
   границу дешевле, чем портить рецепт.

**Дедлайн решения — фаза 5, не конец работ.** `POST /api/recipes` по §8 валидирует
soundline перед записью, а `seed.ts` заливает все `examples/*.soundline`. Если сидер
уважает валидацию, helicopter в банк не попадёт: 9 рецептов вместо 10, и `retrieve`
по «helicopter»/«rotor» вернёт пустоту — то есть банк перестанет показывать, что умеет
отдавать зациклённые текстуры. Фазе 4 helicopter не нужен: тест восстановления слотов
делается на любом рецепте, и брать заведомо внеконтрактный в фикстуры не стоит.

Заодно проверено и зафиксировано, что для `loop`-звука хвост эффекта **считается**:
заявленная длительность зациклённого звука — это период петли, а буфер на 4046 мс
даёт цикл в 2.7 раза длиннее обещанного, то есть шов слышен.

### 6.3. Анализатор: отклонения от ТЗ

| Что | ТЗ | Сделано | Почему |
| --- | --- | --- | --- |
| метрика | один `profileDistance` = поля + STFT loss | `profileDistance` (поля), `stftDistance` (волны), `soundDistance` (оба, 50/50) | STFT loss невозможно посчитать из `SoundProfile`: профиль — 100 чисел, лоссу нужно аудио. При этом банк рецептов хранит только профили, и retrieve обязан сравнивать рецепты, ничего не рендеря. Одна функция не может обслужить оба вызова. |
| STFT loss | log-magnitude L1 | L1 по log-magnitude **плюс spectral convergence**, 50/50 на каждый кадр | L1 в одиночку размывается бинами, где оба сигнала на полу, а в разреженном спектре это почти все бины: синус 1 кГц против синуса 1.4 кГц — ничего общего — давал **0.046**, потому что шесть бинов с разницей 100 дБ усреднились по 250 бинам, согласным насчёт тишины. Энергетическая нормировка даёт на той же паре ≈0.5. |
| `noiseRatio` | «доля энергии вне гармонических пиков» | по кадрам, медиана; порог от **кольцевого** среднего с вырезанным главным лепестком | (а) Усреднение спектра по времени превращает свип в плато без пиков: `bubble-pop` (два тональных слоя, ни одного шумового) мерился как 0.93. (б) Среднее с радиусом ±4, включающее сам пик, сжимало метрику — чисто тональные рецепты давали 0.64 вместо ~0. После правок: тональные 0.00–0.04, шумовые 0.31–0.81. |
| окно анализа | не оговорено | 512..2048 сэмплов, из длины сигнала | Верх — из §9.2: 2048 (43 мс) размазал один свип в три фантомных партиала. Низ — измерено здесь: с окном 128 `peakHz` у `bubble-pop` вышел 772 Гц, то есть прочёл первое мгновение 14-мс свипа вместо свипа. 512 — то же окно, что в Compare-панели плейграунда, и это осознанно: суждение, сложившееся по картинкам, должно значить для оптимизатора то же самое. |
| LUFS | «приблизительная» | ФВЧ 1-го порядка на 100 Гц вместо K-фильтра + стандартный переход к LUFS | Коэффициенты BS.1770 выводятся под конкретную частоту дискретизации; для двух задач, где число используется (сравнение с целью и нормировка перед спектральным сравнением), этого достаточно за одно умножение-сложение на сэмпл вместо шести. |

FFT свой, radix-2, twiddle-факторы вращением, а не `Math.cos` на бабочку. `peakHz`
уточняется параболической интерполяцией по логарифмам: бин 512-го окна — 86 Гц, а
различать надо 1055 против 1098 (§9.2), то есть полбина. Проверено на синусах:
ошибка < 21 Гц на 1055/1098/3210 Гц.

Мини-FFT плейграунда (`apps/web/src/lib/fft.ts`) сознательно **не** переиспользован,
как и требовал §9.1.

### 6.4. Найден и исправлен баг детектора онсета — в анализаторе и в плейграунде

«Самый крутой подъём» искался с кадра 1. У звука, у которого все слои с
`attack 0ms`, подъёма нет вообще: сигнал начинается на полной амплитуде на сэмпле 0,
и каждый следующий кадр тише предыдущего. Максимальный «подъём» тогда равен нулю и
находится в хвостовой тишине, которую рендерер всегда дописывает, — онсет уезжает
за конец звука, а весь профиль обнуляется. Ни ошибки, ни предупреждения.

Исправлено в `packages/analyzer/src/profile.ts` (кадр 0 сравнивается с неявной
тишиной перед сигналом) и тем же однострочником в `apps/web/src/lib/analysis.ts`,
где была ровно та же формула, — иначе Compare-панель ломалась бы на любом рецепте
из одних `attack 0ms`. Регрессионный тест зафиксирован.

### 6.5. Замеренные профили десяти рецептов

Опорные числа: если правка анализатора сдвинет их, это надо объяснять.

| рецепт | dur, мс | atk, мс | peakHz | centroid₀, Гц | flatness | noise | LUFS |
| --- | --- | --- | --- | --- | --- | --- | --- |
| bubble-pop | 42 | 1 | 836 | 1115 | 0.0014 | 0.005 | -15.1 |
| coin | 182 | 1 | 1320 | 4948 | 0.0314 | 0.001 | -18.8 |
| door-creak | 201 | 3 | 100 | 131 | 0.0032 | 0.000 | -28.8 |
| explosion | 1128 | 3 | 58 | 866 | 0.0019 | 0.314 | -28.5 |
| footstep-gravel | 103 | 4 | 5275 | 8456 | 0.3471 | 0.812 | -30.4 |
| helicopter | 3892 | 9 | 62 | 812 | 0.0154 | 0.318 | -36.7 |
| laser | 150 | 1 | 1630 | 6386 | 0.4239 | 0.584 | -20.5 |
| powerup | 464 | 1 | 451 | 4083 | 0.1166 | 0.041 | -19.4 |
| sword-clash | 163 | 6 | 2101 | 4317 | 0.0136 | 0.014 | -27.4 |
| ui-click | 20 | 1 | 1800 | 4232 | 0.1113 | 0.016 | -25.4 |

Проверки на осмысленность, вошедшие в тесты: `peakHz` возвращает написанное в
рецепте число — `sword-clash` 2101 против `modal metal 2100Hz`, `coin` 1320 против
`tone square 1319Hz`, `ui-click` 1800 против `tone sine 1800Hz`, `helicopter` 62
против `sub ~62Hz`. И независимое подтверждение §5.5: анализатор мерит helicopter в
3892 мс там, где компилятор оценивает 4046 — две разные дороги к одному ответу.

Матрица `soundDistance` (диагональ ровно 0, симметрична): ближайшие пары —
bubble-pop↔ui-click 0.36, coin↔laser 0.37, laser↔footstep 0.37; `explosion` дальше
всех от всего (0.58–0.60), единственный длинный низкий звук. Диапазон различающихся
звуков 0.36–0.60 — сверху сжато, но упорядочено, и для fitness важна окрестность нуля.

---

## 7. Фаза 4 — оптимизатор (готово)

**Файлы — все созданы:**

```
packages/optimizer/src/{slots.ts,de.ts,index.ts}
packages/optimizer/package.json, tsconfig.json
packages/optimizer/test/{slots,de,optimize}.test.ts, test/helpers/render.ts
                                                        (50 тестов)
```

**Итого 389/389 тестов зелёные** (было 339). Критерий приёмки выполнен: `bubble-pop`
с обоими крайними положениями всех слотов возвращается к distance < 0.05 за ≤ 40
поколений, и то же на `footstep-gravel` (шумовой рецепт с более плоским ландшафтом).

Полный прогон тестов теперь ~17 с против 1.3 с: 11 сквозных тестов оптимизатора — это
несколько тысяч рендеров. Тяжёлые кейсы сознательно переведены на profile-only fitness
там, где спектральный лосс не участвует в проверяемом свойстве.

### 7.1. Отклонения от ТЗ

| Что | ТЗ | Сделано | Почему |
| --- | --- | --- | --- |
| кандидат | вектор пишется обратно **в AST** | вектор пишется в **текст**, затем `parse` | (а) Измеряется ровно то, что будет отгружено: рецепт пишется с точностью `roundLiteral`, и поиск по полным float'ам с округлением в самом конце отдал бы рецепт, который никто не рендерил. Округление на входе делает пространство поиска в точности множеством записываемых рецептов. (б) Парс — микросекунды против миллисекунд рендера. (в) Нет второго пути перезаписи дерева: пересборка `SoundAST` с подстановкой литералов — это все readonly-типы, и баг там даёт валидный на вид AST, говорящий не то, что текст. |
| подпись | `optimize(ast, targetProfile, renderFn, opts)` | `optimize({ source, target, render, ... })` | Кандидат — текст (см. выше), поэтому вход — текст. `target` — это `{ profile, signal? }`: с аудио fitness считается `soundDistance`, без него — только по полям профиля. Банк рецептов хранит профили и не хранит аудио, так что profile-only — нормальный режим, а не деградация. |
| fitness | `profileDistance(...)` | `soundDistance` + штрафы | Кандидат, не прошедший валидатор, **не рендерится вообще** (spec-first, fail fast) и получает штраф 4 — без градиента внутри невалидной области, чтобы поиск не ходил через неё. Клиппинг — штраф 2, упорядоченный по distance, то есть у него есть путь наружу. Это прямое требование §5.3/§11: у `explosion` запас ~2 дБ, и поиск, которому велено только совпасть по спектру, охотно его съест. Есть тест. |
| поиск | не оговорено | в единичном кубе, оси частот и времён логарифмические | На линейной оси `[500..8000]` Гц всё ниже 1 кГц занимает первые 7 % интервала, и равномерно засеянная популяция почти не попадает туда, где сосредоточена энергия большинства SFX. Кривая та же, что у слайдеров плейграунда. |

**Исправлен баг счётчика простоя (найден ревью, 2026-08-05).** Флаг «был прогресс»
ставился внутри ветки принятия кандидата по условию `score < scores[bestIndex]`. Когда
заменяемый кандидат — сам текущий лидер, его счёт к этому моменту уже перезаписан, и
условие читается как `score < score`, то есть ложь. Лучший фитнес падал поколение за
поколением, счётчик простоя при этом рос, и прогон объявлялся «stalled» посреди
сходимости. Замерено до правки: 8 из 60 засеянных 6-мерных прогонов остановились так,
один — улучшившись за два поколения до сдачи. Это ровно то дорогое направление ошибки,
против которого и калибровался порог: ложный «stalled» отправляет LLM переделывать
топологию. Теперь прогресс определяется сравнением лучшего фитнеса до и после поколения.
Регрессионный тест проверяет инвариант на восьми сидах, а не на одном удачном: если
прогон остановился по простою, последние `stallGenerations` отчётов обязаны показывать
один и тот же лучший фитнес.

`stallGenerations` = 20 (треть бюджета), а не 12: замерено на 3-мерном Растригине —
порог 12 останавливал поиск на вдвое большей ошибке, чем тот же прогон, которому
позволили продолжить. Плато в десяток поколений — рядовая середина поиска, а не
вердикт. Цена ошибки асимметрична: ложный «stalled» отправляет LLM переделывать
топологию, которую переделывать не надо.

`roundLiteral` вынесен в `@txt2sfx/core` (`grammar/units.ts`) и используется и
оптимизатором, и слайдерами плейграунда: два приватных экземпляра этой таблицы
рано или поздно разошлись бы в том, как пишется одно и то же число.

### 7.2. Найден баг: `getChannelData` отдаёт представление в нативной памяти

Первый прогон сквозных тестов **убил воркер vitest** без JS-стека, а до этого
`stftDistance` начал возвращать NaN на сигналах, в которых за мгновение до этого
не было ни одного нечисла.

Причина: `node-web-audio-api` возвращает из `getChannelData(0)` вид на память,
которой владеет нативный контекст. Оптимизатор держит `target.signal` живым через
сотни рендеров; контексты этих рендеров становятся недостижимы, GC их собирает — и
освобождает буфер под видом, который всё ещё держит цель. Сэмплы превращаются в NaN,
любая метрика с этой целью становится NaN (а `NaN <= x` — ложь, поэтому DE не
принимает ни одного кандидата и «лучшим» остаётся seed), затем процесс падает.

Исправлено копированием на выходе из аудиостека — `Float32Array.from(...)` — в
хелперах тестов оптимизатора и анализатора. **Важно для фазы 5:** `seed.ts` рендерит
десять рецептов и снимает профили, то есть окажется ровно в этой же позиции. Всё, что
держит сэмплы дольше контекста, обязано копировать.

---

## 8. Фаза 5 — сервер банка рецептов (готово)

**Файлы — все созданы, плюс три сверх ТЗ:**

```
apps/server/src/{index.ts,db.ts,routes/recipes.ts,seed.ts}
apps/server/src/app.ts      — сборка Fastify без listen (чтобы тесты шли через inject)
apps/server/src/llms.ts     — генератор /api/llms.txt
apps/server/{package.json,tsconfig.json,.gitignore}
apps/server/test/{bank,api,seed}.test.ts                (66 тестов)
```

**Итого 462/462 теста зелёные** (455 после фазы 5, плюс 7 регрессионных из ревью §13).
Оба критерия выполнены и проверены дважды —
на уровне банка и по HTTP: `retrieve` по «coin pickup sound» отдаёт `coin` в топ-1.
Сервер проверен и как CLI: сидер залил 10 рецептов, `GET /api/health` вернул
`{"status":"ok","recipes":10,"grammar":"soundline/v0"}`, `GET /api/llms.txt` — 11.9 КБ
`text/plain`, `retrieve` — `coin`, `fallback: false`.

Итоги и отклонения — §8.1–8.4.

Node + Fastify + better-sqlite3. Схема (SQL приведён в ТЗ дословно): таблица `recipes`
(id, name, prompt, soundline, profile_json, category, tags, duration_ms, rating,
created_at) + `recipes_fts` (fts5, content=recipes).

REST:

- `GET  /api/recipes?q=&category=&limit=` — FTS5 + фильтры
- `GET  /api/recipes/:id`
- `POST /api/recipes` — **валидирует soundline через @txt2sfx/core перед записью**
- `POST /api/recipes/:id/vote` — `{delta: 1|-1}`
- `GET  /api/retrieve?prompt=&k=3` — top-k для few-shot (v0: FTS-ранжирование по промпту
  и тегам; интерфейс готов к замене на embedding-поиск)
- `GET  /api/health`

`seed.ts` парсит все `examples/*.soundline`, рендерит, снимает профили, заливает в БД.

**Критерий приёмки:** `retrieve` по «coin pickup sound» возвращает coin в топ-1.

**Не забыть про требование заказчика:** этот сервер — точка входа для внешнего агента
без установки. Значит: CORS, `GET /api/health`, человекочитаемые ошибки и, желательно,
`GET /api/llms.txt` или аналогичная страница с грамматикой + примерами, чтобы чужой
агент за один запрос получил всё нужное для генерации soundline.

### 8.1. Отклонения от ТЗ

| Что | ТЗ | Сделано | Почему |
| --- | --- | --- | --- |
| БД | `better-sqlite3` | **`node:sqlite`** | Машина разработки — **Windows на ARM64**, а нативный аддон болит именно там: готовые бинарники под win32-arm64 исторически выходят последними, а без них установка сваливается в node-gyp и Visual Studio. В этом воркспейсе `pnpm.onlyBuiltDependencies` уже сужен до одного esbuild по той же причине. `node:sqlite` — часть бинарника Node, собирать нечего. Проверено здесь: `process.arch === 'arm64'`, SQLite 3.51.3, `ENABLE_FTS5` в `PRAGMA compile_options`, `bm25()` и external-content работают. Цена и точный диапазон версий — §8.5. |
| файлы | 4 файла | +`app.ts`, +`llms.ts` | `app.ts` — сборка приложения без `listen`, чтобы тесты гоняли настоящие маршруты через `app.inject()`. Сервер, HTTP-слой которого тестируется только поднятием порта, в итоге тестируется моками — а именно там и прячется неправильный статус-код. `llms.ts` — генератор документа для внешнего агента, см. §8.3. |
| валидация | «`POST /api/recipes` валидирует перед записью» | так и есть, но валидации **нет в слое БД** | Валидация — свойство HTTP-границы, куда приходит недоверенный вход. Сидер — не недоверенный вход, это собственный референсный набор репозитория, и ему нужно уметь загрузить рецепт с замечанием, *сообщив* о нём. См. §8.2. |
| `duration_ms` | одно поле | заявленная длительность в колонке, измеренная — в `profile_json` | Оба факта сохраняются и ни один не теряется: колонка несёт контракт из заголовка, профиль — то, что реально делает рендер. Для `helicopter` это 1500 и 3892 соответственно — ровно то расхождение, о котором говорит валидатор. |

`POST /api/recipes` читает `category` и `duration_ms` **из soundline**, а не из тела,
даже если клиент их прислал: рецепт, у которого сохранённая категория расходится с его
собственным заголовком, будет вечно находиться не теми запросами, а текст — единственное
поле, которое не может ошибаться о себе.

Профиль, наоборот, приходит на доверии: у сервера нет аудиостека и он не может его
пересчитать. Минимум, который он делает, — проверяет форму (`isProfile`), потому что
рецепт с полупустым профилем отравил бы все последующие сравнения, и никто ниже по
конвейеру не смог бы понять, почему.

### 8.2. helicopter в банке: решение принято в пользу «загружать и сообщать»

Дедлайн из §6.2 наступил здесь. Сидер валидирует каждый рецепт и **загружает всё**, печатая:

```
seeded 10 of 10 recipes
  stored  helicopter — error duration.contract
  stored  sword-clash — warn duration.contract

1 recipe(s) carry validation errors: helicopter.
They were stored anyway — ... Re-run with --strict to refuse them instead.
```

Почему так: отказ оставил бы банк без единственной зациклённой текстуры, и
`GET /api/retrieve?prompt=helicopter rotor loop` отвечал бы пустотой — тихая дыра в
демонстрации вместо видимого изъяна в одном рецепте. Флаг `--strict` переключает на
отказ (9 из 10) и выставляет ненулевой exit code; это режим для CI, когда находка из
§6.2 будет закрыта в ту или другую сторону. Оба режима под тестами.

**Решение заказчика (2026-08-05): отложено до конца работ.** Формулировка: «не знаю
пока, думаю уже когда всё будет готово можно решить. Может вообще придётся переделать.
Можно ослабить требования к рецептам».

Значит, на столе три варианта, и третий появился только сейчас:

1. переделать DSP ротора (`feedback ≤ 0.65`) — другой звук;
2. поднять `CATEGORY_LIMITS.cycle.maxDurationMs` до ~4500 и заголовок до 4046;
3. **ослабить сам инвариант** — например, сделать `duration.contract` предупреждением
   для `loop`-звуков, или поднять порог error с 25 % выше.

Третий вариант надо оценивать вместе с фазой 7: если бенчмарк покажет, что модель
регулярно натыкается на этот же инвариант на честных рецептах, требование слишком
строгое, и это довод сильнее любых рассуждений про один helicopter. До тех пор всё
работает: сидер грузит и сообщает, `--strict` отказывает, оба режима под тестами.
Текущая цена ясна и зафиксирована: банк содержит рецепт, который `POST /api/recipes`
не принял бы.

### 8.3. `GET /api/llms.txt` — точка входа для внешнего агента

11.9 КБ текста, **сгенерированного из тех же таблиц, которыми пользуются парсер и
валидатор**: `PRIMITIVES`, `EFFECTS`, `ENVELOPE`, `CATEGORY_LIMITS`, `GLOBAL_LIMITS`.
Написанный руками документ был бы верен в день написания и тихо неверен фазу спустя;
этот не может описать примитив, которого нет, или пропустить параметр, который есть.
Тесты проверяют, что упомянуты все 8 примитивов, все 6 эффектов и все 9 категорий с
их числами.

Содержимое: два правила, экономящие больше всего итераций (не починять сгенерированный
JS; писать `~value[min..max]` для всякого неуверенного числа), грамматика, таблицы
параметров с единицами/диапазонами/дефолтами, таблица категорий как контракт, список
глобальных инвариантов, honesty about limits, few-shot из банка и описание API.

Прочее по требованию §0: CORS открыт (`origin: true`) — банк публичный, секретов нет,
а закрывать его политикой одного источника значило бы заблокировать единственный
сценарий; 404 на неизвестный маршрут отсылает к `/api/llms.txt`; каждая ошибка несёт
машинный `error` и человеческий `message`, а отвергнутый soundline — ещё и `issues` с
их `hint`.

Ранжирование `retrieve`: FTS5 + bm25 с весами по колонкам (`name` 5, `tags` 3,
`prompt` 1). Промпт превращается в выражение с **`OR`**, а не с дефолтным неявным `AND`
(ни один рецепт не содержит сразу «coin», «pickup» и «sound»), и каждый токен
**закавычивается** — промпт это пользовательский текст, а `NEAR`, `OR` и `*` в FTS5
операторы, так что без кавычек поиск «near miss» это 500. Пустой ответ заменяется
фолбэком по рейтингу с флагом `fallback: true`: вызывающему нужны примеры грамматики,
и взять их больше негде, но принять фолбэк за ответ по промпту он не должен.

### 8.4. Обход бага инструментов: `node:sqlite` под Vite

`import { DatabaseSync } from 'node:sqlite'` корректен и работает, когда файл запускает
Node, но падает под Vite (то есть в тестах): Vite определяет встроенные модули, срезая
префикс `node:` и глядя имя в `module.builtinModules`, — а `sqlite`, пока он
экспериментальный, есть в этом списке **только под префиксом**. Vite заключает, что
существует npm-пакет `sqlite`, переписывает импорт на него и не может его найти.
`server.deps.external` не помогает: к моменту externalize префикс уже срезан, и Node
получает специфаер `sqlite`.

Решение — `process.getBuiltinModule('node:sqlite')` в `db.ts`: это штатный API ровно для
такой ситуации (достать встроенный модуль без специфаера, который бандлер может
переписать), production и тесты грузят один модуль одной строкой. Тип импортируется
обычным `import type` — типовые импорты стираются.

Апстрим это починили в **vitest 3** ([PR #7179](https://github.com/vitest-dev/vitest/pull/7179),
закрывает issue именно про `node:sqlite`); воркспейс на vitest 2.1.9. После бампа
обходной путь можно убрать и вернуть обычный импорт — это решение фазы 7 вместе с CI,
потому что мажорный бамп тестового раннера трогает все 462 теста.

Ещё одна мелочь того же рода: Node не отображает `.js` в импортах на `.ts`, поэтому
`node src/seed.ts` не работает при `verbatimModuleSyntax`-стиле импортов. Скрипты `dev`
и `seed` собирают проект и запускают `dist/`.

### 8.5. FTS5 — это флаг сборки Node, и Node его переключал

Самая ценная находка ревью. JS-API `node:sqlite` стабилен, но собран ли его SQLite с
FTS5 — **нет**: флага нет в Node 22.5–22.15, он появляется в 22.16, снова отсутствует
во **всех** 23.x и включён безусловно с 24.0. То есть «есть `node:sqlite`» не значит
«эта схема запустится», а падение выглядит как голое `no such module: fts5` из
`CREATE VIRTUAL TABLE` в глубине старта — читается как битая схема, а не как отсутствующий
флаг сборки.

Что сделано:

- `engines` исправлен с неверного `>=22.5.0` на `>=22.16.0 <23 || >=24.0.0`;
- `openBank` пробует FTS5 в `temp`-таблице до применения схемы и падает с внятным
  текстом, называющим версию Node и диапазон, — проверка в рантайме надёжнее любой
  строки в `engines`, которая всё равно носит совещательный характер;
- `--experimental-sqlite` убран из трёх скриптов: он нужен только на 22.5–22.12, где
  FTS5 всё равно нет, а на будущих версиях удалённый флаг превратился бы в фатальное
  «bad option». Проверено: на Node 24.15 сервер стартует без флага и **без всякого
  ExperimentalWarning** — прежняя запись про предупреждение тоже была устаревшей.

Заодно перепроверено утверждение про better-sqlite3: с v13 (июль 2026) он кладёт
N-API-прибилды прямо в свой tarball, включая `prebuilds/win32-arm64.node`, без
install-скрипта. То есть возражение «нужен тулчейн» к нему больше не применимо, и это
готовый путь отхода, если `node:sqlite` когда-нибудь отрегрессирует. Решение не меняется
— ноль зависимостей против одной на 17 МБ, — но записано в TSDoc `db.ts`.

---

## 9. Фаза 6 — веб-плейграунд

**Файлы:** `apps/web/src/App.tsx`, `components/{PromptBar,SoundlineEditor,LayerTimeline,
Visualizer,CompareView,ExportPanel,Gallery}.tsx`, `api.ts`. Vite + React + TS.

Сценарий: ввёл промпт → (нет ключа? кнопка деградирует до выбора ближайшего рецепта
через `/retrieve`) → звук играет, слои видны на таймлайне, soundline открыт в редакторе →
пользователь крутит слот-значения слайдерами (слайдеры генерируются из слотов `~`) →
Export: копировать JS / скачать WAV / копировать soundline. Правка текста немедленно
перепарсивается, ошибки парсера подчёркиваются в строке (`error.loc` уже содержит
`offset`/`length` именно для этого).

Тёмная тема, неоновые акценты по цветам слоёв (циан/пурпур/янтарь/изумруд),
моноширинный шрифт для soundline. Визуализатор — свой canvas-код, спектрограмма
с логарифмической осью частот.

**Плюс требование заказчика:** поле для вставки **Gemini API-ключа** прямо в UI
(в памяти вкладки, без localStorage), выбор провайдера Gemini/Anthropic/mock.

**Критерий приёмки:** сценарий проходит вручную end-to-end с mock-провайдером.

### 9.1. Фаза 6a — локальный плейграунд (готово, вынесен вперёд фаз 3–5)

Причина переноса: без интерактивного прослушивания качество примеров не доводится, а
работа через файлы и ручной рендер в WAV — слишком долгий цикл. Промпт-бар и поле
Gemini-ключа осознанно **не** сделаны: они требуют `packages/agent` (фаза 7) и валидатора
(фаза 3), без которого LLM будет уверенно выдавать нерабочий soundline. Всё остальное из
§9 закрыто, и это ровно та часть, которой не нужен ключ.

**Файлы:** `apps/web/` — `index.html`, `vite.config.ts`, `plugins/examples-fs.ts`,
`src/{main.tsx,App.tsx,styles.css}`, `src/lib/{examples,slots,engine,fft,highlight}.ts`,
`src/components/{Editor,Diagnostics,Gallery,Sliders,Timeline,Visualizer,ExportPanel}.tsx`.
Vite 5 + React 19 + TS, алиасы на `src/index.ts` пакетов (как в `vitest.config.ts`).

Ключевые решения, которые не стоит переоткрывать:

- **Источник истины — текст в редакторе.** Слайдеры не мутируют AST, а перезаписывают
  спан `NumberLiteral.loc` в исходнике. Важно: этот `loc` покрывает только числовой токен
  (`3200Hz`), без `~` и `[500..8000]`, поэтому рамка слота остаётся на месте, а редактор
  всегда показывает то, что звучит. После каждой правки офсеты сдвигаются — значит
  переспарсить и пересобрать список слотов до следующего изменения.
- **Два пути воспроизведения.** Play — живой граф через `buildGraph` (тот же путь, что у
  экспортируемого кода); Loop — офлайн-рендер через зацикленный `AudioBufferSourceNode`,
  потому что `cycle`-звук иначе не оценить, а зацикливание живого графа даёт шов на стыке.
- **Картинки рисуются по офлайн-рендеру, не по `AnalyserNode`** — изображение соответствует
  рецепту, а не моменту, и есть до нажатия Play.
- **Ось частот логарифмическая**, слайдеры частот и времён — тоже (`lib/slots.ts`):
  на линейной шкале `[500..8000]` вся полезная нижняя часть умещается в 5 % хода.
- **Запись в `examples/` только в dev** (`apply: 'serve'`, имя строго `[a-z0-9-]+`,
  каталог только `examples/`, перевод CRLF→LF из-за `.gitattributes`). Список рецептов в
  dev берётся с этого же эндпоинта, а не из `import.meta.glob`: glob — снапшот времени
  сборки и не видит только что сохранённый файл. HMR для glob-модуля намеренно
  проглатывается (`import.meta.hot.accept`), иначе Save вызывает full reload и убивает
  несохранённые буферы.
- **Save под другим именем = «сохранить копию»**: буфер исходного рецепта остаётся
  грязным, ничего не теряется молча.
- Свой мини-FFT в `src/lib/fft.ts` **не** переиспользовать в фазе 3: у анализатора другие
  требования (детерминизм, Node, `SoundProfile`), общая «утилита» испортит обоих.

Что осталось от §9 на фазу 7: промпт-бар, выбор провайдера, поле ключа и галерея из
банка вместо `examples/`. `CompareView` сделан — см. §9.3.
**Закрыто 2026-08-05, итоги и отклонения — §15.**

### 9.2. `bubble-pop` переписан под референс

Заказчик указал эталон — `C:\Projects\games\block_chpok\src\pop1.mp3` (игровой хлопок
пузыря) — и сообщил, что пресет на него не похож. Замер объяснил, почему.

| метрика (в момент хлопка) | референс | старый пресет | новый |
|---|---|---|---|
| доминирующая частота | 1055 Гц | **97 Гц** | 1098 Гц |
| центроид | 1400 Гц | 960 Гц | 1250 Гц |
| полоса 0.8–2 кГц | −3 дБ | −1 дБ | −2 дБ |
| полоса 2–6 кГц | −23 дБ | −31 дБ | −19 дБ |
| спад, дБ за 5 мс | 0/−2.8/−9.1/−16.6/−20.7 | 0/−7/−21.7/−34/−38 | 0/−1.6/−9.1/−16.9/−24.3 |

Диагноз: старый рецепт вёл частоту **вниз** (`tone sine 480Hz -> 80Hz`) и вся энергия
садилась на 97 Гц — глухой стук вместо хлопка. Референс делает наоборот: одиночная
резонансная частота ползёт **вверх** 960 → 1109 Гц за 8 мс (модель Миннарта: резонанс
каверны растёт по мере схлопывания), спад ≈1.2 дБ/мс, энергия сосредоточена в 0.8–2 кГц.

Важно для методики: «кластер партиалов 896/982/1060 Гц» в замере с окном 2048 сэмплов
(43 мс) — артефакт. Окно накрывало весь хлопок и смазало один скользящий тон в три
пика. С окном 512 картина однозначна. **Для транзиентов измерять коротким окном.**

Новый рецепт — два слоя, 876 Б: `tone tri` со свипом 700 → 1120 Гц под `lp 2200Hz`
(треугольник даёт нужные −20 дБ в полосе 2–6 кГц, синус давал −51 дБ и звучал ватно)
плюс `click 1ms` через `bp 2800Hz` на кромку. Отдельный низкий слой убран: он стоил
195 Б и выводил экспорт за потолок 900 Б, а его вклад в референсе объясняется ранней
частью того же свипа.

Не воспроизведено сознательно: у референса есть ~150 мс хвоста комнаты (−28 дБ,
≈1 кГц). В контракт `pop` (≤60 мс) он не влезает и является свойством помещения, а не
пузыря — это работа ревербератора движка. Вариант с `verb 170ms mix 0.3` проверен
(1224 Б, 10 узлов, категория `misc`) и лежит вне `examples/`, потому что тест
`render.test.ts` фиксирует ровно 10 рецептов.

### 9.3. Compare view — сравнение с референсом в плейграунде

**Файлы:** `src/components/Compare.tsx`, `src/lib/{analysis,draw}.ts`, `spectrumAt` в
`src/lib/fft.ts`. Референс загружается через file-picker или drop, декодируется в
браузере, живёт **только в памяти вкладки** — файл пользователя никуда не копируется.

Решения, без которых панель была бы бесполезной:

- **Выравнивание по онсету включено по умолчанию.** Записанный референс начинается
  когда включили запись, а не когда случился звук: в эталонном `pop1.mp3` хлопок стоит
  на 125 мс за тихим пре-роллом. Без выравнивания diff показывает картину сдвига, а не
  разницу звуков. Онсет ищется как **максимальный подъём** 1-мс RMS-огибающей, потом
  откат к «подножию» транзиента и уточнение до сэмпла. Порогом искать нельзя — порог
  цепляется за пре-ролл и выравнивает два звука по шуму комнаты.
- **Нормировка пиков тоже по умолчанию.** Референс 0.98 против рендера 0.79 — это
  постоянные 1.8 дБ по всему полю, они топят спектральную разницу, которая и нужна.
- **Декодирование в `OfflineAudioContext` на `GLOBAL_LIMITS.sampleRate`.** Платформа
  сама пересемплирует 48 кГц референс к 44.1 кГц рендера, и спектрограммы можно вычитать
  бин к бину. Иначе сетки бинов не совпадают.
- **Три режима:** `stacked` (кандидат над референсом), `overlay` (огибающие в одной
  панели — там наложение реально помогает) и `diff` (знаковая разница, расходящаяся
  палитра). Палитра diff — циан/янтарь, а не красный/зелёный: картинку должен уметь
  прочитать любой читатель, включая дальтоника и vision-модель.
- **Переключатель окна `full` / `zoom`.** Хвост комнаты у референса длиннее кандидата в
  5 раз и на общей оси сжимает исследуемые первые 40 мс в полоску у левого края.
- **Канвас самодостаточен:** в шапке имена обоих сигналов, режим и состояние
  выравнивания/нормировки, в diff — легенда направления. Картинка, которую нельзя
  прочитать без окружающего UI, бесполезна ровно в тот момент, когда её скопировали.
  `Copy PNG` / `Save PNG` дают 1761×579 (~70 КБ), `Copy numbers` — markdown-таблицу
  метрик; для vision-модели полезно отдать и то и другое.
- Метрики считаются в окне **512 сэмплов (~12 мс)** у собственного онсета каждого
  сигнала — см. предупреждение из §9.2 про смазывание длинным окном.

Сверка с офлайн-замерами сошлась: панель находит онсет референса на 124.7 мс (вручную
было 125) и даёт спад 0/−3/−9/−17/−21/−30 дБ (вручную 0/−2.8/−9.1/−16.6/−20.7/−30.0).

`lib/draw.ts` — общий слой рисования для `Visualizer` и `Compare`: палитра, лог-ось,
осциллограмма. Иначе суждение, сложившееся в одной панели, не переносится в другую, а
это и есть смысл сравнения.

---

## 10. Фаза 7 — агент, бенчмарк, документация, CI (готово)

**Готово 2026-08-05, 540/540 тестов.** Ниже — исходное ТЗ фазы, как оно было
сформулировано; что и почему сделано иначе — §14.

**Файлы:**

```
packages/agent/src/{provider.ts,anthropic.ts,gemini.ts,mock.ts,prompts.ts,loop.ts}
bench/{run.ts,README.md,targets/*.json}
docs/{ARCHITECTURE,SOUNDLINE_GRAMMAR,PRIMITIVES,ACOUSTIC_PROFILE,AGENT_LOOP,API}.md
README.md, LICENSE (Apache-2.0), CONTRIBUTING.md, ROADMAP.md, .env.example
.github/workflows/ci.yml, tsconfig.test.json
```

- `LLMProvider { complete(msgs): Promise<string> }`; реализации: Anthropic, **Gemini**
  (ключ из env или передан вызовом — см. §0), mock с зашитыми ответами.
- `prompts.ts` — системный промпт: грамматика + физика + few-shot из банка (`/api/retrieve`).
- `loop.ts` — `generateSound(prompt, opts)`: prompt → soundline → validate → render →
  метрики/diff → правка → ... до порога или лимита итераций.
  **Жёсткое правило:** LLM меняет топологию только если оптимизатор упёрся
  (fitness не улучшился за N поколений и остаётся выше порога).
- `bench/run.ts` — для каждой цели из `targets/*.json`: agent + optimizer → скор → таблица.
- README по структуре образца img2threejs: логотип-заглушка (SVG), слоган
  *«Text-to-SFX as code. Sounds that weigh bytes, not megabytes.»*, три абзаца «что это»,
  блок «убийственный пример» (5 строк soundline рядом с ~40 строками Web Audio и подписью
  о размере экспорта), mermaid-диаграмма полного цикла, quick start, таблица примитивов,
  **honesty about limits** (процедурный синтез силён в SFX — удары, щелчки, зумы, UI,
  sci-fi — и слаб в органике: голос, реалистичные животные, сложные текстуры),
  roadmap/contributing/license.
- ROADMAP: v0 прототип → v0.2 CLAP-судья и vision-сравнение спектрограмм →
  v0.3 SFX-Bench 50 целей + публичная таблица → v0.4 showcase на GitHub Pages →
  v1.0 стабильная грамматика.
- CI: lint + test + build на PR. Добавить `tsconfig.test.json` — сейчас `tsc -b`
  покрывает только `src`, тесты типизируются отдельным вызовом.

---

## 11. Хвосты и напоминания

- [x] `git init` + первый коммит.
- [x] Замерить пик в примерах — сделано, правка не потребовалась (§5.3).
- [x] `LICENSE` (Apache-2.0, полный текст) — лежит в корне, 11.5 КБ.
- [x] README актуализирован под фазы 1–2 + 6a (2026-08-05): «убийственный пример»
      показывал **старый** `bubble-pop` с нисходящим свипом — тот самый антипаттерн,
      который §9.2 забраковал, а правило `pop.rising-resonance` должно ловить;
      заменён на актуальный рецепт. Плюс поправлены число тестов (было 90),
      состав репозитория (не было `apps/web`), имена экспортов в примере кода
      (`codegen`/`renderSound`, а не выдуманные) и формулировка про размер экспорта
      по §5.4. Полноценный README фазы 7 (mermaid, таблица примитивов) — всё ещё впереди.
- [x] `tsconfig.test.json` для типизации тестов в CI — сделано, плюс `test/` в корне
      для репозиторных проверок (дрейф документации). `pnpm typecheck` теперь
      прогоняет `tsc -b` по src, `tsc -p tsconfig.test.json` по src+тестам и
      typecheck плейграунда.
- [x] Перенести замороженные решения §3 в `docs/SOUNDLINE_GRAMMAR.md`,
      решения §5.1–5.2 — в `docs/{ARCHITECTURE,PRIMITIVES}.md` — сделано. Заодно
      исправлены два устаревших числа: `bubble-pop` весит **876 Б**, а не 824, и его
      пик **0.785**, а не 0.859 (таблицы §5.3–5.4 писались до переписывания рецепта
      в §9.2). Замеры пересняты для всех десяти рецептов.
- [x] Локальный плейграунд: редактор, слайдеры слотов, визуализатор, экспорт,
      запись в `examples/` (§9.1). Промпт-бар остался на фазу 7.
- [x] **Остаток фазы 6: промпт-бар, выбор провайдера, поле ключа, галерея из банка** —
      сделано 2026-08-05, плюс публикация рецепта в банк и показ замечаний валидатора
      в плейграунде. Итоги и отклонения — §15.
- [x] **Gemini-провайдер** — сделано (`packages/agent/src/gemini.ts`), ключ приходит
      аргументом, уходит в заголовке `x-goog-api-key`, а не в query string (§14.2).
      **Поле ключа в UI — сделано (§15).** Дефолтная модель сменена на
      `gemini-3.5-flash-lite`: заказчик получил от вендора 404 «`gemini-2.5-flash`
      is no longer available to new users». Отсюда же поле «model id» в плейграунде
      и экспорт `GEMINI_DEFAULT_MODEL` / `ANTHROPIC_DEFAULT_MODEL` (§15.4).
- [x] Точка входа для внешнего агента без установки — `GET /api/llms.txt`, генерируется
      из таблиц парсера и валидатора, плюс открытый CORS и подсказка в 404 (§8.3).
- [x] **Фаза 5, копирование сэмплов** — сидер копирует буфер (§8, §7.2).
- [x] **Решение по helicopter в банке:** сидер загружает и сообщает, `--strict` отказывает
      (§8.2).
- [ ] **helicopter, сам рецепт — отложено заказчиком до конца работ** (§8.2). Третий
      вариант, появившийся при этом решении: ослабить инвариант, а не рецепт. Оценивать
      по данным бенчмарка фазы 7. **Данные появились, см. §14.6:** шесть целей проходят,
      ни одна не спотыкается о `duration.contract`, единственный warn во всём наборе —
      `sword-clash` (17 %). Доказательств, что инвариант слишком строг, нет; а риск
      «агент учится на невалидном примере» закрыт отдельно и независимо (§14.3).
      Решение по рецепту больше ничего не блокирует.
- [x] Фаза 3: учтены §5.3 (пик проверяется только на рендере — отдельный
      `validateRender`) и §5.5 (helicopter пойман, см. §6.2).
- [x] **Правило `envelope.attack-longer-than-voice`** — сделано, severity по величине:
      `attack > width` — error, `> width/2` — warn. Проверено на кейсе из §11
      (`click 0.3ms` под дефолтным `attack 1ms`).
- [x] **Правило `pop.rising-resonance` (warn)** — сделано, ловит нисходящий свип
      источника в категории `pop`. См. §9.2.
- [ ] **Анализатор + `decodeAudioData`:** `node-web-audio-api` умеет читать
      mp3/wav/flac/ogg через symphonia — референсные сэмплы без ffmpeg. Сам
      анализатор к этому готов (вход — `{ samples, sampleRate }`), но чтения
      референсного файла в тестах пока нет: все проверки идут на синтетических
      сигналах и рендерах. Нужно для фазы 4, когда появится цель-референс.
- [ ] **Открытый вопрос к заказчику (§6.2):** `helicopter` — error по контракту
      длительности, и правкой заголовка не лечится. Принять как внеконтрактный
      пример или менять `feedback` ротора.
- [x] Фаза 4: пик после оптимизации — сделано штрафом в fitness, есть тест на
      `explosion` (§7.1).
- [x] **Копирование сэмплов** — правило на будущее: всё, что держит `getChannelData`
      дольше своего `OfflineAudioContext`, обязано копировать (§7.2). Учтено в `bench/`
      и в тестовых хелперах агента; правило записано в `CONTRIBUTING.md` и
      `docs/ARCHITECTURE.md`.
- [x] Фаза 7 README: писать «300–1000 байт для типичного SFX», не «всегда < 1 KB» (§5.4) —
      сделано при актуализации README, повторить в финальной версии.

---

## 12. Порядок дальнейшей работы

Очередь определена зависимостями, а не удобством. Валидатор и анализатор идут первыми,
потому что от них зависят все остальные незакрытые фазы: оптимизатору нужен
`profileDistance` как fitness, агенту — `validate` + `humanReadableDiff` в промпте
(без валидатора LLM уверенно выдаёт нерабочий soundline, поэтому промпт-бар и был
намеренно отложен, §9.1), серверу — `profile_json` в схеме рецептов.

1. ~~**Фаза 3 — валидатор + анализатор** (§6).~~ **Готово 2026-08-05**, 339/339
   тестов. Итоги и отклонения — §6.1–6.5.
2. ~~**Фаза 4 — оптимизатор** (§7).~~ **Готово 2026-08-05**, 389/389 тестов.
   Итоги и отклонения — §7.1–7.2.
3. ~~**Фаза 5 — сервер банка** (§8).~~ **Готово 2026-08-05**, 455/455 тестов.
   Итоги и отклонения — §8.1–8.4.
4. ~~**Фаза 7 — агент, бенчмарк, docs, CI** (§10).~~ **Готово 2026-08-05**, 540/540
   тестов. Итоги и отклонения — §14.
5. ~~**Фаза 6, остаток** (§9).~~ **Готово 2026-08-05**, 573/573 теста. Итоги и
   отклонения — §15. Исходная формулировка шага, для истории: промпт-бар, выбор провайдера
   Gemini/Anthropic/mock, поле Gemini-ключа (в памяти вкладки, без localStorage),
   галерея из банка вместо `examples/`. Порядок поменялся местами сознательно:
   агент был предпосылкой, теперь это действительно тонкий слой поверх готового API.
   Что уже лежит навстречу:
   - `generateSound({ prompt, provider, render, bank, onEvent })` — весь цикл одним
     вызовом; `onEvent` отдаёт `request/reply/validated/rendered/optimized/feedback/done`,
     то есть панель прогресса рисуется без собственной логики;
   - `geminiProvider({ apiKey })` / `anthropicProvider({ apiKey })` / `mockProvider(...)` —
     ровно три пункта в переключателе провайдера; ключ передаётся аргументом и нигде
     не кэшируется, так что «в памяти вкладки» — это просто React state;
   - `httpBank(baseUrl)` — галерея и few-shot из банка, с уже готовым `fallback`-флагом;
   - `render` — уже есть в плейграунде (`lib/engine.ts`), инжектируется как есть;
   - в браузере провайдеры работают без изменений: это `fetch`, а не SDK (§14.2).

Правило на каждый шаг: в конце фазы тесты зелёные, и этот документ обновлён — статус
в шапке, чекбоксы §11 и запись об отклонениях от плана с обоснованием, как в §5.2.

---

## 13. Внешнее ревью фаз 3–5 (2026-08-05)

Заказчик попросил пересмотреть решения фаз 3–5 вторым мнением, отдельно — выбор SQLite
на win32-arm64. Ревью подтвердило выбор и нашло четыре настоящих дефекта. Все закрыты,
каждый с регрессионным тестом; итог 462/462.

| # | Находка | Где | Что сделано |
| --- | --- | --- | --- |
| 1 | `engines: ">=22.5.0"` неверен: FTS5 нет в 22.5–22.15 и во всех 23.x | `apps/server/package.json`, `db.ts` | Диапазон исправлен, добавлена проверка FTS5 в рантайме, убран мёртвый `--experimental-sqlite`. §8.5 |
| 2 | Счётчик простоя DE не замечал улучшения самого лидера | `optimizer/src/de.ts` | Прогресс считается сравнением до/после поколения. §7.1 |
| 3 | Идемпотентность сидера ломалась после 100 рецептов: дедупликация шла по `list`, ограниченному `MAX_LIMIT` | `server/src/seed.ts`, `db.ts` | Добавлен `hasName`, спрашивается по имени. Тест сажает в банк 125 рецептов. |
| 4 | Профиль тишины (`-Infinity` LUFS) не переживал JSON: клиент получал «missing-profile», отправив профиль | `analyzer/src/profile.ts` | Введён `SILENCE_LUFS = -70`, `extractProfile` клампит. Тест проверяет round-trip через JSON. |
| 5 | Повторный `POST /api/recipes` создавал дубликат | `server/src/routes/recipes.ts` | Идентичный (name, soundline) возвращается с 200 и `created: false`; другой soundline под тем же именем по-прежнему создаётся. |
| 6 | Подсказка `attack-longer-than-voice` называла −16 дБ там, где замер §11 дал −41 | `core/src/validate/invariants.ts` | Число убрано: огибающая считается, а слышимая потеря зависит от цепочки и спектра источника. Инструкция осталась. |
| 7 | Закрывающий бэктик приклеивался к рецепту без завершающего перевода строки | `server/src/llms.ts` | Guard на `\n`. |

Замена `createRequire` на `process.getBuiltinModule` и путь отхода к better-sqlite3 v13+
— тоже из ревью, см. §8.4 и TSDoc `db.ts`.

Подтверждено без изменений: расщепление валидации на `validate`/`validateRender`;
severity по величине; `param.range` как error; исключение для `-> 0`; кандидат-как-текст
в оптимизаторе; порядок штрафов (invalid 5 > clipping ≤3 > чистые ≤1); лог-оси в кубе
поиска; валидация только на HTTP-границе; чтение категории и длительности из soundline;
`OR` + закавычивание в FTS; фолбэк с флагом; генерация `llms.txt` из живых таблиц;
`app.ts` ради `inject()`.

Одно предупреждение ревью на будущее, важное для фазы 7: банк — источник few-shot для
агента, и пока в нём лежит `helicopter`, на который `validate` даёт error, агент может
сымитировать собственный пример и быть отвергнутым тем же конвейером, который этот
пример ему выдал. Это усиливает пункт про ослабление инварианта из §8.2: решать лучше по
данным бенчмарка, но **до** того, как цикл агента начнёт учиться на банке.

**Закрыто в фазе 7 — §14.3.** Решение заказчика: не выбирать судьбу `helicopter`, а
сделать выбор ненужным — few-shot берётся с предпочтением рецептов, проходящих
валидацию (`selectFewShot`), причём и в промпте агента, и в `GET /api/llms.txt`.
Противоречие исчезает при любой судьбе рецепта.

---

## 14. Фаза 7 — итоги (2026-08-05)

**540/540 тестов зелёные** (было 462; +62 у агента, +8 у bench, +8 репозиторных).
`tsc -b` по пяти пакетам и серверу чистый, `tsc -p tsconfig.test.json` (src + тесты)
чистый, `pnpm bench --strict` — 6 из 6, `pnpm build:web` собирается.

**Файлы:**

```
packages/agent/src/{provider,anthropic,gemini,mock,contract,fewshot,bank,prompts,loop,index}.ts
packages/agent/test/{extract,prompts,fewshot,providers,loop}.test.ts, test/helpers/fixtures.ts
bench/{package.json,tsconfig.json,README.md}, bench/src/{run,targets}.ts,
bench/targets/0{1..6}-*.json, bench/test/targets.test.ts
docs/{ARCHITECTURE,SOUNDLINE_GRAMMAR,PRIMITIVES,ACOUSTIC_PROFILE,AGENT_LOOP,API}.md
test/docs.test.ts, tsconfig.test.json, .github/workflows/ci.yml
README.md (переписан), CONTRIBUTING.md, ROADMAP.md, .env.example, assets/logo.svg
```

### 14.1. Отклонения от ТЗ фазы

| Что | ТЗ | Сделано | Почему |
| --- | --- | --- | --- |
| `LLMProvider` | `complete(msgs)` | `complete({ system, messages, maxTokens, signal })` | Голый массив сообщений не может нести системный промпт, а он у нас на 12 КБ и оба вендора принимают его отдельным полем (`system` у Anthropic, `systemInstruction` у Gemini). Уплощать его в список — значит заставлять каждого провайдера распаковывать обратно, и ошибаться в этом молча: системный промпт, поехавший как user-turn, всё равно даёт правдоподобный ответ. |
| `llms.txt` | генератор в `apps/server` | **перенесён в `packages/agent/src/contract.ts`**, сервер импортирует | §12 требовал, чтобы промпт агента брал грамматику оттуда, а не дублировал. Перенос выполняет это буквально: одна функция, два потребителя — HTTP для чужого агента и in-process для нашего цикла. Вариант «агент делает GET /api/llms.txt» отвергнут: он делает банк предпосылкой генерации и добавляет сетевой отказ там, где документ выводится из локальных таблиц. Цена — `apps/server` зависит от `@txt2sfx/agent` (пакет без runtime-зависимостей). |
| bench | `bench/run.ts` в корне | workspace-пакет `@txt2sfx/bench` с `src/` и `dist/` | Тот же путь, что у сервера (`tsc -b && node dist/`), и по той же причине: Node не отображает `.js`-импорты на `.ts` (§8.4), а запускать бенчмарк через vitest — значит мерить под тестовым раннером. Плюс `pnpm bench` из корня и свои devDependencies (`node-web-audio-api`). |
| цели bench | `{ prompt, target }` | + **сценарий ответов модели** (`attempts`) | Бенчмарк, которому нужен ключ, никто не запускает; бенчмарк, чьи числа меняются с обновлением модели, не отвечает на вопрос «сломался ли репозиторий». Скриптованный ответ делает прогон воспроизводимым: тот же вход, та же «модель», и подвинуть скор может только наш код. `--provider gemini\|anthropic` игнорирует сценарий и превращает те же цели в оценку модели. |
| CI | «lint + test + build» | typecheck (src+тесты) + test + build + build:web + bench --strict + сидер | Линтера в проекте нет и он не добавлялся: TypeScript уже идёт со `strict`, `noUncheckedIndexedAccess` и `exactOptionalPropertyTypes`, а `tsconfig.test.json` закрывает единственную настоящую дырку — `tsc -b` не видит тесты, а тест, который не типизируется, обычно всё равно исполняется. Заводить ESLint ради стиля в проекте, где стиль держится ревью и TSDoc, — это конфиг, который начнёт спорить с решениями §5.2. |
| матрица CI | не оговорено | Node 22.16 и 24 | Ровно границы поддерживаемого диапазона из §8.5: 22.16 — первая версия с FTS5, 24 — где флаг включён безусловно. Прогон на 22.16 держит это утверждение честным. |
| vitest | «после бампа обходной путь можно убрать» (§8.4) | **остаёмся на vitest 2** | Единственная выгода бампа — снять две строки `process.getBuiltinModule`, а это штатный API ровно для такой ситуации, а не костыль. Мажорный бамп раннера трогает все 540 тестов и не даёт пользователю ничего. Решение зафиксировано как решение, а не забыто. |

### 14.2. Провайдеры на `fetch`, а не на SDK

Оба провайдера — сырой HTTP через `fetch`, без вендорских SDK. Решают два требования:
пакеты несут **ноль runtime-зависимостей** (§4), и плейграунд должен работать с ключом,
который пользователь вставил в браузере, — значит один и тот же код провайдера
исполняется в Node и на странице, и каждый его байт уезжает в бандл. SDK на вендора —
это два дерева зависимостей и проблема бандлинга ради десятка строк `fetch`.

Цена названа честно в TSDoc: ретраи, отображение ошибок и формы ответов теперь наши, и
изменение формата на стороне вендора ломает нас там, где бамп SDK бы починил. Поэтому
каждая форма запроса задокументирована с версией API, под которую написана, а политика
ретраев живёт в одном месте (`providerFetch`).

Детали, которые не вкусовые:

- Anthropic: **не отправляются** `temperature`/`top_p`/`top_k` и никакой `thinking` —
  на текущем семействе моделей это 400, а не «менее случайные ответы». Есть тест,
  падающий, если кто-то вернёт их обратно. `stop_reason: 'refusal'` приходит **успешным**
  200 с пустым `content`, поэтому проверяется до чтения текста; `max_tokens` —
  ошибка, а не рецепт, потому что половина рецепта парсится до половины слоя и падает.
- Gemini: ключ в заголовке `x-goog-api-key`, **никогда** в query string — ключ в URL
  оседает в логах прокси, истории браузера и `Referer`, а он чужой.
- mock: кончился сценарий — исключение, а не цикл по кругу. Цикличный mock превращает
  баг цикла в бесконечный разговор, который выглядит как «медленно».
- Ни один провайдер не читает ключ из окружения сам: плейграунд держит вставленный ключ
  в памяти вкладки и передаёт вызовом, а провайдер, тихо падающий на `process.env`,
  использовал бы неверный ключ ровно тогда, когда они расходятся.

### 14.3. Few-shot: предпочтение проходящих валидацию (решение заказчика)

`selectFewShot(recipes, k)`: набирать примеры из рецептов, проходящих валидацию;
**меньше примеров лучше, чем пример, который конвейер отверг бы.** Откат на невалидные
— только если чистых нет вовсе (промпт без примеров не учит форме языка), и тогда рядом
называется правило, которое каждый из них ломает, чтобы модель копировала структуру, а
не дефект.

Применяется в двух местах: в системном промпте агента и в `GET /api/llms.txt`, где
роут запрашивает вдвое больше рецептов и фильтрует. Так защита достаётся и чужому
агенту, а не только нашему циклу.

Что это даёт помимо ответа ревью: вопрос «что делать с `helicopter`» перестал быть
блокирующим. Починят рецепт, ослабят инвариант или не сделают ни того ни другого —
агент всё равно не учится на примере, который конвейер отвергает. Тест
`fewshot.test.ts` начинается с проверки предпосылки: если `helicopter` когда-нибудь
станет валидным, тест скажет об этом, а не будет молча проверять пустоту.

### 14.4. Жёсткое правило и второй случай «упёрся»

ТЗ: LLM меняет топологию, только если оптимизатор упёрся. Реализовано, и по ходу
нашёлся второй, не менее законный случай — **рецепт без единого `~`-слота**. Фитить
нечего, значит расстояние, на котором он рендерится, и есть расстояние этой структуры,
и менять остаётся только структуру. Это к тому же частый вывод модели: первая попытка
охотно пишет уверенные литералы.

Важно: по `de` этого не прочитать. При `dimensions === 0` оптимизатор возвращает
`stopped: 'target'` — «оптимизировать нечего», а не «порог достигнут». Поэтому в цикле
`stuck = de.stopped === 'stalled' || slots.length === 0`, с комментарием.

Обратная сторона правила тоже реализована: если поиск **всё ещё улучшался** и просто
исчерпал бюджет поколений, цикл заканчивается с `outcome: 'distance'` и не тратит
итерацию модели. Просить новую топологию там — значит выбросить работающий поиск; это
та же асимметрия, из-за которой `stallGenerations` = 20, а не 12 (§7.1).

### 14.5. Мелкие находки фазы

- **Отказ модели — это ответ.** Ответ без единого ограждения кода завершает прогон с
  `outcome: 'refused'` и текстом модели в `message`. Контракт сам велит отказываться от
  голоса и правдоподобных животных одним предложением; требовать в ответ на это
  «пришли fenced block» — значит выпросить ровно тот разочаровывающий звук, против
  которого инструкция и написана. Ответ *с* ограждением, но без рецепта — другое дело,
  он ремонтируется.
- **Извлечение рецепта не берёт fenced-блок без заголовка.** Соблазнительное «ну возьми
  тогда последний блок» читает JS-сниппет как рецепт и превращает честное «ты не прислал
  soundline» в ошибку парсинга чужого `play(ctx, 0)`: одна потраченная итерация и
  путаница в транскрипте. Найдено тестом, исправлено в `prompts.ts`.
- **Запрос — это значение, а не живой массив.** Цикл передавал провайдеру тот же
  мутируемый `messages`, который потом дополнял; mock записывал ссылку, и «транскрипт»
  первого вызова показывал последний ответ. Теперь передаётся копия — заодно это
  корректно для любого провайдера, который очередь или ретрай делает сам.
- **`param.slot-range` поймал сам себя на цели bench:** слот `click ~1ms[0.5..6]` вышел
  за задокументированный предел `width` (5 мс) — правило сработало на нашем же
  материале, диапазон исправлен. Второй warn (`envelope.attack-longer-than-voice`) снят
  добавлением `attack 0ms` — той самой идиомы, которой контракт учит модель для щелчков.
- **Устаревшие числа в плане** (см. §11): `bubble-pop` — 876 Б и пик 0.785. Замеры
  пересняты для всех десяти рецептов и лежат в `docs/PRIMITIVES.md`.

### 14.6. Что показал бенчмарк

Шесть целей, скриптованная модель, profile-only метрика, оптимизатор 16 × 24, seed 1,
~0.3 с на весь прогон:

| цель | путь | расстояние | порог | экспорт |
| --- | --- | --- | --- | --- |
| bubble-pop | accepted | 0.090 → 0.031 | ≤ 0.100 | 877 Б |
| ui-click | accepted | 0.192 → 0.077 | ≤ 0.100 | 786 Б |
| coin | invalid → accepted | 0.000 → 0.000 | ≤ 0.050 | 531 Б |
| laser | distance → accepted | 0.000 → 0.000 | ≤ 0.050 | 553 Б |
| footstep-gravel | accepted | 0.171 → 0.005 | ≤ 0.120 | 1708 Б *(over)* |
| explosion | render → accepted | 0.000 → 0.000 | ≤ 0.050 | 1458 Б *(over)* |

Колонка «путь» — самое информативное, что есть в отчёте: `invalid → accepted` значит,
что валидатор поймал категорию и модель исправилась, `render → accepted` — что поймано
клиппирование буфера, `distance → accepted` — что сработало жёсткое правило и модель
переделала структуру. Цель, у которой путь схлопнулся в одинокое `accepted`, перестала
проверять свою стадию — регрессия, которую колонка расстояния не покажет; поэтому форма
каждой цели зафиксирована тестами (`bench/test/targets.test.ts`).

Про инвариант из §8.2 бенчмарк говорит следующее: ни одна из шести целей не спотыкается
о `duration.contract`, единственное предупреждение во всём наборе — `sword-clash` с
перерасходом 17 %. Данных в пользу «требование слишком строгое» нет. Признать это
доказательством обратного тоже нельзя: шесть целей — это шесть целей, и настоящую
статистику даст v0.3 с пятьюдесятью.

### 14.7. Что осталось открытым

- ~~Остаток фазы 6 — §12, пункт 5.~~ **Закрыт 2026-08-05, §15.**
- `decodeAudioData` в тестах анализатора (референс-файлом вместо рендера) — ушло в
  v0.3 `ROADMAP.md` вместе с набором сэмплов и его лицензированием.
- Судьба `helicopter` — §11, больше ничего не блокирует.
- Перцептивный судья (CLAP, vision-сравнение спектрограмм) — v0.2.

---

## 15. Фаза 6, остаток — итоги (2026-08-05)

**617/617 тестов зелёные** (было 540; +33 в `apps/web/test`). `tsc -b` по пяти пакетам и
серверу чистый, `tsc -p tsconfig.test.json` чистый, `tsc -p apps/web/tsconfig.json`
(теперь включает и тесты приложения) чистый, `vite build` собирается: 330 КБ JS /
109 КБ gzip — в бандл входят React, `core`, `analyzer`, `optimizer` и `agent`, потому что
цикл целиком исполняется на странице.

**Файлы:**

```
apps/web/src/lib/{agent,bank,catalog}.ts          — провайдеры и рендер для цикла,
                                                    клиент банка, состав галереи
apps/web/src/components/PromptBar.tsx             — промпт, провайдер, ключ, model id, лог
apps/web/src/components/{Gallery,Diagnostics}.tsx — группы по origin; замечания валидатора
apps/web/src/App.tsx                              — интеграция, публикация в банк
apps/web/test/{agent,bank,catalog}.test.ts        — 31 тест
packages/agent/src/{gemini,anthropic,index}.ts    — экспорт дефолтных id моделей
```

Плюс правки: `apps/web/{package.json,vite.config.ts,tsconfig.json}` (три новых
воркспейс-зависимости и алиасы к ним), `styles.css`, `README.md`, `ROADMAP.md`,
`docs/AGENT_LOOP.md` (раздел «In the playground»).

### 15.1. Галерея: банк **рядом с** `examples/`, а не вместо

Единственное существенное отклонение от формулировки §9/§12. ТЗ говорило «галерея из банка
**вместо** `examples/`»; сделан переключатель источника.

Причина не в осторожности, а в том, что эти два хранилища делают разное. `examples/`
пишется под `vite dev` (§9.1) — это место, где рецепт **авторится**, и именно там лежит
референсный набор репозитория, на который опираются тесты и документация. Банк —
общий, поисковый, источник few-shot для агента; туда рецепт **публикуется**. Замена
первого вторым: (а) выключила бы Save и осмысленную работу без запущенного сервера,
(б) сделала бы галерею пустой в статической сборке, (в) в момент, когда сервера нет,
оставила бы плейграунд вообще без рецептов — а mock-провайдер отвечает именно из них.

Третий источник появился по ходу и оказался нужен: `session` — рецепты, сгенерированные
только что и не сохранённые никуда. Они идут первыми и перекрывают одноимённые
хранимые (`lib/catalog.ts`): две строки с одним именем выбирали бы один и тот же буфер
редактора и отличались бы только значком. Группы в сайдбаре подписаны, потому что
разница между ними — не происхождение, а что с рецептом можно сделать: session не
сохранён, `examples/` пишется, банк опубликован.

### 15.2. Сверх ТЗ: публикация в банк и замечания валидатора в UI

Оба добавления закрывают дыры, которые стало видно именно из-за появления входной двери.

**Публикация (`POST /api/recipes` из сайдбара).** Без неё банк навсегда содержал бы
десять засеянных рецептов: эндпоинт существовал с фазы 5, но дотянуться до него из UI
было нечем, и «галерея из банка» показывала бы ровно то же, что `examples/`. Профиль
измеряется в браузере (`extractProfile` по буферу, который сейчас на экране) — у сервера
нет аудиостека, и он такой рецепт не примет; заодно сохранённый отпечаток принадлежит
именно тому звуку, который прослушали. Ошибки не глотаются: `422` с `issues` печатается
с `hint`-ами, потому что эти сообщения написаны, чтобы по ним что-то сделать.

**Замечания валидатора в `Diagnostics`.** Плейграунд (фаза 6a) старше валидатора
(фаза 3), и до сих пор инварианты применялись только внутри цикла агента. Правка руками
могла молча дать `pop` на 400 мс — рецепт, который `POST /api/recipes` отверг бы, а
цикл агента вернул бы модели. Теперь `validate(ast)` идёт по тому же тексту, что и всё
остальное, и `severity` сохраняется: `error` красный, `warn` янтарный. Проверено на
`helicopter` из банка — панель показывает `duration.contract`, 4046 мс при заявленных
1500, с хинтом про `feedback`.

### 15.3. Что добавляет браузер к готовому циклу

| Что | Решение | Почему |
| --- | --- | --- |
| ключ | React state, `password`, передаётся в фабрику провайдера на каждый запуск | Требование §0 буквально: живёт в памяти вкладки, уходит только в запрос, который начал пользователь. Ни `localStorage`, ни модульного кэша; провайдеры и так не читают окружение сами (§14.2). `autoComplete="new-password"`, а не `off`: Chrome игнорирует `off` на password-поле и подставил в него сохранённый пароль сайта. |
| Anthropic | заголовок `anthropic-dangerous-direct-browser-access: true`, добавляется инжектируемым `fetch` приложения | Без него API отказывает странице, и отказ приходит непрозрачной CORS-ошибкой без тела. Это факт про браузер, а не про провайдера, поэтому он не зашит в пакет, который исполняется и в Node. |
| mock | отвечает из рецептов на странице, ранжированных по промпту; на каждый вызов — следующий кандидат; кончились — одно предложение без ограждения кода | Критерий приёмки фазы («сценарий проходит с mock-провайдером») требует полного круга без ключа. Следующий кандидат вместо повторения одного и того же прогоняет ремонтную ветку цикла, а не заглушает её; предложение без ограждения цикл читает как отказ (§14.5) и печатает дословно — правильный конец скриптованного разговора. |
| оптимизатор | 16 × 14 вместо 16 × 24 у бенчмарка | Поколение — это популяция офлайн-рендеров на UI-потоке; поиск на минуту читается как зависание. Когда результат близок, но недостаточно, ручной ход — панель Slots: тот же поиск, только слышно сразу. |
| цель | галочка `match reference` превращает референс из Compare-панели в `target` цикла | Референс уже декодирован в памяти вкладки (§9.3), а выравнивание по онсету и нормировку делает анализатор, так что подрезать его не нужно. Без галочки рецепт принимается, как только парсится, валидируется и звучит, — честный потолок для «сделай лазер». |
| прогресс | одна строка на событие `onEvent` | Это и есть аргумент проекта в миниатюре: валидатор поймал категорию, рендер — клиппинг, оптимизатор двинул числа, модель спросили ещё раз только там, где ей есть что сделать. Спиннер прятал бы ровно это. |
| имя рецепта | слаг из промпта, коллизия — суффикс `-2` | Два прогона «laser» — это два кандидата для сравнения; молча перезаписать первый значило бы выбросить сравнение, которое пользователь в этот момент и делает. |
| сайдбар | `position: sticky`, высота вьюпорта | В подвале стало пять контролов вместо трёх, а главная колонка длиной в несколько экранов: без этого Save и Publish парковались где-то под Compare-панелью. |

### 15.4. Дефолтная модель Gemini сменена по факту 404 от вендора

Заказчик запустил генерацию с настоящим ключом и получил
`404 { "message": "This model models/gemini-2.5-flash is no longer available to new users" }`.
Дефолт сменён на `gemini-3.5-flash-lite`.

Из этого сделано два вывода, и они важнее самой строки:

1. **Поле «model id» в промпт-баре.** Ответ «отредактируй `gemini.ts`» на 404 от вендора
   плохой, а `keyedProvider` уже умел принимать `model`. Пустое поле — «дефолт
   провайдера», не «модель с пустым именем».
2. **`GEMINI_DEFAULT_MODEL` и `ANTHROPIC_DEFAULT_MODEL` экспортируются из
   `@txt2sfx/agent`.** Плейграунд показывает их в placeholder, то есть обещает то, что
   действительно будет вызвано; вторая копия строки в UI однажды соврала бы. Тест
   `agent.test.ts` сверяет placeholder с тем, что вернёт провайдер.

Заодно снят пин на конкретный id в `packages/agent/test/providers.test.ts`: тест теперь
проверяет **форму** пути (`/v1beta/models/<id>:generateContent`), потому что id вендор
меняет по своему расписанию, а тест на форму запроса не должен падать из-за этого.
Оба провайдера так и остаются на `fetch` без SDK (§14.2) — включая то, что один и тот же
код провайдера исполняется в Node и на странице.

### 15.5. Тесты и как проверялся сценарий

31 новый тест, все на чистой логике, без аудиостека и без сети: клиент банка на
инжектированном `fetch` (недоступный сервер, мусор в ответе, отказ с `issues`,
идемпотентный повтор), провайдеры (ключ Gemini только в заголовке, браузерный заголовок
Anthropic, пустой ключ, дефолт и override модели), mock (ранжирование, следующий
кандидат, исчерпание), имена рецептов, строки прогресса, состав галереи.
`renderSignalFor` и `targetFromBuffer` тестами не покрыты сознательно: это обвязка
`AudioBuffer`, и подделка `AudioBuffer` проверяла бы подделку.

Тесты плейграунда типизируются его собственным конфигом (`include: ["src", "test"]`):
корневой `tsconfig.test.json` исключает `apps/web` намеренно — там свои JSX и DOM.

**Критерий приёмки фазы («сценарий вручную end-to-end с mock-провайдером») выполнен**,
проверено в браузере через Chrome DevTools на живом `vite dev`:

- mock, промпт «a sharp laser gun shot for a space shooter» → лог
  `asking → reply 339 chars → rendered peak 0.827 → parses and validates — clean →
  accepted`, рецепт в редакторе, слот-слайдер, экспорт 553 Б, запись в группе `session`;
- с поднятым банком тот же прогон дал `2 example(s)` — то есть few-shot реально пришёл
  из `/api/retrieve`, и `selectFewShot` отбросил невалидный из трёх (§14.3);
- `Publish to bank` → `published as #11`, `GET /api/health` стал отдавать 11 рецептов,
  рецепт появился в галерее банка;
- `helicopter`, открытый из банка, показал `error duration.contract` в панели диагностики.

### 15.6. Мост в devtools и три дефекта, которые он нашёл (2026-08-05)

Заказчик получил на Gemini плохой результат («звук барабана между бочкой и snare» на
промпт про камень в озере) и предложил: пусть на этапе доработки в роли LLM выступает
внешний агент через devtools. Сделано — `apps/web/src/lib/bridge.ts` плюс
`window.txt2sfx` (только под `vite dev`, в статическую сборку не попадает, есть тест).
Провайдер `bridge` парковает запрос, отвечающий пишет рецепт руками, а всё ниже —
извлечение, валидатор, рендер, оптимизатор, фидбек — работает без изменений. Плюс
`measure()`: профиль кандидата и цели, замечания валидатора, расстояние и **те самые
директивы `humanReadableDiff`**, которые цикл отправил бы модели.

Мотив не в удобстве: подстройка цикла против хостед-модели — медленный и шумный
эксперимент, где каждое изменение стоит round trip и новой выборки, так что два прогона
никогда не расходятся по одной причине. Мост убирает и сеть, и сэмплирование: проверяется
**наша** половина.

Первый же прогон нашёл три дефекта, из которых модель виновата в одном:

1. **Окно простоя больше бюджета поколений — ветка «переделай топологию» недостижима.**
   `stallGenerations` был константой 20 (треть от дефолтных 60), а плейграунд я поставил
   на 14 поколений, чтобы браузер не подвисал. `stopped` мог быть только `target` или
   `budget`, а `budget` цикл сознательно не считает поводом просить новую структуру (§14.4).
   Итог: модель ни разу не увидела директивы диффа. Исправлено — `stallWindowFor(generations)`
   в `de.ts` (треть бюджета, не меньше 6; для дефолтных 60 это те же измеренные 20),
   регрессионный тест на инвариант «окно всегда меньше бюджета».
2. **Бюджет итераций съедается ремонтом.** `maxIterations: 3`, из них две ушли на parse
   error и нарушение инварианта, третья — первый и последний замер. Поднято до 5;
   ремонтные итерации ничего не рендерят и стоят дёшево.
3. **`measure()` возвращал прошлый рендер.** API собирался из значений замыкания, и
   вызов сразу после прогона отдавал числа предыдущего рецепта — я на них едва не сделал
   вывод. Теперь всё изменяемое читается через ref, объект на `window` ставится один раз.

Добавлено по ходу: событие `generation` в `AgentEvent` (фит — самое медленное место цикла,
44 поколения без единой строки прогресса читаются как зависание) и **кнопка «Fit to
reference»** в панели Slots — 60 поколений по текущему буферу. Она отвечает на «поиск
всё ещё улучшался, когда кончился бюджет»: по логике самого цикла ответ в этом случае —
больше поколений, а не новый вызов модели. Каталог `.refs/` добавлен в `.gitignore`:
референсные записи — чужое аудио под чужой лицензией, в репозиторий они не попадают,
а `vite dev` их отдаёт по `/@fs/`.

**Что показал прогон по воде** (цель — `404829__nilbul__water-splash.wav`, профиль:
`noiseRatio 0.939`, `flatness 0.575`, центроид 8 кГц → 6.6 кГц, 796 мс):

| попытка | расстояние | что изменилось |
| --- | --- | --- |
| Gemini | 0.556 → **0.509** (budget) | `sub 180→45Hz` + `modal membrane Q15` = буквально бочка и корпус барабана |
| мост, 1 | 0.474 → **0.457** (budget) | ни одного осциллятора, только шум в полосах — по замеру, а не по слову «камень» |
| мост, 2 | 0.473 → **0.428** (budget) | по диффу: огибающая цели двугорбая, поэтому медленная атака + `delay` на поздних слоях |
| мост, 3 | 0.287 → **0.278** (budget) | полоса на 600 Гц вместо lp (доминанта уехала на 82 Гц), шире полосы, живой хвост |
| + Fit | 0.278 → **0.278 (stalled)** | числа исчерпаны: остаток ошибки — структурный |

Ключевое наблюдение — не числа, а то, **чем** они получены: цель измеряется в 0.94 по
noiseRatio, то есть это широкополосный шум, и любой тональный слой в рецепте — ошибка,
которую оптимизатор исправить не может. Gemini написал два тональных слоя, потому что
слово «камень» тянет к удару, а измерение говорит обратное. Директивы диффа это и
сообщают; их просто никто не показал модели (дефект 1).

Второе наблюдение — про грамматику. Огибающая цели `[1, 0.04, 0.09, 0.18, 0.13, 0.06,
0.04, 0.05]` — это удар, пауза, **второй подъём** и длинное плато. Двугорбость выражается
уже сейчас (медленная атака + `delay`), плато — через `hold`, и это недоиспользованный
инструмент. А вот чего в наборе нет физически: **плотного поля мелких событий**. У воды,
гравия, дождя и огня спектрограмма зернистая, а `noise` стационарен, а `chirp` — один
свип. Отсюда заявка на примитив `grains` (N коротких событий в окне: плотность, разброс
частоты и времени, seed) — он процедурный, в экспорте это один буфер, и открывает сразу
целый класс. Пока его нет, `flatness` упирается в ~0.42 против нужных 0.575.

Третье, дешёвое и не сделанное: **слоты, упёршиеся в границы своего диапазона, никому не
сообщаются.** У Gemini таких было 6 из 8, у моего третьего прохода — 3 из 4
(`bp ~3000Hz[400..3000]`, `lp ~1400Hz[100..1400]`). Это DE говорит «оптимум снаружи того,
что ты мне разрешил» — сигнал, который стоит добавить и в `OptimizeResult`, и в
repair-сообщение.

### 15.7. Примитив `grains`, нормализация промпта, ключ в secure storage (2026-08-05)

Три задачи по итогам §15.6. **616/616 тестов**, все typecheck-и чистые, `vite build`
341 КБ / 113 КБ gzip.

**1. `grains` — девятый примитив.** `packages/core/src/primitives/grains.ts`, генератор в
`compile/buffers.ts` (две половины, TS и строка для экспорта), сигнатура в
`grammar/signatures.ts`, 8 тестов в `test/grains.test.ts`.

Одно зерно — затухающая синусоида с подъёмом частоты на 6 % за свою длину: модель
Миннарта, та же физика, что в `bubble-pop` (§9.2). Параметры: `freq`, `rate`
(зёрен в секунду), `width`, `spread` (разброс в октавах), `jitter` (0 — метроном,
1 — естественный разброс). Решения, которые стоит знать:

- **Плотность — не громкость.** Поле нормируется к пику 1, поэтому `rate 220` плотнее
  `rate 12` (RMS 0.0442 против 0.0218) и не громче (пик 0.477 против 0.545). Иначе
  модель, потянувшись к плотности, каждый раз перекручивала бы `gain` — та самая
  путаница, из-за которой водные попытки звучали неверно. Аналитической границы у суммы
  перекрывающихся зёрен нет, поэтому, в отличие от `modal`, нормировка идёт вторым
  проходом (~40 байт в экспорте).
- **Порядок операций в двух половинах совпадает буквально.** `f * (i/R)` и `f * i / R`
  различаются в последнем бите, а тест паритета требует побитового равенства живого
  графа и экспорта. Комментарий в коде стоит там же, где ловушка.
- **Цена: ~960 Б на однослойный рецепт.** Та же сделка, что в §5.4: процедурные
  генераторы — фиксированная стоимость отказа от ассетов.

**Проверка на воде дала честный, но неудобный результат.** Четвёртая попытка (два
слоя из четырёх — грани) дала 0.424 → **0.322**, то есть *хуже* ручной шумовой версии
(0.278), а `flatness` осталась 0.401 против 0.435. Вывод, который из этого следует, —
не «примитив не нужен», а **метрика не видит того, что он добавляет**: «поле мелких
событий» — свойство *временнóе* (плотность транзиентов), а в `SoundProfile` нет ни
одного поля, которое его измеряет; STFT-лосс задевает это лишь косвенно. То есть
оптимизатор не может вознаградить гранулярность, даже когда она перцептивно верна.
Отсюда следующая заявка, уже обоснованная данными: **поле плотности транзиентов в
профиле** (например, crest factor по коротким окнам или число онсетов в секунду).
До тех пор судить о `grains` можно только на слух — что плейграунд и позволяет.

**Открытое расхождение, не разобранное:** после того прогона `Fit` сообщил
`0.278 → 0.278 (stalled)`, а панель на том же буфере показывала 0.322. Два пути,
которые должны считать одно и то же (`optimize` и `measure`), дали разные числа.
Это подрывает любое сравнение, поэтому это первое, что надо проверить; гипотезы —
разный сид рендера, разная длина буфера или устаревшее замыкание в одном из путей.

**2. Нормализация промпта.** `packages/agent/src/query.ts`: `searchQuery` — один
дешёвый вызов (`maxTokens: 64`), запрос → 3–8 английских ключевых слов; `parseKeywords`
отдельно и строго — прозу отвергает, а не подрезает, потому что у вызывающего есть
хороший фолбэк. В цикле — `rewriteQuery?: boolean` (по умолчанию выключено; включено в
плейграунде для всех провайдеров кроме mock) и `retrievalQuery?: string`, который его
перебивает. Новое событие `retrieval` показывает, чем искали и что нашлось, включая
флаг `fallback`: три примера, не совпавших ни с чем, выглядят точно как три совпавших,
и это самый дорогой тихий отказ в цикле. Рерайт не делается, когда банка нет — вызов
без потребителя. 15 тестов.

**3. Ключ в secure storage.** `apps/web/src/lib/keystore.ts`: AES-GCM, ключ шифрования —
**неизвлекаемый** `CryptoKey` (`extractable: false`) в IndexedDB, шифротекст рядом.
`localStorage` не используется: он хранит открытый текст, и правило §4 остаётся в силе.
Опция per-provider (чекбокс `remember`), сохранение — в момент реального использования
ключа, а не на каждое нажатие клавиши, и кнопка `forget` рядом.

Честная формулировка, вынесенная в TSDoc: **это не сейф**. Страница, которая ключом
пользуется, обязана уметь его прочитать, поэтому скрипт, попавший на этот origin,
вызовет `load()` так же, как приложение. Что даёт эта схема: на диске нет читаемого
текста (в DevTools → Application видны IV и шифротекст), ключ шифрования нельзя
выгрузить даже нашим собственным кодом, и копия базы на другой машине бесполезна.
Отсюда и opt-in, и `forget`. 8 тестов на настоящем WebCrypto против in-memory KV;
последний проверяет саму гарантию — в хранилище нет секрета, а `exportKey` на
wrapping-ключе падает.

### 15.8. `bend`, затенённая переменная и расхождение метрики со слухом

Заказчик послушал поле зёрен: «звук стал богаче и интереснее», но «больше похоже на
разлёт мелких осколков льда», а в референсе — всплеск воды. Отчёт точный и указал на
физику: лёд и стекло — это короткий яркий пинг, **частота которого не двигается**, а
вода — пинг с восходящим глиссандо (каверна схлопывается, резонанс растёт). Подъём был
зашит константой 6 % — почти не слышно.

**Найден настоящий баг, попутно.** Фактор подъёма назывался `k`, а цикл по зёрнам
считает индексом `k` — переменная была затенена, поэтому `bend` не действовал вообще, а
чирп зависел от **номера зерна**. Ошибка была одинаковой в обеих половинах генератора,
поэтому тест паритета остался зелёным: паритет доказывает, что два проигрывателя
согласны, а не что хоть один из них прав. Это записано комментарием там же, в
`buffers.ts`.

**`bend` стал параметром** (`0.25..4`, дефолт 1.3): `1` — лёд/стекло, `1.2–1.6` — вода,
меньше 1 — «блуп» поднимающегося пузыря. Означает **конечную** частоту зерна; внутри
делится на 2, потому что фаза `2πf·t·(1 + k·t/T)` даёт мгновенную частоту
`f·(1 + 2k·t/T)`, и параметр, который гнёт вдвое сильнее обещанного, хуже, чем никакого.
Тест проверяет и направление, и то, что `bend 1` действительно оставляет высоту в покое.

**Главная находка — метрика и слух расходятся, и расхождение измеримо.** Пятый проход
(зёрна ниже, длиннее, с подъёмом 1.45–1.6) дал 0.453 → 0.324, но оптимизатор при этом
утащил **6 из 8** слотов зёрен на верхнюю границу: freq 3948 из 4000, rate 294 из 300,
spread 3.44 из 3.5, bend 2.3 из 2.4. То есть поиск тянет звук ровно туда, откуда его
увёл слух — к ярким плотным пингам. Причина понятна: единственный способ поднять
`flatness` до референсных 0.575 нашими примитивами — сделать пинги ярче и плотнее, тогда
как настоящий референс берёт flatness широкополосной турбулентностью. Поля, измеряющего
«плотность мелких событий», в профиле нет, поэтому объяснить поиску разницу нечем.

Отсюда практическое правило, которое стоит перенести в документацию грамматики:
**`~слот` — это делегирование метрике.** То, что выбрано на слух, надо оставлять
литералом, иначе оптимизатор это перекрутит; в рецепте `stone splash` характер зёрен
записан числами, а слоты оставлены только там, где метрике можно доверять (частоты
фильтров и уровни).

**Второй отчёт слухом: «морская волна или песок из железного кузова».** Это
непрерывность, и виноваты были три вещи, все — про время, а не про спектр: слой `wash`
(полоса шума с `hold 220ms`) — буквально прибой; `rate 120` с 420-мс спадом — поток
капель, а не всплеск; мягкая атака вместо жёсткой кромки (у референса спад −8 дБ за
первые 5 мс, у кандидата было −1). Дальше — короткая серия проходов по слуху и по
огибающей, и каждый нашёл ровно одну ошибку:

| проход | что изменено | что сказал замер |
| --- | --- | --- |
| 6 | убран `wash`, `rate` 120→70, спады вдвое, `click` на кромку | пересол: `rate 14` дал шесть отдельных пингов, flatness 0.028 — почти чистые тоны, в огибающей дыры |
| 7 | аэрация вернулась как широкополосный слой на низком gain с длинным спадом и **без** `hold` | форма пошла в цель, но всплеск громче удара |
| 8 | всплеск и капли позже (пауза после удара), хвост длиннее, общий уровень +6 дБ | огибающая `[1, 0.96, ...]` — всплеск почти равен удару, а у референса там 0.04 |
| 9 | всплеск и капли тише (0.26/0.22) и позже (150/260 мс), аэрация тоньше | `[1, 0.19, 0.48, 0.17, 0.16, …]` против `[1, 0.04, 0.09, 0.18, 0.13, …]`: удар → пауза → всплеск → оседание. 802 мс против 796, noiseRatio 0.978 против 0.939, distance 0.333 |

Методический вывод из этой серии важнее самого рецепта: **каждый отчёт слухом находил
временнýю ошибку, которую метрика не подсвечивала**, а из шести директив диффа полезной
раз за разом оказывалась только одна — «energy sits too early». Метрика описывает спектр
подробно и время бедно, и именно поэтому она проигрывает уху на этом классе звуков.

И приоритет для следующего шага теперь не спорный: **поле плотности транзиентов в
`SoundProfile`** (crest factor по коротким окнам либо число онсетов в секунду) —
без него `grains` работает вслепую, а fitness активно вредит. Второе — расхождение
0.278/0.322 из §15.7.

### 15.9. Что осталось за пределами v0

Ничего из плана. Открытые пункты — те же три, что и после фазы 7, и все они за границей
v0: судьба `helicopter` (§11, ничего не блокирует), `decodeAudioData` в тестах анализатора
и перцептивный судья — v0.2–v0.3 в `ROADMAP.md`. Известные шероховатости, зафиксированные
как решения, а не как долг: статическая сборка не умеет писать в `examples/` (это dev-only
эндпоинт), а состояние банка не опрашивается периодически — проверка идёт при смене
источника, URL и по кнопке `refresh`, потому что регулярно дёргать адрес, который набрал
пользователь, — это шум.

## 16. `plate`: примитив, добавленный по измерениям, а не по плану

Первое расширение набора примитивов после v0, и повод для него — не идея, а тупик,
который стало видно в цифрах. Референс — рендер Stable Audio на промпт
`sword-clash` (*«Steel on steel: a noise strike transient plus two metallic modal
rings»*, seed 2923379959, WAV, чтобы кодек не искажал цель фита). Стенд — headless-фит
той же дифференциальной эволюцией, что в плейграунде (140×20, `anchor 0`).

### 16.1. Что упиралось

Лучшее, что даёт старый набор (`modal` + `pluck` + шум, 5 слоёв, 22 слота): **0.252**,
и поиск встаёт. Остаток всегда одни и те же две цифры: 741 мс против 1002 и −29.0 LUFS
против −17.2. Ни то, ни другое оптимизатор достать не может, потому что причина
структурная:

- у `modal` длина звона наследуется от резонаторов, `tau = Q / (pi * f)`. При потолке
  `Q 400` на 3.6 кГц это `tau` 35 мс, то есть −60 дБ за 250 мс;
- огибающая с этим **перемножается**, а не продлевает: две экспоненты `a` и `b` дают
  −60 дБ на `6.9 * ab / (a + b)`, что всегда короче каждой. В категории `impact`, где
  спад огибающей ограничен 1200 мс, секунды яркого звона не собрать вообще.

### 16.2. Что сделано

`plate <freq> [modes] [stretch] [slope] [decay] [tilt]` — банк растянутых модов, где
**длина написана временем** (t60 нижнего мода, не зависит от частоты), а `tilt`
возвращает физику: верх умирает раньше. Буфер, оба бэкенда, паритет побитовый, экспорт
одного слоя — 760 Б.

| рецепт | слои | экспорт | distance | длит. | атака | доминанта | громкость |
| --- | --- | --- | --- | --- | --- | --- | --- |
| референс | — | — | — | 1002 мс | 14 мс | 3605 Гц | −17.2 LUFS |
| `modal`+`pluck`+шум | 5 | 1894 Б | 0.252 | 741 мс | 30 мс | 3287 Гц | −29.0 LUFS |
| `plate`×2 + шум | 3 | 1505 Б | **0.179** | 949 мс | 14 мс | 3644 Гц | −18.1 LUFS |

11 дБ громкости и 200 мс длительности пришли из одного изменения, и не из того, из
которого ожидалось: звон, чья длина не привязана к высоте, можно **держать** через
`hold`, а не срезать экспонентой. Отсюда идиома, вынесенная в док параметра `decay`:
длину писать в примитив, огибающую держать плоской над ней.

### 16.3. Три гипотезы, из них две неверные — и это главный результат

- **Неверно: плотность снижает crest factor.** Ожидалось `sqrt(2*modes)` ≈ 14 дБ на
  12 модах (у референса 15 дБ). Измерено: 3 мода — 21.7 дБ, 12 — 23.4. Причина — закон
  амплитуд `1/k`: нижний мод владеет пиком, добавленные моды тише и умирают быстрее.
  **Разрыв по crest к диффузии остаётся открытым**, и следующая попытка не должна
  повторять эту.
- **Неверно: положение спектра можно задать `bp` на слое.** Фит упорно ставил
  доминантный партиал на 1800 Гц (ровно половина цели) — скат 12 дБ/окт не перебивает
  6 дБ преимущества фундаментала. Пришлось добавить `slope` (закон `k**-slope`): на
  банке 600 Гц из 16 модов средний центроид идёт от 832 Гц при `slope 2` до 2942 Гц при
  `slope 0`. Это и есть управление спектром внутри банка.
- **Верно и дорого: моды выше Найквиста нельзя рендерить.** Первая версия фитилась к
  центроиду 4.6 кГц против цели 3.1 целиком на свёрнутой энергии (`plate 3600Hz
  stretch 1.4` кладёт мод №4 на 113 кГц). Теперь такие моды выбрасываются и не входят в
  нормировку, поэтому `freq`, `stretch` и частота дискретизации вместе решают, сколько
  из `modes` слышно.

### 16.4. Что осталось

- **Слепая зона метрики подтвердилась численно.** Свободный фит доходит до **0.163**,
  но с доминантным партиалом на 1800 Гц вместо 3605 — то есть метрика согласна
  обменять «тот же объект» на лучшие остальные члены. Приходится ограничивать диапазон
  слота руками. Это аргумент за перцептивного судью из `ROADMAP.md` и за пересмотр весов
  (`peakHz` 0.2 против `centroid` 0.2 + `envelope` 0.2 — их сумма перевешивает).
- **Остаточная разница теперь в шуме, а не во времени.** flatness 0.0007 против 0.0616 и
  noiseRatio 0.0024 против 0.0488: рендер слишком чистый. Тихий шумовой слой
  перелетает цель (0.23), значит нужно что-то между — расстройка модов между собой
  (биения) или шумовое возбуждение банка. Это следующий шаг, если он понадобится.
- **Порядок директив в `humanReadableDiff` вводит в заблуждение.** Он сортирует по
  драматичности в децибелах: «14 дБ тише» стоит первым при весе loudness 0.05, а
  `centroid`/`envelope`/`peakHz` по 0.2 — ниже. Стоит сортировать по `вес × ошибка`.
- **mp3 как цель фита искажает профиль.** Тот же рендер в mp3 320k против WAV: атака
  28 мс против 14, flatness 0.0224 против 0.0616. Для прослушивания и A/B mp3 годится,
  для фита цель должна быть WAV — стример в плейграунде отдаёт `--format` по умолчанию
  из `run.py`, так что менять надо там, если это станет мешать.

## 17. `txt2sfx-bridge claude` — CLI Claude Code как модель плейграунда (2026-08-08)

Направление B (плейграунд → агент) до сих пор требовало агента на том конце: либо
MCP-сессию, которая не забывает звать `sfx_next_request`, либо клиента с `sampling`,
которого сегодня почти ни у кого нет. Новый режим убирает агента из этого направления
совсем: `npx txt2sfx-bridge claude` сам собирает припаркованные задания и отвечает на
каждое запуском бинарника `claude`, уже стоящего на машине. Кнопка «Генерировать» с
провайдером `agent` начинает работать без MCP-клиента, без конфига и без перезапуска —
ключа по-прежнему нет нигде, отвечает та подписка, под которой CLI залогинен.

### 17.1. Решения и что отвергнуто

| решение | почему так, а не иначе |
| --- | --- |
| **подпроцесс, а не `@anthropic-ai/claude-agent-sdk`** | у `packages/bridge` ноль runtime-зависимостей, и именно это делает `npx txt2sfx-bridge` работоспособным (один файл бандла). SDK — второе дерево зависимостей ради `spawn`. Тот же аргумент, что в §14.2 про провайдеров на `fetch`. |
| **системный промпт файлом (`--system-prompt-file`), диалог в stdin** | контракт ≈ 14 КБ по факту замера, а Windows режет всю командную строку на 32767 символах. Промпт в argv — это баг, который проявляется только на чужой машине. В argv не попадает ничего, что читает модель. |
| **`--tools ""`** | это не сессия агента, а одно завершение: просят один блок с рецептом, а на запрос разрешения в конце пайпа отвечать некому. |
| **`is_error` проверяется отдельно от кода возврата** | отказ, исчерпанный бюджет и проблема с авторизацией приходят как *успешный* запуск с объяснением в `result`. По коду возврата это выглядело бы как ответ модели, и плейграунд стал бы парсить сообщение об ошибке как soundline. |
| **`PATH` × `PATHEXT` разбирается вручную (win32)** | `CreateProcess` сам дописывает `.exe`, но не умеет запускать `claude.cmd` из npm-установки. `shell: true` решил бы это одним флагом и отдал бы аргументы шеллу без кавычек — а во временном пути с `--system-prompt-file` пробел появляется у любого пользователя с пробелом в имени. Поэтому шим ищется сам и запускается через `cmd /d /s /c` с квотингом от Node. |
| **воркер последовательный** | хаб по построению держит одно задание, а `nextRequest` идемпотентен и может выдать его дважды — защита от второго запуска `claude` на то же задание обязана жить на этой стороне. |
| **ошибка завершения = `fail` задания, а не молчание** | плейграунд висит на `agent.complete`; молчание выглядит как зависший мост, а `fail` показывает человеку причину. |

### 17.2. Что проверено и чем

Тесты (`packages/bridge/test/claude.test.ts`, 16 штук) идут через подставной
`CommandRunner`: проверяется форма обмена — что в argv, что в stdin, что делает отказ с
припаркованным заданием, — и ничему из этого не нужны модель, сеть и чужая подписка.
Существование самих флагов тест проверить не может: они сверены с `claude --help`
руками, поэтому собраны в одной функции `claudeArgs`, и слишком старый CLI упадёт на
первой генерации со своим же сообщением про неизвестный флаг.

Живьём проверено дважды: реальный `claudeCompleter` на настоящем системном промпте
(14 414 Б) вернул рецепт за 13.6 с, и режим целиком подцепился к уже запущенному
демону, увидел припаркованное задание и отправил его в CLI.

**Наблюдение, которое стоит записать:** в живой пробе модель написала
`bp ~1.6kHz[900..3000]` — старт слота в килогерцах, границы в герцах. Валидатор ловит
это ровно так, как и задумано («slot start 1.6 is outside its range»), и дальше работает
обычный виток ремонта. То есть первый ответ CLI ничем не привилегирован — он проходит
тот же путь, что ответ любого хостед-провайдера.

### 17.3. Что осталось

- **Версия CLI не проверяется по возможностям, только по факту запуска.** `--version`
  спрашивается на старте (чтобы отсутствие бинарника не выяснялось после нажатия
  «Генерировать»), но набор флагов не сверяется. Если Claude Code переименует
  `--system-prompt-file`, это выяснится на первой генерации.
- **Один процесс `claude` на одно задание.** Кэш промпта между витками ремонта не
  переиспользуется: `--resume` дал бы это, но привязал бы мост к сессиям CLI, а
  плейграунд и так шлёт весь диалог целиком.
- **В плейграунде режим виден только во вкладке Claude Code диалога моста.** Провайдер
  `agent` про него не знает и по-прежнему говорит про поллинг, если ответить некому.

## 18. Референсная модель переехала в мост (2026-08-08)

Stable Audio Open Small — это *цель*, к которой подгоняется рецепт, и самая полезная
вещь на машине: найти запись «ржавая калитка открывается, потом тяжёлая щеколда» руками
— это медленный шаг каждой сессии. До этой правки она была доступна только из
`vite dev` через плагин, который запускал `test/stable-audio/run.py` из клона
репозитория. То есть единственная функция, которой нужна локальная машина, была
единственной функцией, которой не могло быть у хостед-плейграунда.

Теперь ей владеет мост. Аргумент за мост и аргумент за эту функцию — один и тот же: код,
который обязан выполняться на машине человека, вызываемый со страницы, которую мы не
хостим. Мост уже локальный демон, уже ставится одним `npx`, и в нём уже живут рендерер,
оптимизатор и инструменты агента.

**Запрет из §2 не нарушен.** «Никаких облачных text-to-audio моделей в ядре» — это
по-прежнему так: в `core` ничего не добавилось, модель никуда не ходит через нас, веса
лежат на диске пользователя, и результат её работы — референс для сравнения, а не
поставляемый артефакт.

### 18.1. Решения

| решение | почему так, а не иначе |
| --- | --- |
| **`run.py` кладётся в npm-тарбол** (`scripts/pack-assets.mjs` → `dist/stable-audio/`) | у пользователя нет этого репозитория — это и есть весь смысл `npx`. Альтернатива «вшить питон строкой в бандл» — вторая копия файла, который лежит в гите, и она разъедется первой же правкой. Копия, а не бандл: esbuild нечего делать с `.py`. |
| **рабочая папка ≠ папка со скриптом** | `run.py` строит venv рядом с собой, а «рядом с собой» в кэше npx — это каталог, который может исчезнуть между двумя запусками. Поэтому скрипты копируются в `~/.txt2sfx/stable-audio/`. Из клона рабочая папка — сам `test/stable-audio`, чтобы venv из терминала и venv из вкладки были одним venv, а не двумя копиями по пять гигабайт. `TXT2SFX_STABLE_AUDIO_DIR` перекрывает оба варианта. |
| **вкладка ставит модель — то, что раньше было отвергнуто** | отвергался *спиннер*, а не загрузка: «1.7 ГБ и лицензионный гейт за крутилкой в браузерной вкладке — неправильная форма». Сейчас на экране видно, что будет скачано, куда именно, ссылка на лицензию, поле токена и каждая строка, которую печатает установщик. Прятать нечего — значит, и повода отказывать нет. |
| **`run.py --provision` вместо провижининга на TypeScript** | что значит «установить» — знает `run.py` (uv, CPython 3.10, выбор CPU/CUDA-колёс, отпечаток `requirements.txt`, гейт весов). Повторить это в Node — второе определение той же вещи. Флаг ставит venv и скачивает веса, ничего не рендерит. |
| **`model.*` по сокету, а не по HTTP-роуту** | сокет уже открыт (это по нему ходит `agent.complete`), уже проходит проверку токена и Origin, и по нему уже умеют летать события — а установка на минуты без потока строк выглядит как зависание. `model.log` уходит **только тому табу, который спросил**: чужая установка, скроллящаяся в соседнем окне, — это шум. |
| **дедлайна в хабе нет** | дедлайн у ребёнка: десять минут на рендер, два часа на установку. Второй, более короткий, в хабе превратил бы медленную закачку в загадочный обрыв. |
| **токен — в окружении ребёнка, не в argv** | argv возвращается в событии `start`, виден в списке процессов любому другому пользователю машины и попадает в лог того, кто его пишет. Живёт токен один запрос и нигде не сохраняется — то же правило, что `lib/keystore.ts` описывает для вставленного API-ключа. |
| **размеры считаются обходом каталогов, а не берутся из таблицы** | «примерно 1.7 ГБ» рядом с наполовину скачанным файлом хуже, чем ничего. Симлинки не разыменовываются: HF-кэш линкует блобы в снапшоты, и обход по ссылкам показал бы 3.4 ГБ на чекпоинт в 1.7. |
| **плагин Vite остался, но стал шимом** | иначе `pnpm dev` потребовал бы второго терминала ради правки одной панели. Свой `spawn` в нём был бы вторым определением argv и лицензионного гейта — теперь оба конца зовут один модуль. |
| **панель — три экрана, а не один с предупреждением** | у большинства посетителей честное состояние вкладки — «у вас этого нет». Форма рендера с выключенной кнопкой сообщает, чего человек не может, не сообщая, что нажать. |

### 18.2. Что проверено и чем

`packages/bridge/test/stable-audio.test.ts` (25) — граница с `run.py`: argv из
недоверенного тела, разрезание строк по `\r` (прогресс-бар качалки — это он), обе
машиночитаемые строки (`txt2sfx-result`, `txt2sfx-provisioned`), раскладка HF-кэша,
выбор рабочей папки и запись установщика в папку, где его нет. `hub.test.ts` (+5) —
проводка `model.*`: base64 в ответе, события только спросившему табу, слова ошибки
как есть, `unsupported` для моста без установщика.

Живьём проверена та самая ветка, ради которой всё делалось: бандл esbuild плюс
`dist/stable-audio/` скопированы в пустой каталог вне репозитория, и `doctor` оттуда
сообщил `model needs-venv`, рабочую папку `C:\Users\…\.txt2sfx\stable-audio` и
правильный каталог кэша весов. Сама установка (несколько гигабайт) в тестах не гоняется.

### 18.3. Что осталось

- **Рендеры из `out/` по мосту не читаются.** Список файлов, которые оставил терминальный
  запуск, в статусе есть, а метода «дай мне вот этот» — нет; в дев-режиме он тянется
  роутом `/out/<name>`. Стоит `model.read`, если список кому-то понадобится.
- **`--cpu-torch` наружу не выведен.** В `provisionArgs` он есть, панель его не
  показывает — на GPU-машине это разница в ~3 ГБ загрузки, и однажды кто-то захочет
  выбрать.
- **Проба интерпретатора кэшируется на 30 с.** Компромисс: четыре запуска процессов на
  каждый `status`, и при этом «поставил uv и нажал снова» обязано сработать.

### 18.4. Найденное при релизе 0.2.0: `npx` из тарбола ничего не делал

Смоук-тест воркфлоу («поставить тарбол в каталог, который никогда не видел этот
репозиторий, и запустить `doctor`») поймал то, ради чего он и написан, и находка
касается уже опубликованной 0.1.0.

Часовой в конце `cli.ts` сравнивал `import.meta.url` с `process.argv[1]`. На POSIX npm
кладёт бинарник в `node_modules/.bin` **симлинком**, а ESM-загрузчик Node разыменовывает
симлинки, прежде чем выдать модулю его собственный URL. Пути не совпадали никогда,
`main()` не вызывался, и `npx txt2sfx-bridge` завершался с кодом 0, не напечатав ничего.

Почему это не всплыло раньше: на Windows npm пишет `.cmd`-шим, который передаёт
настоящий путь, поэтому на машине разработчика всё работало. Ровно та разница платформ,
которую локальная проверка скрывает по построению.

Форма отказа — худшая из возможных: не исключение, не ненулевой код, а быстрая тихая
успешная команда. Искать в такой ситуации нечего — нет ни строчки, по которой можно
загуглить. Поэтому исправление (`realpathSync` над `argv[1]`) закрыто тестом
`packages/bridge/test/cli.test.ts`, который ходит **через симлинк**: это форма
настоящей установки, и единственное место, где её не случается, — машина разработчика.

---

## 19. Результаты генераций переживают перезагрузку, и имя даёт модель (2026-08-08)

Две правки одного разговора, обе — про то, что вкладка теряла результат работы.

### 19.1. `session` в `localStorage` — сознательное отступление от §4

Сгенерированный рецепт — самая дорогая вещь на странице: вызов модели, потом популяция
рендеров на каждое поколение фита. До этой правки он жил ровно в одном месте — массиве
`session` внутри `App` — и перезагрузка (или падение вкладки, или неловкий эксперимент в
devtools) уносила всё, что не успели сохранить в `examples/` или опубликовать в банк.
Восстанавливать не из чего.

Правило §4 «никакого `localStorage`» сформулировано против двух вещей: секрет в открытом
виде на диске и кэш, который расходится с тем, что показывает редактор. Ни то, ни другое
здесь не происходит, поэтому правило переписано, а не нарушено: **настройки и
несохранённая работа — можно, секреты — никогда.**

| решение | почему так |
| --- | --- |
| **отдельный модуль `lib/session.ts`, а не поле в `lib/library.ts`** | `library` — это то, что браузер помнит *о каталоге* (корзина, «когда трогали», подсказка), и в его шапке прямо написано: ничему из этого не место ни на диске, ни в банке. Здесь наоборот — содержимое, и у него другая политика чтения, другой размер и другой аргумент. |
| **банк — не ответ** | публикация в банк — это осознанный акт «пусть будет у всех»; недоделанному кандидату там нечего делать, а `examples/` вообще git-каталог и доступен на запись только под `vite dev`. |
| **в записи только `name`, `source`, `prompt`** | категория и всё остальное выводятся из текста парсером — кэшировать их значило бы завести второй источник правды рядом с редактором, против правила «ничего не кэшируется, что может разойтись с тем, что на экране». Рендеры не хранятся: аудио пересчитывается за миллисекунды, текст — нет. |
| **буфер редактора сворачивается в `source` при записи** | у session-рецепта нет «сохранённой» версии нигде, поэтому то, что показывает редактор, **и есть** рецепт. Иначе после перезагрузки вернулся бы рецепт без правок ползунками — половина результата. |
| **дебаунс 500 мс** | запись — это `JSON.stringify` всего несохранённого рабочего набора, а протяжка ползунка даёт правку на кадр. Полсекунды потерянного ввода при перезагрузке несравнимы с потерей рецепта. |
| **кап 50, новые в конце** | дело не в квоте (50 саундлайнов ≈ 50 КБ против ~5 МБ на origin), а в рабочем наборе: за сороковым кандидатом сессии никто не вернётся, а неограниченный список рос бы всё время жизни профиля браузера, и ни один экран приложения не показывает, насколько он вырос. |
| **битая запись выбрасывается, а не «ремонтируется»** | рецепт с пустым телом открылся бы пустым редактором, который утверждает, что он — сгенерированный звук. Это хуже, чем отсутствие записи. Исключение — `prompt`: у рецепта, приехавшего по ссылке, его никогда не было. |

Ключ секретов это не касается: он по-прежнему в IndexedDB под неизвлекаемым `CryptoKey`
(§15.3), потому что `localStorage` хранит открытый текст. 6 тестов
(`apps/web/test/session.test.ts`), включая квоту, которая отказывает в записи.

### 19.2. Имя звуку даёт модель, а не слаг промпта

Заказчик попросил «использовать подключённую LLM, чтобы дать название и назначить
категорию». Оказалось, что модель это уже делает — и в первой же строке: заголовок
`sound "<name>" <duration> <category>` пишет она, категорию валидатор проверяет как
контракт, а `recipeName` брала *промпт* и слагала его, выбрасывая имя.

Цена этого была больше, чем «некрасиво». `slug` оставляет только ASCII, поэтому любой
запрос не на латинице сворачивался в `sound`, `sound-2`, `sound-3`: сессия русских
промптов давала список из нумерованных «ничего», в котором нельзя найти нужный звук.
Теперь имя берётся из заголовка (`stone splash` → `stone-splash`), промпт — фолбэк для
ответа, чей заголовок непригоден. Слаг всё равно применяется: `sound "ui click"` —
законное имя, а каталожное имя ещё и имя файла в `examples/`; ровно эту конвенцию
референсный набор уже соблюдает (`ui-click.soundline` содержит `sound "ui click"`).

Категорию отдельно «назначать» не понадобилось: она приходит тем же заголовком,
проверяется валидатором (и в ремонтном сообщении прямо сказано «меняй рецепт, а не
категорию»), выводится парсером в `items` и уже управляет чипсами галереи.

В контракт (`packages/agent/src/contract.ts`) добавлен один пункт: имя в заголовке — это
то, под чем звук будет лежать, поэтому два–четыре английских слова о событии и материале,
на каком бы языке ни был запрос. Раньше о имени не говорилось ничего — модель выбирала его
наугад, и это работало только потому, что никто его не читал.

Побочный эффект: у **mock**-провайдера ответ — это существующий рецепт, поэтому его имя
теперь честно коллидирует и приезжает с суффиксом (`coin-2`) вместо свежего имени на
рецепте, который уже лежит в каталоге.

## 20. Референсной модели пишут подпись, и рендер по умолчанию 2 с (2026-08-08)

Заказчик отрендерил «магическое заклинание леденая стрела» и получил звук, не имеющий к
запросу отношения, а в логе — `prompt "���������� ..."`. Два разных дефекта, из которых
виден был только второй, и он же оказался безобидным.

### 20.1. Промпт доезжал целым — его убивал токенизатор

Измерено, а не предположено. `sys.argv[1]` в дочернем Python содержит правильные
кодпоинты (`0x43c 0x430 0x433 …`): `CreateProcessW` несёт UTF-16, CPython читает argv
через `wmain`. Мусор в логе — это **эхо**: у Python на трубе `sys.stderr.encoding ==
cp1251`, а мост декодирует обе трубы как UTF-8. Лечится одной переменной окружения
(`PYTHONIOENCODING=utf-8`) и не имеет отношения к звуку.

Настоящая причина — в `model_config.json` самого чекпоинта:

```json
{ "id": "prompt", "type": "t5", "config": { "t5_model_name": "t5-base", "max_length": 64 } }
```

Замер кэшированным токенизатором:

```
"магическое заклинание леденая стрела"
  → 26 токенов: ▁ ма <unk> и <unk> е ско е ▁ <unk> а к ли на ни е ▁ ле де на <unk> ▁ ст ре ла </s>
  → декодируется обратно как "ма<unk>и<unk>еское <unk>аклинание ледена<unk> стрела"
```

То есть модель никогда не слышала про ледяную стрелу: ей дали фразу с дырами, она
потратила 30 с CPU и вернула собственную медиану. Никакая правка кодировок этого не
касается. Второй предел того же энкодера — 64 токена: на десяти реалистичных подписях
худшая измеренная ставка — 3.28 символа на токен, значит **≈200 символов** (около 34
слов) — всё, что модель читает, остальное молча отбрасывается.

### 20.2. Решения

| решение | почему так |
| --- | --- |
| **отдельный модуль `packages/agent/src/caption.ts`, по образцу `query.ts`** | прецедент уже был: `searchQuery` переписывает запрос в английские ключевые слова для FTS банка. Здесь та же форма — один дешёвый вызов, — но **противоположная политика отказа**: плохой поисковый запрос стоит худших примеров и потому падает молча, а плохая подпись стоит самого рендера, и «сырой промпт» тут не фолбэк, а как раз то, что было сломано. Поэтому `audioCaption` возвращает `source: 'model' \| 'prompt'` и `note`, а не подменяет тихо. |
| **подпись — редактируемое поле панели, а не скрытый шаг** | на диффузионной модели подпись **и есть** инструмент: это разница между одним рендером и другим. Спрятать её — та же ошибка, что спрятать загрузку за спиннером, и у панели не осталось бы ответа на «почему оно сделало вот это». |
| **автоматически по Render, но один раз на промпт** | заказчик выбрал «автоматически, но видимо». Подпись перепишется, если поле пусто или промпт сдвинулся; правка длины или сида вызова не тратит. Ручная правка помечается как `hand` и не перетирается. |
| **сорвавшийся вызов не помечает подпись как «написанную»** | иначе один таймаут означал бы, что до конца сессии в модель уходит непереведённый промпт. Поле показывает, что уйдёт, следующий Render пробует снова. |
| **`captionIssue` — репорт, а не запрет** | детерминированная проверка («есть буква не латиницей», «длиннее лимита») даёт стабильный `rule`-id, панель рисует его на языке читателя, кнопка остаётся живой. Та же позиция, что у `renderSound` про клиппинг и у `codegen` про бюджет размера. |
| **правило «не латиница», а не список письменностей** | список — это все письменности мира: греческая, ханьская, хангыль и иврит рассыпаются там же и так же. Вычитание множеств сделано лукахедом, без флага `v`. |
| **мост не переводит сам** | ему для этого нужна была бы своя языковая модель, а у вызывающего она уже есть. Мост пропускает строку насквозь и отражает её в `start`-событии. |
| **`mock` для подписи уходит к подключённому агенту** | mock отвечает готовым рецептом — это не подпись ни на каком языке. Если к мосту прицеплен кодовый агент, подпись пишет он: модель у него своя, ключ не нужен, и он на этой же машине по той же причине, что и референсная модель. Иначе `null`, и панель это говорит. |
| **`captionProviderFor(choice, attached)` — именно `attached`, а не «демон жив»** | запрос, припаркованный на мосту, за который никто не держится, ждёт свой таймаут. Подпись того не стоит. |
| **выбор провайдера вынесен в `providerFor` в `lib/agent.ts`** | вызывающих стало два — цикл генерации и подпись, — и второй появился ровно в тот момент, когда копия того же `switch` начала бы расходиться. |

### 20.3. Дефолт 2 с — в одном месте

Заказчик выбрал менять и CLI, и панель, чтобы не разошлись. `PRESETS['small']['seconds']
= 2.0` в `run.py`, `DEFAULTS` моста продолжает его зеркалить. Аргумент не только в
скорости: `seconds_total` — это **условие**, поэтому на 2 с модель компонует короткое
событие, а не одиннадцатисекундную петлю, у которой метрике потом надо объяснять, что
десять секунд лишние. Скорость — вторым: сэмплирование всегда идёт по полному окну, а
декод линеен по длине и на CPU он длиннее сэмплирования (замерено: 11.8 с против 18.8 с
для 11-секундного рендера).

Заодно исправлен диапазон поля: было `min 1 / max 12`, а мост отказывает вне `0.25…11` —
набранная 12 приезжала отказом с другого конца. Теперь `0.25…11` с шагом 0.25.

### 20.4. Что проверено и чем

- **argv доезжает целым** — спавн как у моста, `repr()` и кодпоинты из дочернего Python.
- **эхо чинится `PYTHONIOENCODING`** — тот же спавн, до и после: `cp1251` → `utf-8`.
- **токенизация** — `t5-base` из кэша HF, 10 английских подписей и 3 русских фразы;
  отсюда и `<unk>`-дыры, и константа `CAPTION_LIMIT = 200`.
- **18 тестов** `packages/agent/test/caption.test.ts` (разбор ответа, обрезка по запятой,
  все ветки `captionIssue`, фолбэки, «на пустой промпт вызова нет»), **4 теста** на выбор
  провайдера в `apps/web/test/agent.test.ts`. Всего 951 зелёный, `typecheck` чистый.
- **сквозной рендер не подтверждён**: `run.py` падает с `0xC0000005` внутри нативной
  загрузки весов. Воспроизводится **и без** новой переменной окружения, то есть к правке
  отношения не имеет — на машине было свободно 2.6 ГБ из 15.6 при commit 57 из 63 ГБ, а
  CPU-путь специально поднимает автоэнкодер в fp32 (+~700 МБ). Проверять после
  освобождения памяти.

### 20.5. Что осталось

- Локализованы только английский и русский. Остальные семь языков читаются по-английски —
  это политика `apps/web/test/i18n.test.ts`, а не забытая работа.
- `MAX_PROMPT` в мосте всё ещё 400 символов при читаемых 200. Это граница против
  случайной вставки, а не против длинной подписи; обрезать в мосте значило бы решать за
  панель, которая подпись и сочиняет.
- Подпись нигде не сохраняется: сменил рецепт — пиши заново. Это ровно тот же выбор, что
  у токена и у `repo` в той же панели.

## 21. Плейграунд переехал в корень домена, и мост стал ему доверять (2026-08-08)

Заказчик открыл `https://txt2sfx.github.io/txt2sfx/`, и мост ответил `403` на `/pair`.
Дефект оказался не в мосте, а в дефолте: страница, ради доступа к машине с которой мост и
существует, была единственной, которой он не доверял.

### 21.1. Сайт — отдельный репозиторий, а не переименование этого

`https://txt2sfx.github.io/` без подпапки отдаёт ровно один репозиторий — тот, что назван
по хосту. Вариантов было три.

| вариант | почему не он / почему он |
| --- | --- |
| переименовать `txt2sfx/txt2sfx` в `txt2sfx.github.io` | одна настройка, но монорепозиторий, который ещё и источник npm-пакета, начал бы отвечать на имя хоста. И главное: **trusted publisher на npm привязан к репозиторию** — до правки его настроек публикация бы ломалась, а тег к тому моменту был бы израсходован |
| **отдельный репозиторий под собранный сайт** ← выбрано | сборка остаётся здесь, где тесты, дерево уезжает туда, где origin. Идентичность репозитория и npm не тронуты |
| свой домен | лучший ответ на второй вопрос (см. 21.2), но стоит денег и в этот разговор не входил |

Цена варианта — один хранимый секрет, и он самый узкий из возможных: `GITHUB_TOKEN`
действует только в своём репозитории, а OIDC (`deploy-pages`) умеет деплоить только в тот
репозиторий, где выполняется workflow. Поэтому **deploy key**: запись ровно в один
репозиторий, без срока годности, радиус поражения — перезаписать публичный статический
сайт, то есть ровно то, что джоб и делает. Fine-grained PAT был бы шире (привязан к
человеку) и требовал бы ротации.

Организация запрещала deploy keys (`deploy_keys_enabled_for_repositories: false`) —
тумблер включил заказчик, у токена агента нет `admin:org`.

Решения внутри джоба:

| решение | почему так |
| --- | --- |
| **хост-ключи GitHub из `api.github.com/meta` по TLS** | `ssh-keyscan` доверяет всему, что ответит на 22-м порту, и молча принял бы ротацию |
| **проверка `dist/index.html` перед публикацией** | следующий шаг — `rsync --delete` поверх живого сайта, и молча собравшееся «ничего» опубликовало бы пустую входную дверь |
| **тема коммита читается из git, а не выражением** | текст автора, попав в выражение, склеился бы со скриптом до того, как bash увидел бы его как данные |
| **артефакт из `verify` не переиспользуется** | там матрица из двух версий Node, и выбирать, чей билд увидит мир, — решение без хорошего ответа |
| **`PAGES_BASE_PATH` оставлен** | org-сайт отдаётся из корня, префикса больше нет и `configure-pages` ушёл; но без переменной плейграунд под путём грузится пустым телом, и это стоит держать доступным форкам |

Побочный результат: опубликованная сборка теперь **байт в байт** та же, что даёт локальный
`pnpm build:web` — раньше отличалась префиксом.

**Один поучительный провал по дороге.** Первый прогон умер за 0 с на разборе workflow, без
шагов и без аннотации, которую отдаёт API. Комментарий, объяснявший, почему тема коммита
не подставляется выражением GitHub, **сам содержал разделители этого выражения**. Actions
разбирает их где угодно в файле, включая комментарии внутри `run:`, а пустое выражение
невалидно. В комментарии теперь прямое предупреждение не писать их даже в комментарии.

### 21.2. `https://txt2sfx.github.io` — в дефолтном allowlist моста

Переезд в корень **не решал** исходную проблему: origin — это схема+хост+порт, путь в него
не входит, поэтому и на `/`, и на `/txt2sfx/` браузер присылал один и тот же
`Origin: https://txt2sfx.github.io`. Решение — внести его в дефолт (`PLAYGROUND_ORIGIN` в
`packages/bridge/src/http.ts`).

Аргумент за: смысл демона — «код, который обязан работать на машине человека, доступный со
страницы, которую хостят где угодно». Для почти каждого эта страница — наша, и дефолт, при
котором задокументированный сценарий отвечает `403`, — это дефект дефолта, а не урок
пользователю. Тем более что объясняющая фраза («restart it with `--allow-origin …`»)
рисовалась только внутри диалога моста, по клику на бейдж.

Что это даёт, прямым текстом: всё, что даёт паринг, — токен, сокет, playground-методы, и
`sfx_export` по-прежнему в границах `--allow-write`. Это ровно то доверие, которое человек
уже оказал, запустив `npx txt2sfx-bridge` и открыв эту страницу. Чем это **не** является:
разрешением «любой https-странице». Одна точная строка, сравниваемая целиком — без
маски, без порта, и `http://txt2sfx.github.io` это уже другая строка (4 таких соседа
закреплены тестом).

Остаточный риск записан в коде, а не спрятан: это `*.github.io`, и GitHub отдаёт с того же
хоста любой репозиторий того же владельца. Сегодня весь origin — это один репозиторий, в
котором лежит только собранный плейграунд (Pages на репозитории-источнике отключён), но
будущий project-сайт в той же организации унаследовал бы это доверие. Ровно это и есть
аргумент за собственный домен, если он когда-нибудь появится.

### 21.3. Что осталось

- Флага «не доверять и опубликованной странице» нет. Если понадобится — это `--only-origin`
  или `--no-default-origins`, но выдумывать его без спроса не стали.
- Внешние ссылки на `/txt2sfx/` теперь битые: между project- и org-сайтом GitHub редиректа
  не делает. В README и в манифесте моста всё поправлено.
- Deploy keys разрешены теперь всем репозиториям организации, не только этому. Тумблер
  можно вернуть: политика проверяется при добавлении ключа, уже добавленный продолжит
  работать.

## 22. Вкладка `Search` — библиотека Freesound как источник референсов (2026-08-08)

Заказчик попросил третий режим рядом с `Soundline` и `Model`: модель формулирует поисковый
запрос, страница ходит на freesound.org, показывает лучшее с прослушиванием, лицензией и
ссылкой. Форма запроса — та же, что у двух соседних вкладок, и поэтому вкладка, а не новый
инструмент: **это ещё один способ добыть B-сторону**. Model рендерит цель диффузией,
Search её находит записью; ни то ни другое не является поставляемым артефактом.

Побочный, и на самом деле главный, результат — снятие блокера v0.3 из ROADMAP: «интересным
целям бенчмарка нужны записи, а для этого нужен набор с лицензией, которую мы можем
опубликовать». Фильтр `license:"Creative Commons 0"` — это и есть ответ, только пока
вручную и по одному звуку.

### 22.1. Что проверено до написания кода

Всё решение держится на двух заголовках, поэтому они измерены curl'ом, а не взяты из
документации:

```
GET  freesound.org/apiv2/search/text/   → 401 + Access-Control-Allow-Origin: *
OPTIONS (preflight, Request-Headers: authorization)
                                        → 200 + Access-Control-Allow-Headers: …authorization…
                                                Access-Control-Max-Age: 86400
GET  cdn.freesound.org/previews/…       → Access-Control-Allow-Origin: *, Accept-Ranges
```

Значит: прокси не нужен ни для поиска, ни для превью; статическая сборка на Pages работает
как есть; превью можно **декодировать** через `decodeAudioData`, а не только проиграть
`<audio>`, — то есть найденный звук попадает в ту же точку, что и перетащенный файл.
Ошибка ключа приезжает как `401 {"detail":"Invalid token"}`.

### 22.2. Решения

| решение | почему так |
| --- | --- |
| **вкладка, а не поиск в галерее** | галерея — это наши рецепты, у каждого есть исходник, слоты и экспорт. У найденной записи нет ничего из этого: это цель. Смешать их — значит пообещать, что найденное можно тюнить |
| **запрос пишет `searchQuery` из `query.ts`, нового промпта не заводится** | индекс банка и индекс Freesound хотят одного и того же — английских слов материала, объекта и события. Второй системный промпт, говорящий то же самое, — это день, когда они разойдутся. Политика отказа тоже совпадает: плохой запрос стоит худшей выдачи, а не сорванного рендера, поэтому падает молча в сырой промпт |
| **фильтры детерминированные, ранжирование модельное** | «скрипты проверяют, модель судит» буквально. Длительность, лицензия, каналы, частота — факты из ответа API: ими фильтруем и сортируем кодом. Название, теги, описание — текст, и по нему решает модель, отдавая порядок и одну строку «почему». Ни один звук не выпадает из выдачи от того, что модель промолчала: она переставляет, а не отбирает |
| **`Authorization: Token …`, а не `?token=`** | ключ не оказывается в URL — то есть ни в истории, ни в реферере, ни в логах. Цена — preflight, и она разовая: `Access-Control-Max-Age: 86400` |
| **ключ Freesound живёт ровно там же, где ключ Gemini** | это credential: память вкладки, `keystore` по явной галочке, `localStorage` не участвует. Отдельная запись `freesound` — свой ключ, своё «забыть» |
| **скачивается превью, оригинал — ссылкой на страницу** | оригинал закрыт OAuth2 (token-auth его не отдаёт), а OAuth2 — это редирект и client secret, которых у статической страницы нет и не будет. Честный ответ: кнопка отдаёт превью (mp3, ~128 кбит) и говорит, что это превью, а рядом ссылка на страницу, где лежит оригинал |
| **CC0 — фильтр по умолчанию, лицензия — на карточке** | CC-BY требует атрибуции в конечном продукте, а решение об этом принимает не тот, кто нажал «скачать», и не через месяц. Поэтому по умолчанию видно только то, у чего обязательств нет, а переключение на «все лицензии» показывает бейдж и готовую строку атрибуции у каждого результата |
| **найденное — новый вид B-стороны `library`, а не `upload`** | подпись под графиком объясняет, *что* сравнивается; «ваш файл, выровненный по атаке» про запись из библиотеки — неправда, а неправда в подписи к метрике дороже одной строки кода |
| **проверка звука — тем же анализатором** | превью декодируется, и дальше работает всё, что уже есть: Compare A/B, `⌖ Fit to reference`, `match reference` на прогоне. Модель судит слова, анализатор меряет звук |

### 22.3. Что сделано

| файл | что в нём |
| --- | --- |
| `packages/agent/src/rank.ts` | `rankSounds` + `parseRanking`: единственная стадия, к которой допущена модель. Возвращает **мнение о порядке**, а не отбор; строгий разбор выбрасывает id, которых не было в списке (иначе на экране появилась бы строка, которой поиск не возвращал, и мёртвая кнопка play) |
| `apps/web/src/lib/freesound.ts` | клиент APIv2: запрос, разбор, лицензии, строка атрибуции, `fetchPreview` → `File` (та же дорога, что у перетащенного файла) |
| `apps/web/src/lib/useSearch.ts` | состояние поиска выше экранов — по той же причине, что и прогон генерации: панель размонтируется при каждом уходе в Compare, а терять вставленный ключ и выдачу на переключении вкладки нельзя |
| `apps/web/src/components/SearchPanel.tsx` | вкладка: ключ, чипы лицензии и длины, строки результатов, лицензия ссылкой |
| `PromptRow`, `Studio`, `ComparePanel`, `App` | четвёртая вкладка, `BKind = 'library'`, свой цвет (`HUE.library`), кнопка строки промпта действует на активную вкладку |

Запрос пишет **тот же `searchQuery`**, что и банк, а провайдера выбирает **тот же
`captionProviderFor`**: обе стадии — про английские слова, а не про синтез, и `mock`
(который отвечает готовым рецептом) не годится ни для той, ни для другой.

### 22.4. Что проверено и чем

- **CORS и preflight** — curl'ом до написания кода (см. 22.1), затем в браузере: запрос
  из вкладки ушёл на `freesound.org/apiv2/search/text/` с `Authorization`-заголовком,
  вернулся `401`, и страница **прочитала тело** — что и означает, что заголовки на месте.
  В консоли ни одной ошибки CORS.
- **Сквозной сценарий** — с подменённым `window.fetch` (страница не менялась, подменена
  только глобальная функция): две строки выдачи, бейджи `CC0` и `CC-BY` разного цвета,
  `→ B` скачивает превью, декодирует и кладёт в референс — Compare показывает B,
  подпись «запись с freesound.org», а анализатор меряет её (тестовый тон 440 Гц дал
  dominant 431 Гц, centroid 441 Гц).
- **Отказ ключа** — настоящим запросом с неверным ключом: красная плашка с нашей фразой
  и словами библиотеки (`Invalid token`) под ней.
- **32 теста**: 12 в `packages/agent/test/rank.test.ts` (разбор, выдуманные id, повторы,
  проза между строк, все фолбэки) и 20 в `apps/web/test/freesound.test.ts` (форма
  запроса и поля закреплены с инжектированным `fetch`, ключ только в заголовке, `by-nc`
  не читается как `by`, строка атрибуции, `safeName`). Всего 949 зелёных, `typecheck`
  чистый по пакетам и плейграунду.

### 22.5. Что осталось

- **Оригинал не скачивается** — только превью (~128 кбит mp3). Оригинал за OAuth2, а это
  редирект и client secret, которых у статической страницы нет. Панель говорит это прямо
  и даёт ссылку на страницу звука.
- **Модель не слышит выдачу.** Ранжирование — по названию, тегам и длительности. Второй
  проход (декодировать превью и померить своим анализатором) архитектурно ничего не
  ломает — `fetchPreview` уже отдаёт `File`, — но не сделан.
- **Одна страница выдачи**, 30 результатов, пагинации нет. Счётчик честно говорит
  «в библиотеке 137, здесь 30».
- **Теста на `useSearch` нет**: в репозитории нет окружения для React-компонентов
  (`environment: 'node'`), и заводить его ради одного хука не стали. Логика, которую
  можно проверить без React, вынесена в `freesound.ts` и `rank.ts` и покрыта.
- Локализованы английский и русский; остальные семь языков читаются по-английски — это
  политика `apps/web/test/i18n.test.ts`, а не забытая работа.
- Лимиты ключа Freesound нигде не показаны: `429` отличается от `401` и объясняется
  отдельной фразой, но счётчика «сколько осталось за сегодня» нет — API его не отдаёт.

### 22.6. Общий ключ на бекенде запрещён условиями API — прочитано, а не предположено

Возник вопрос: у заказчика есть токен, бекенд в разработке — не отдать ли поиск через
него, чтобы посетителю не нужно было регистрироваться. Ответ дают
[условия API](https://freesound.org/help/tos_api/), пункт 4(d) ограничений:

> Use, copy, distribute or modify the API or Content in any "service bureau" or
> "timesharing" business to end users **that are not logged on to the Platform with valid
> user credentials and live session**.

Плюс: «You will be provided with one Access Key per Application… may be revoked if you
share them with any third party». То есть наш ключ, обслуживающий анонимных посетителей
страницы, — это ровно описанный service bureau, и цена ошибки — отзыв ключа, а не
замечание.

Что из этого следует:

| | можно | почему |
| --- | --- | --- |
| **ключ пользователя в его вкладке** (сделано) | да | он и есть «logged on to the Platform with valid user credentials»: ключ выдан лично ему |
| **наш ключ на бекенде для анонимов** | **нет** | 4(d) |
| **наш ключ для сборки корпуса бенчмарка** | да | 1(a) и 1(b): Application забирает Content для собственной работы, а не раздаёт его |
| **долгий кэш выдачи у нас** | нет | 1(b) разрешает «limited intermediate copies… deleted when they are no longer required» — короткий TTL, не витрина |
| **коммерческое использование** | отдельный разговор | «negotiated on a case by case basis with UPF» |

Дверь без вставки ключа существует, и это **OAuth2**, а не наш ключ: authorization code
grant, `client_id`/`client_secret` держит бекенд, пользователь логинится своим аккаунтом
Freesound, запросы уходят с **его** `Bearer`-токеном (24 ч, есть refresh). Тогда каждый
конечный пользователь действительно «logged on with valid credentials and live session»,
а не прячется за нашим ключом. Побочно это снимает и главное ограничение вкладки:
`Download Sound` («allows you to download a sound in its original format/quality… requires
OAuth2») перестаёт быть недоступным, и кнопка сможет отдавать оригинал, а не превью.
Превью аутентификации не требуют ни в каком случае — это подтверждено документацией
ресурса, и потому текущая вкладка работает вообще без OAuth2.

---

## 17. Публичный банк: аккаунты, лайки, комментарии (2026-08-08)

Заказчик: «лёгкий бекенд, который хранит soundline, лайки и комментарии; нужно придумать,
как предотвратить спам и флуд и принести социальную пользу проекту». Выбраны: GitHub
OAuth, комментарии с текстом без ссылок и опциональным soundline, свой VPS
`txt2sfx.pix3.dev`, деплой через GitHub Actions по изменениям сервера.

### 17.1. Главное решение: цена входа — звук, а не капча

`node-web-audio-api` переведён из devDependencies в dependencies `apps/server`, и
`POST /api/recipes` теперь **рендерит** то, что публикует. Правило «нет аудиостека ниже
`apps/`» не нарушено: сервер и есть `apps/`, а сидер уже держал аудиостек именно там.

Три следствия сразу, и только первое — про корректность:

1. **Профиль больше нельзя подделать.** Это было единственное поле, приходившее на веру
   (§8: сервер не умеет мерить), а рецепт с выдуманным профилем отравляет любое
   расстояние, посчитанное против него, и никто ниже по течению не объяснит почему.
2. **Опубликовать не-звук физически нельзя.** В системе нет поля, куда спамер напишет
   сообщение: имя и промпт коротки и индексируются, тело — DSL, который либо компилируется,
   либо отклонён.
3. **Запись стоит настоящей работы** — не капчи, а того самого, ради чего сервис
   существует. Честный автор платит один раз за звук, флуд платит за попытку, причём
   перед лимитером, который уже сказал «нет».

DoS-поверхность мала по построению: компилятор режет рендер на 5 с
(`DEFAULT_MAX_DURATION_MS`), валидатор — на 6 слоёв, то есть работа на запрос ограничена
**до** старта. Остаётся навал, поэтому рендеры идут через очередь шириной 2, а токен
лимита тратится **перед** очередью. Таймаута сознательно нет: `startRendering` нативный и
из JS не отменяется, таймаут освободил бы запрос и оставил работу крутиться — видимость
ограничения без ограничения.

### 17.2. Идентичность рецепта — каноническая форма

`fingerprint = sha256(serialize(parse(src)))`, UNIQUE. Это одновременно и идемпотентность
повтора (была по паре `(name, soundline)`, обходилась одним пробелом), и антифлуд: тысяча
перепостов под тысячей имён — одна строка. Индекс частичный (`WHERE fingerprint <> ''`),
потому что рецепт, который парсер больше не принимает, канонической формы не имеет, и все
такие строки иначе схлопнулись бы на пустой строке — банк стал бы недоступен для записи со
второй такой записи.

### 17.3. Кто пишет: GitHub, и один возрастной гейт

Анонимный лайк — это счётчик без смысла: его можно нажать миллион раз скриптом, и никакая
арифметика по IP этого не меняет, только наказывает тех, кто делит выходной узел.
Анонимный комментарий нельзя модерировать: банить нечего. Поэтому запись стоит аккаунта, а
**чтение не стоит ничего и никогда** — экрана логина перед звуком нет.

Провайдер — GitHub: у аудитории он уже есть, нам не нужны ни пароли, ни почта, ни
восстановление, а аккаунт, который уже существует, имеет историю, которой у однодневки
нет. Scope не запрашивается вообще: токен читает публичный профиль и больше ничего,
используется один раз и выбрасывается — база с чужими GitHub-токенами это обязательство
без соответствующей функции.

**Возрастной гейт `MIN_ACCOUNT_AGE_DAYS = 30` — и есть вся защита от промышленного
спама.** Спам — это бизнес с моделью издержек: ему нужны аккаунты пачками и сейчас, а
аккаунт, который должен был существовать месяц, оптовый поставщик не наколдует. Всё
остальное (лимиты, запрет ссылок) — против упорного одиночки.

Сессия — bearer в заголовке, **никогда не cookie**: браузер тогда не прикрепит её к
запросу, который сделала чужая страница, и CORS может остаться открытым — то самое
свойство, ради которого банк существует (§8.3, «чужой агент с чужой страницы без
договорённостей»). В базе лежит только хэш токена.

Передача сессии на страницу — одноразовым кодом в фрагменте (`#signin=…`, 60 с), который
меняется на токен по POST. Bearer прямо в редиректе осел бы в истории, в кнопке «назад» и
в чужом скриншоте. Возврат разрешён только на origin из списка: открытый редирект на
эндпоинте логина — это способ доставить сессию кому угодно.

**Solo-режим.** Без настроенного OAuth-приложения банк работает как раньше: один встроенный
аккаунт `local`, вход не нужен, — и **отказывается слушать не-loopback** в этом состоянии.
Анонимный записываемый банк на публичном интерфейсе — не конфигурация, которую кто-то
выбирает осознанно. Так работают ноутбук и все тесты.

### 17.4. Социальная польза — обратная связь в цикл, а не витрина

`rating` уже участвует в ранжировании `/api/retrieve` и в `selectFewShot` (§14.3). Значит
лайк — не тщеславие: сообщество показывает **чужой модели**, какие рецепты стоит
имитировать. Это и есть ответ на «принести социальную пользу», и это же оправдывает
защиту лайка аккаунтом и лимитом.

Дальше:

- **Комментарий может быть звуком.** `{ body, soundline }`, и вложенный soundline проходит
  тот же парсинг-валидацию-рендер, что и публикуемый рецепт. Самое ценное действие в
  системе — то, которое бот выполнить не может. Хранится в `comments`, а не в `recipes`:
  контрпредложение — это ответ, а не запись банка, оно не должно попадать в few-shot и не
  должно бороться за уникальный fingerprint.
- **Ссылок в комментариях нет вообще.** Комментный спам — экономическая деятельность, и
  полезная нагрузка всегда ссылка. Запрет убирает выгоду целиком и не стоит ничего:
  единственное, на что здесь нужно ссылаться, — другой рецепт, и `#12` делает это лучше
  URL, потому что плейграунд рисует его нажимаемым. Отказ с причиной, а не тихая вырезка:
  текст, который молча теряет часть себя, хуже текста, который не приняли.
- **Родословная.** `parent_id` плюс автор: галерея показывает, из чего что выросло, и
  копится корпус с ответом «почему звук такой».
- **Корпус выкладывается наружу.** `pnpm --filter @txt2sfx/server dump` пишет канонические
  `.soundline` и `bank.jsonl` с профилями, лайками и авторством. Это то, что делает банк
  выбрасываемым: если сервис исчезнет, звуки останутся, а история банка — это git log, а
  не обещание. Ни комментариев, ни сессий, ни жалоб в дампе нет.

### 17.5. Лимиты — ведро токенов, и оно зарабатывается

Счётчик в сутки плох дважды: позволяет потратить дневную норму за секунду (флуд — это
ровно оно) и обнуляется на границе, которую все видят. Ведро тратится с той же скоростью,
с какой пополняется: «двадцать в день» должно означать «не двадцать сразу». Токены —
float, пополнение непрерывное от прошедшего времени; ни таймера, ни фоновой уборки.

Норма растёт от **полученных** лайков, а не выданных (иначе способ купить норму — нажать
кнопку шестьдесят раз): ×2 от 10 лайков, ×3 от 50, дальше потолок — аккаунт с репутацией,
который однажды испортится, не должен ещё и иметь самый громкий рупор.

Отказ возвращает 429 с `Retry-After` и говорит, чем норма растёт. Токен списывается
**после** парсинга, валидации и проверки дубля: отказ, который ещё и стоит попытки, учит
не пробовать, а не чинить звук.

### 17.6. Модерация: прятать, а не удалять

`hidden`, а не `DELETE`. Скрытое уходит из всех выдач, из поиска, из `/api/retrieve` и из
дампа, но строка остаётся там, где на неё можно посмотреть ещё раз: модерацию здесь делает
один человек без второй пары глаз, и действие, уничтожающее улику, пересмотреть нельзя.

Роли нет, есть `TXT2SFX_ADMIN_TOKEN` в окружении сервиса — честная форма для одного
оператора. Без токена маршруты **не регистрируются вовсе**: пароль по умолчанию хуже
отсутствующей функции, а эндпоинт, отвечающий 401, уже подтвердил, что существует.
Поэтому же неверный токен получает 404. Сверху nginx запирает `/api/moderation/` на
loopback.

Бан снимает живые сессии сразу: бан, который ждёт следующего входа, не наступает никогда.

### 17.7. Что сломано намеренно

- **`POST /api/recipes/:id/vote` удалён.** Он принимал `{delta: ±1}` от кого угодно, то
  есть рейтинг был тем, что сказал последний запустившийся скрипт. Вместо него `PUT`/
  `DELETE .../like`: состояние одного человека, идемпотентно в обе стороны, счётчик
  выводится из строк.
- **`profile` в теле публикации игнорируется** (принимается ради старых клиентов).
- **`findIdentical(name, soundline)` → `findByFingerprint`.**
- Тестовые фикстуры банка теперь дают каждому рецепту свой soundline: раньше все
  использовали один и тот же текст, и с UNIQUE это стало вторым дублем. Гнётся фикстура,
  а не правило.

### 17.8. Развёртывание

VPS Ubuntu 24.04 (общий с другими сайтами), Node 24.15, nginx 1.29, certbot.
`/srv/txt2sfx/{app,data,corpus,env}`, systemd-юнит от непривилегированного пользователя с
`ProtectSystem=strict` и записью только в `data/` и `corpus/`, `CPUWeight=200` (банк должен
проигрывать ядро игровому серверу-соседу), `MemoryMax=512M`.

Деплой — `deploy-server.yml` с фильтром путей: сервер и всё, с чем он слинкован
(`core`, `shared`, `analyzer`, `agent`), но не `apps/web` — плейграунд публикует себя из
`ci.yml`. Сборка на самой машине: альтернатива — тарбол, а это решение о том, что в него
класть (dist шести пакетов, `node_modules` с symlink-раскладкой pnpm, нативный модуль под
нужный libc), и любая ошибка там даёт сервис, который стартовал и падает на первом
рендере. Коммит пинится по SHA, не по ветке: применяется то, что тестировали, а не то,
на что `main` указывает к моменту открытия соединения.

### 17.9. Открытые вопросы

- **OAuth-приложение GitHub создаёт заказчик** (client id/secret → `/srv/txt2sfx/env`).
  До этого банк живёт в solo-режиме и слушает только loopback, то есть публично он в этом
  состоянии просто не отвечает.
- **Таймер дампа корпуса и публичный репозиторий под него** не заведены: команда есть,
  расписание и ключ на push — решение заказчика.
- **Формула нормы не измерена** ни на каком реальном трафике, потому что его ещё нет.
  Менять её надо в `allowanceFor`, и причина должна лечь в комментарий рядом.
- **Список TLD в правиле про ссылки** — компромисс: он ловит то, чем торгуют, и не
  трогает `sword-clash.wav`, `attack.ms` и `e.g.`. Правило, отклоняющее обычные
  предложения, удалят через неделю вместе с защитой.

### 22.7. OAuth2 вместо ключа: что переделано (2026-08-08)

Заказчик посмотрел настройки Freesound: **опции «пользователь приходит со своим ключом»
там нет**, выдаётся приложение, а приложение доходит до людей через OAuth2. Плюс §4d,
разобранный выше. Решение: оставить только OAuth2-путь, поле ключа убрать.

| решение | почему так |
| --- | --- |
| **токены живут на клиенте, сервер не хранит ничего** (выбор заказчика) | у сервера нет ни строки, ни таблицы, ни аккаунта под это: обмен `code → token` подписывается client secret'ом, которого нет в браузере, и на этом его роль кончается. Отсутствие закреплено тестом («no row, no session, no account»). Побочно снялся вопрос «нужен ли вход через GitHub, чтобы поискать звук» — не нужен |
| **тот же танец редиректов, что у входа** | `?return=` по allow-list, одноразовый `state`, одноразовый код во фрагменте (`#freesound=…`), обмен по `POST`, фрагмент стирается `replaceState` до обмена. Формы намеренно одинаковые: кто понял одну, понял обе, и один класс ошибки нельзя допустить двумя разными способами |
| **`/refresh` без аутентификации у банка** | он аутентифицирует себя сам: вызывающий предъявляет refresh-токен, который Freesound выдал **ему**, а банк добавляет только client secret. Требовать сверху аккаунт банка значило бы привязать подключение Freesound к системе личности, к которой оно отношения не имеет |
| **`Authorization: Bearer` из вкладки прямо в библиотеку** | у `search`, у превью и у `download` замерен `Access-Control-Allow-Origin: *`, поэтому проксировать нечего: обещание «ничего не идёт через наш сервис» сохраняется, трафик оригиналов мимо нас |
| **кнопка скачивания отдаёт оригинал** | `Download Sound … requires OAuth2` — ровно то, чего не мог вставленный ключ. Имя берётся из поля `type` библиотеки, а не угадывается: FLAC, сохранённый как `.wav`, — та же ложь, что переименованный WAV |
| **одна повторная попытка и только на один код** | access-токен живёт 24 ч, поэтому «библиотека отвергла токен» — это ожидаемый конец долгой сессии: обновить и повторить один раз. Слепой ретрай на любой ошибке превратил бы throttling в удвоенное число запросов |
| **`remember` шифрует в keystore** | то же место, что у ключа Gemini; без галочки подключение живёт, пока открыта вкладка. Хранится по сути refresh-токен: access всё равно суточный |

Новое: `apps/server/src/freesound.ts`, `apps/server/src/routes/freesound.ts`,
`apps/web/src/lib/freesound-auth.ts`; поле ключа в панели заменено блоком подключения на
четыре состояния (банк не умеет / не подключено / подключаемся / подключено).
`FREESOUND_CLIENT_ID` и `FREESOUND_CLIENT_SECRET` — в `.env.example`, redirect URI
приложения: `<PUBLIC_URL>/api/freesound/callback`.

**Найденный по дороге дефект, поучительный.** `fetch`, положенный в `useRef`,
вызывается как метод объекта-рефа — WebIDL отвечает `Illegal invocation`. Проба
`/api/freesound/config` ловила это в `catch` и честно сообщала «банк не умеет
подключать»: настроенный банк выглядел ненастроенным. Лечится `fetch.bind(globalThis)`;
в коде стоит объяснение, потому что симптом не указывает на причину.

**Проверено:** 15 тестов сервера (форма запроса, allow-list, одноразовость `state` и
кода, отсутствие следов в базе, 400 против 502), 33 теста плейграунда (`Bearer`,
`fetchOriginal`, разбор и стирание фрагмента, границы свежести токена). В браузере пройден
весь путь до внешней страницы: кнопка → `/api/freesound/start` → 302 →
`freesound.org/apiv2/login/?next=/apiv2/oauth2/authorize/…`, и возврат с
`#freesound_error=cancelled` — фрагмент стёрт, фраза показана. Всего 1086 зелёных.

**Не проверено сквозняком:** обмен настоящего кода на настоящий токен, поиск с
`Bearer`, скачивание оригинала. Для этого нужны реальные `client_id`/`client_secret`
зарегистрированного приложения с redirect URI на живой `PUBLIC_URL`.

## 23. `NeurosLoop` — саундтрек из тех же примитивов, что и эффект (2026-08-08)

Третий экран рядом с `Sounds` и `Studio`: восемь-шестнадцать тактов игровой музыки,
собранных из **тех же** soundline-рецептов. Дорожка не владеет синтезатором — она владеет
*семейством*, а семейство это шаблон, превращающий `(высота, громкость, длина, яркость)`
в обычный текст рецепта. Дальше работает всё, что уже есть: парсер, компилятор в IR,
офлайн-рендер, `codegen`.

Это и есть причина, по которой раздел вообще принадлежит этому репозиторию: заявление
проекта не изменилось — *опиши, получи код, который это играет*, — изменилась только
длина того, что описывают.

### 23.1. Композитор — функция, а не модель, и экран об этом не врёт

Кнопка `Compose` **не ходит к модели**, и интерфейс нигде не утверждает обратного.
Пресет задаёт палитру (темп, тональность, число тактов, набор дорожек), `knobsFor`
читает из промпта горсть ключевых слов, остальное — один засеянный PRNG на дорожку.
Пресет + промпт + соль воспроизводят аранжировку побайтно.

| решение | почему так |
| --- | --- |
| **детерминированный композитор вместо модели** | модель написала бы мелодию лучше. Но для неё нужен ключ, сетевой круг и цикл починки ради результата, который **нечем провалидировать**: «физики аккордовой последовательности» для валидатора не существует. Засеянный композитор честен насчёт того, что он генератор *правдоподобных* аранжировок: мгновенный, бесплатный, работает офлайн — и, главное, даёт ноты, из которых живут экспорт в MIDI и в JS. Кому нужна мелодия лучше — у того есть MIDI |
| **таблица ключевых слов только английская** | это не проблема перевода: слова здесь — *музыкальное указание*, и честное решение — модель, а не ещё восемь словарей. Промпт на другом языке всё равно меняет seed, то есть даёт свою аранжировку; он просто не двигает темп. Сказано прямо в подсказке под строкой (`loop.hintNew`) |
| **громкость ноты запечена в рецепт** | текст *является* ключом кэша и ключом экспорта. Нота, отличающаяся только громкостью, обязана быть другим рецептом — иначе модулю пришлось бы оборачивать сгенерированный код узлом усиления, а он сам подключается к `ctx.destination`. Скорость квантуется шагом 0.05, поэтому шестнадцать тактов хэта схлопываются в три-четыре рендера |
| **баланс дорожек — отдельно от velocity** | `LaneSpec.level` применяется ровно в одном месте (`voiceFor`). Velocity — музыкальный акцент, он уезжает в MIDI; level — решение о миксе. Свести их значило бы, что «прибрать шейкер» молча переписывает партитуру |
| **у голосов нет категории** | категория — это обещание судиться по набору физических инвариантов, а «соль третьей октавы, держится два такта» — не `pop`, не `impact` и не `cycle`. `misc` — честный ответ, и сериализатор его всё равно опускает |

### 23.2. Петля — один буфер, и хвост заворачивается на голову

Аргумент тот же, что у `loop()` в `lib/engine.ts`, только сильнее: шестнадцать тактов —
это несколько сотен событий, и живой граф означал бы пересоздание сотен осцилляторов на
каждом круге со швом там, где граница попала в атаку.

Смикшировано один раз, играется через `AudioBufferSourceNode`. Сэмплы за концом
**сворачиваются в начало** — это и есть разница между петлёй и куском, который сыграли
дважды. Тест `mixLoop` проверяет заворот отдельно, потому что это единственная арифметика
здесь, которая ломается незаметно до восьмого такта второго круга.

**Хедрум — единственное место, где нормализация уместна.** `renderSound` сообщает о
клиппинге и никогда не нормализует, потому что у клиппующего *рецепта* есть автор,
который это чинит. У микса автора нет: это сумма двенадцати независимо корректных
рецептов, и число, при котором они складываются в 1.8, не писал никто. Поэтому микс
масштабируется до 0.95 и **сообщает применённый коэффициент строкой под таймлайном** —
тихо применённый хедрум был бы изменением уровня, с которым пользователь ничего не может
сделать.

### 23.3. Индикатор сборки измеряет, а не тикает

Очевидный способ сделать строку «раскладываю барабанную сетку…» — `setInterval`,
открывающий дорожку каждые 800 мс. Выглядит так же и является ложью: индикатор прогресса,
который ничего не меряет, учит не доверять всем остальным в приложении.

Дорожки открываются по мере **фактического** завершения: написать ноты, отрендерить все
её различные голоса (шестнадцать тактов хэта — три-четыре офлайн-рендера, пэд — шесть).
Рендеры ложатся в кэш, так что финальный микс ничего не пересчитывает.

### 23.4. Что сделано

| файл | что в нём |
| --- | --- |
| `apps/web/src/lib/loop.ts` | модель: гаммы, ступени, пресеты, `knobsFor`, генераторы паттернов, `laneClips` (картинка выводится из нот, а не рисуется по таблице форм) |
| `apps/web/src/lib/loop-voice.ts` | стык с остальным проектом: нота → soundline. Семь семейств, ударные — по номерам General MIDI |
| `apps/web/src/lib/loop-render.ts` | `mixLoop` (чистая, тестируется в Node), кэш голосов по тексту, `renderLoop`, `renderStems` |
| `apps/web/src/lib/loop-export.ts` | модуль JS/TS (таблица `codegen`-функций + расписание), настоящий SMF format 1, перкуссия на канале 10 |
| `apps/web/src/lib/useLoop.ts` | состояние выше экранов; ничего не персистится — соль воспроизводит аранжировку, а схема в `localStorage` была бы миграцией без выгоды |
| `screens/Loop.tsx`, `components/LoopTimeline.tsx` | экран и таймлайн |
| `download.ts` | четвёртый список форматов и четвёртая память выбора (`loop`) |

### 23.5. Что проверено и чем

`apps/web/test/loop.test.ts`, 28 тестов, четыре класса лжи, которые ищет этот файл:

- **голос, который не парсится** — каждый различный голос каждого пресета парсится,
  рендерится через `node-web-audio-api` и обязан звучать (`peak > 0.001`) и не клиппить;
- **композитор, который не детерминирован** — две сборки одних и тех же входов
  сравниваются нота в ноту;
- **петля со швом** — заворот хвоста проверяется на числах, и отдельно на настоящем
  миксе `cave-ambience`, где каждая нота длиннее своей клетки;
- **`.mid`, который не MIDI** — заголовок, число треков, канал 10 у ударных.

Плюс сквозной микс на настоящем стеке (`OfflineAudioContext` из `node-web-audio-api`
подставляется глобально — единственное место в тестах, где это делается).

Замеры на трёх пресетах: `overworld-run` 30 с петли, 98 различных голосов, 355 ударов,
JS-модуль 49 кБ, MIDI 3.3 кБ; `cave-ambience` 24 с, 41 голос, 51 удар, 21 кБ / 600 Б.
Микс плотных пресетов приходит к пику ~1.8 и убирается на ~5.6 дБ — это видно строкой.

### 23.6. Что осталось

- **Не проверено в браузере.** Chrome DevTools MCP в этой сессии был занят другим
  профилем; проверены сборка (`vite build`), типы и тесты, но не клик по экрану.
- Модуль JS — десятки килобайт, а не сотни байт: это цена аранжировки из ста рецептов.
  Отчёт, а не гейт, как и бюджет 1 кБ у `codegen`, но в интерфейсе размер пока
  показывается только в строке статуса после скачивания.
- Промпт не влияет на выбор пресета: «boss fight» в строке при выбранном `menu-drift`
  соберёт `menu-drift`. Пресет — явный контрол, и угадывать за пользователя дороже.
- Переводов, кроме `ru`, нет: `t()` откатывается на английский по ключу.

### 22.8. Правки по результатам прода (2026-08-08)

Заказчик прошёл сценарий на живом сайте. Три замечания, все про то, что вкладка обещала
не то, что делает.

| правка | почему |
| --- | --- |
| **в режиме Search исчезают Soundline, Slots и Export** | ровно тот же аргумент, по которому они исчезают на вкладке Model: у чужой записи нет рецепта. Редактор, стойка слайдеров и карточка экспорта под ней говорили бы, что найденное можно крутить и экспортировать как своё, а нельзя ни того, ни другого |
| **в каждой строке выдачи — волна** | список отвечает на вопрос «которое из них — тот звук», и тридцать имён в столбик отвечают на него плохо: `door_slam_heavy` и `door ambience` различаются тем, что одно удар, а другое подложка, и это видно в ста пикселях огибающей до всякого прослушивания. Рисуется тем же `Bars`, что и карточка галереи |
| **сохранение сразу в нужном формате** | `FormatMenu` в строке, свой список `LIBRARY_FORMATS`, своя память выбора. Первым пунктом — **Original**, и он же дефолт: всё остальное — это мы декодируем и перекодируем чужую работу, то есть потеря качества, принятая за автора. Конвертация всегда идёт от оригинала, никогда от превью (иначе получился бы большой файл с информацией маленького) |
| **авторизация Freesound хранится по умолчанию, без галочки, и стирается по Disconnect** (выбор заказчика) | и это отступление от правила «ключ помнить только по явной галочке» — обоснованное. Ключ вендора это bearer-секрет со счётом, вставленный один раз, отозвать его со страницы нельзя. Здесь — OAuth-грант на **собственный** аккаунт пользователя, полученный кругом, который он сам прошёл, и отзываемый из двух мест: Disconnect тут и настройки Freesound. Подключение, которое надо восстанавливать на каждой перезагрузке, — это не подключение, а церемония |

**Как волна себя окупает.** Рисовать — значит декодировать, декодировать — значит тянуть
превью целиком, а тридцать превью на один поиск это мегабайты, которых никто не просил.
Поэтому строка просит свою волну, только когда попадает на экран (`IntersectionObserver`,
`rootMargin: 200px`), не больше двух декодов одновременно, один раз на звук и никогда
повторно: сорвавшийся декод запоминается как отказ. До этого рисуется `ghostBars` —
приглушённый и выведенный из id, то есть форма, а не утверждение.

**Второй дефект гонки, найденный прогоном.** Boot-эффект (спросить банк, поднять
сохранённое) и эффект возврата (`#freesound=…` → обмен) стартуют вместе, а boot ждёт
круговой запрос — и его `setState('off')` приземлялся **после** удачного обмена, возвращая
подключённую вкладку на кнопку. Лечится функциональным обновлением, которое не понижает
живое состояние. Тот же класс, что `Illegal invocation` из 22.7: симптом («не подключается»)
не указывал на причину.

**Проверено в браузере:** блоки под вкладкой исчезли; три строки выдачи нарисовали три
разные огибающие (удар, подложка, линза) из трёх подставленных превью; меню форматов
открывается в строке; подключение пережило перезагрузку без единой галочки, а после
Disconnect не вернулось и после перезагрузки. 206 тестов плейграунда зелёные.

## 24. Одна модель вместо пяти, и `NeurosLoop` наконец сочиняет моделью (2026-08-08)

Две правки заказчика, и они про одно: подключённая модель должна использоваться, а не
выбираться.

### 24.1. Выбора провайдера больше нет — есть правило

В строке промпта стоял чип с пятью вариантами: `mock`, `agent`, `bridge`, Gemini,
Anthropic. Четыре из них отвечали на один вопрос — «кто сейчас держит языковую модель», —
а пятый, `mock`, отвечал на **другой**: выдавал уже существующий рецепт из каталога под
именем нового. Выбор между ними был работой, которую пользователь не мог сделать хорошо.

Теперь это `chooseProvider` в `apps/web/src/lib/agent.ts`, чистая функция из двух строк:

| условие | кто отвечает |
| --- | --- |
| к мосту подключён кодовый агент | **агент** — всегда, даже если ключ вставлен: он бесплатный, он сильнее и его подключили нарочно |
| агента нет, ключ Gemini есть | **Gemini** |
| ни того ни другого | никто, и строка говорит это **до** нажатия, а не после |

Одну функцию читают пятеро: `disabled` у кнопки, подпись рядом с ней, заголовок модалки,
шаг подписи для диффузионной модели и композитор `NeurosLoop`. Пять копий одного `if` —
это день, когда двое из них разойдутся и интерфейс начнёт врать про то, чья квота сейчас
потратится.

- `mock` и Anthropic **убраны из плейграунда целиком** (в `packages/agent` оба провайдера
  остались — на них стоит бенч). Вместе с `mock` ушло «работает без ключа»: это
  сознательный размен, названный заказчиком. Пустой каталог с рецептами больше не выдаёт
  себя за генерацию.
- Провайдер devtools (`lib/bridge.ts`) жив, но **недостижим из интерфейса**: `window.txt2sfx.run`
  передаёт его в прогон явным override'ом (`RunContext.provider`). Отладочному инструменту
  нечего делать в таблице, которую читает UI.
- Все настройки модели переехали в модалку из шапки: ключ, id модели, «запомнить» /
  «забыть» и `fit to reference`. Последняя — не про «кто отвечает», но это второй
  переключатель, который трогают раз в час, и держать ради него поповер в строке было
  дороже, чем положить его рядом.

### 24.2. `NeurosLoop`: модель пишет партитуру, а скрипт её судит

В шапке `lib/loop.ts` стояло «композитор — функция, а не модель», и аргумент был: мелодию
нельзя провалидировать так, как soundline, — нет «физики аккордовой последовательности».
Половина этого верна и осталась. Неверная половина — в том, что у **партитуры** физики
достаточно и без вкуса: закрытая таблица дорожек, сетка шестнадцатых, регистр, который
голос вообще может озвучить, бюджет нот, который потянет микс, число тактов, которое
заворачивается в петлю. Всё это проверяется ровно так же, как `pop.max-duration`.

**`scoreline`** (`apps/web/src/lib/score.ts`) — текстовый DSL, а не JSON, и это спор, а не
вкусовщина:

- схема ловит форму, а форма тут не была проблемой: пересекающиеся диапазоны тактов, лига
  без ноты перед ней, две дорожки с одной и той же партией — всё это пишется руками при
  любом контейнере;
- партитура — это и **формат хранения** (модельный трек не воспроизводим ничем, кроме
  самого себя), и **формат чтения** для «Add tracks»: модель видит то, что уже играет;
- ошибка с номером строки чинится лучше, чем указатель в JSON-массив на двести элементов.

Ноты пишутся строкой символов (`X` акцент, `x` обычная, `-` призрачная, `.` пауза,
`=` лига) поверх диапазона тактов, высоты — **ступенями** лада, а не MIDI: модель,
которая не видит номера ноты, не может написать ноту вне тональности. Тишина не
записывается вовсе — лид, вступающий на 13-м такте, это одна строка.

Правил 30, у каждого стабильный id и императивная подсказка (`SCORE_RULES`) — тот же
контракт, что `INVARIANTS`. Ремонт: три захода, по одному классу ошибок за раз, ничего не
спасаем частично (четыре дорожки прошли, пятая нет — возвращается весь документ). Жёсткое
правило цикла в общем виде: **модель просят переписать только когда провалилась
детерминированная проверка**, никогда — потому что аранжировка показалась скучной.

Что модель делает сверх нот: называет трек (в рейле больше нет `boss-fight · take 3`),
пишет строку про каждую партию — и именно она показывается, пока эта партия рендерится,
вместо фиксированной фразы из `LANE_MESSAGE`; в режиме Add сама выбирает, каких дорожек
не хватает, вместо порядка из `SPARE_LANES`; по ↻ переписывает одну дорожку, зная
остальные.

**Что не сдвинулось.** Сеяный композитор не тронут ни на строку — это запасной путь, и он
обязан остаться ровно таким же хорошим. Первые три трека при заходе по-прежнему стоят
ноль. Голоса, микс, шов петли и оба экспорта не знают, кто написал ноты. Свойство
«дорожка появляется, когда её звук существует» пережило одношаговый ответ: фаза `writing`
— это живой промис, а не таймер, и предыдущая аранжировка остаётся на экране и звучит,
пока модель пишет.

### 24.3. Что проверено и чем

- `apps/web/test/score.test.ts` — 23 теста: раскрытие паттерна по тактам (включая
  «пятнадцать тактов плюс сбивка в шестнадцатом»), лиги в длительность, ступени по кругу,
  каждая нота модели превращается в рецепт, который парсится; каждое правило ловит свой
  случай; у каждого правила императивная подсказка.
- `apps/web/test/agent.test.ts` переписан под правило вместо таблицы провайдеров.
- `corepack pnpm typecheck` и `corepack pnpm test` — 65 файлов, 1133 теста, зелено.
  `vite build` собирается.

### 24.4. Что осталось

- **Не проверено в браузере**: Chrome DevTools MCP в этой сессии занят другим профилем.
- **Модельный трек не переживает перезагрузку.** По правилу `session.ts` должен: он стоил
  вызова модели и не воспроизводим ничем. Дёшево — текст партитуры уже лежит на каждой
  дорожке (`LoopLane.score`), восстановление — это парс, а не новая схема хранения.
- Remix («сделай барабаны в полтора раза реже») — тот же механизм, третий режим строки.
- Самокритика по измеренному миксу (peak, headroom) — только если отчёты покажут, что
  реальные треки регулярно теряют больше 6 дБ.
- Переводы новых строк есть только в `ru`; остальные восемь откатываются на английский.

## 25. Диалог модели стал вкладками, а ключ перестал спрашивать разрешения (2026-08-09)

Заказчик прошёлся по диалогу модели и строке промпта списком: шесть замечаний, каждое —
про один и тот же изъян, элемент управления стоит не в той комнате.

**Вкладки стали глобальными.** Пикер клиентов (`Claude Code · Codex · opencode · Cursor ·
Other`) лежал *внутри* шага 1 инструкции по мосту, а ключ Gemini — секцией выше. Вопрос
на самом деле один — **у кого модель** — и ответы взаимоисключающие. Теперь это одна
полоса вкладок над всем диалогом, где `Gemini` — шестая вкладка (фиолетовый акцент: это
единственный ответ, который не кодовый агент), и под ней ровно один ответ. Открывается на
живом: `Gemini`, когда отвечает ключ, иначе `Claude Code` — без ключа и без агента
проблема почти всегда в агенте.

**Чекбокс `запомнить ключ` убран, ключ помнится всегда.** Это был вопрос с одним
разумным ответом, который задавали трижды в час. Тревогу вызывал не выбор, а незнание,
куда ключ уедет, — поэтому подсказка из тултипа стала печатным текстом под полем: хранится
на этой машине, зашифрован в IndexedDB ключом, который нельзя выгрузить, не в
`localStorage`, никуда не отправляется; рядом `забыть его`. `remember` удалён из
`ProviderSettings` — поле, у которого одно значение, это не настройка.

**`подгонять числа под референс` уехало в панель сравнения** кнопкой-защёлкой рядом с
`Подогнать слоты к B`, и появляется только когда B загружен. В диалоге модели это был
единственный чекбокс, отвечавший на другой вопрос (не «кто пишет рецепт», а «куда целятся
числа»), и он был мёртв, пока референса нет. Настройка по-прежнему живёт в
`ProviderSettings` — это то, что получает прогон, — но переключается там, где ей место.

**Адрес моста больше не дублируется.** Он был значением строки `Демон моста` в чек-листе
и *ещё раз* полем в подвале. Теперь адрес редактируется прямо в своей строке, а
`Проверить снова` стоит рядом с ним: это ровно тот же вопрос — слушает ли кто-нибудь по
этому адресу.

**`Копировать промпт для агента` убрана.** Она копировала абзац инструкций для агента,
который ещё не подключён, — то есть для агента, которому из этого диалога всё равно нечего
вставить, — и занимала место главного действия в подвале диалога, где главное действие
это одна команда во вкладке выше. Тот же текст печатает `txt2sfx-bridge doctor`, и
`bridgeOnboardingPrompt` никуда не делся.

**`via your agent` заменён шестерёнкой справа от кнопки генерации.** Ярлык притворялся
управляющим элементом: стоял между промптом и кнопкой, сообщал факт, меняющийся дважды в
неделю, и — главное — его два состояния имели разную ширину, так что кнопка съезжала вбок
от того, вставлен ключ или нет. Шестерёнка — квадратная, нарисованная svg (⚙ на половине
платформ эмодзи), *после* кнопки: строка читается промпт → запуск, а настройки это то,
куда тянешься, когда прочтение не сработало. Единственное состояние, ради которого стоит
прерывать чтение, выжило — когда отвечать некому, шестерёнка янтарная и с точкой. Сама
иконка — присланная заказчиком, и координаты оставлены как есть, вместе с переворачивающим
`matrix(.1 0 0 -.1 0 2000)`: перерисовать её «покрасивее» в 16 единицах значило бы
перерисовать чужую иконку от руки.

**Студия открывается «пишем рецепт», а не чужим звуком.** Промпт с главной уводил в
студию, где стоял выбранный рецепт — в свежей вкладке первый из встроенных. Экран говорил
«вот звук», пока звук ещё писался, да ещё и чужой. `selected` это состояние выразить не
может: он всегда называет какой-то рецепт. Поэтому у студии появился флаг `composing`:
ставится при запуске с главной, снимается, когда рецепт есть (`onGenerated`) или когда
рецепт выбран руками в рейле — это и есть выход, если прогон вернулся ни с чем. Пока он
стоит, на экране только строка промпта и лог цикла, а вкладки, панель, редактор и слайдеры
заменены плейсхолдерами их формы: пустая колонка дёрнула бы лог вверх ровно в тот момент,
когда на него смотрят, а спиннер сказал бы «ждите» рядом с полосой, которая и так называет
стадию. Пульсация останавливается вместе с прогоном — застывший плейсхолдер под прогоном,
который ничего не дал, это правда, а мерцающий продолжал бы обещать.

### 25.1. Что осталось

- **Проверено в браузере** (Chrome DevTools MCP, `vite dev`): шестерёнка, обе вкладки
  диалога, строка демона с полем и `Проверить снова`, кнопка `Generate against B` в обоих
  состояниях, и полный проход главная → студия → скелетон → рецепт на живом агенте. На
  узком окне вёрстка строки чек-листа глазами не смотрена.
- Два налёта грабель по дороге, оба стоят строчки: `{/* ... */}` первым элементом внутри
  `? (` — это не комментарий, а второй элемент без фрагмента, и babel падает там, где tsc
  ещё не смотрел; и `.composing` уже занят `NeurosLoop` (пунктирная однострочная полоса),
  так что новый класс — `.studio-composing`. Глобальный CSS без модулей: имя проверяется
  глазами.
- Переводы новых строк есть только в `en` и `ru`; остальные семь откатываются на
  английский. Из всех девяти удалены ключи, которых больше нет
  (`dialog.copyPrompt*`, `dialog.agentPrompt`, `dialog.daemon`, `dialog.agentAria`).

## 26. Плейграунд полезен без ключа: мастер-слайдеры, пресеты, retrieval, запись (2026-08-09)

До этого без ключа и без агента путь «текст → звук» был тупиком: кнопка генерации
заблокирована, полезное — редактор, слайдеры слотов, Fit, галерея — было, но требовало
уже иметь рецепт. План `.plans/no-key-playground.md` (шесть фаз, исполнены Opus-агентами
по фазам в порядке 1 → 2 → 5 → 3 → 4 → 6) закрывает эту дыру четырьмя механизмами, ни
один из которых не притворяется генерацией.

**Мастер-слайдеры — третий слой «structure and numbers are separate».** Слоты крутят одно
число, `packages/core/src/transform/scale.ts` крутит *класс* чисел: `scaleKind` умножает
каждый литерал данного kind (`freq` | `time`) на ratio и переписывает текст по спанам —
kind берётся из сигнатур, никаких таблиц имён. Реализация повторяет решение оптимизатора
(кандидат — это текст, правки от последнего спана к первому), но общий helper сознательно
НЕ выносился: `optimizer/src/slots.ts` и `web/src/lib/slots.ts` покрыты тестами и
работают, вынос — открытый follow-up (см. шапку `scale.ts`). Ради масштабирования границ
слотов `SlotRange` получил `minLoc`/`maxLoc` (спаны токенов границ; два старых теста
пинили точную форму через `toEqual` — переведены на `toMatchObject`). Brightness — не
чистый kind: lp/hp масштабируются с клампом в границы сигнатуры, слой без них получает
вставку `>> lp` от 12 кГц (прозрачен при ratio=1, центроиды референсов < 8 кГц), `bp`
не трогается — его центр это высота слоя, а не яркость. UI-семантика — jog: база
запоминается при захвате, каждое движение = одно умножение от базы (округление не
накапливается), отпускание коммитит текст и возвращает ползунок в 1.0. Никакой
пост-обработки вне рецепта: что слышно, то и экспортируется.

**Variation** (`apps/web/src/lib/variation.ts`) — сид-рандомизация всех `~`-слотов через
`collectSlots`/`applyVector`/`positionToValue` из оптимизатора; лог-кривая бесплатно.

**`presets/` — 50 рецептов, контракт легче examples.** Каталог с мета-строками
`# prompt:` / `# tags:` в leading-комментариях (`recipeMeta` в core), строгий рендер:
validate без единой ошибки, peak ≤ 0.95, не тишина — helicopter-исключение остаётся
уникальным для examples. Сидер грузит оба каталога с политикой per-directory
(`SeedDirectory`, не строки — чтобы temp-каталог теста не угадывал политику по пути);
под `--strict` клиппящий пресет получает `REFUSED: clipping`. Пресеты вшиты в бандл
(`lib/presets.ts`, тот же `import.meta.glob`), `Origin` += `'preset'`, приоритет
слияния session > examples > bank > preset (при живом банке пресет уже там — дубликат
по имени выпадает). `test/presets.test.ts` пинит полный манифест имён, не количество:
переименование ломает шаренную ссылку так же, как удаление. Рендер-отчёт: max peak
0.743 (plasma-bolt), тишайший 0.147 (ui-hover, намеренно), клипов ноль.

**Retrieval вместо тупика.** Без провайдера промпт уходит в `/api/retrieve` (k=3),
`fallback: true` трактуется как промах — загрузить топ-по-рейтингу как ответ на промпт
было бы ровно тем размыванием границы, за которое убили demo mode; тогда локальный поиск
по вшитым примерам+пресетам (теги как labels с весом 2), нулевое пересечение → null.
Три места, где граница проговаривается: label кнопки («Find in bank», не «Generate»),
строка лога («retrieved, not generated»), трактовка fallback. Hero-кнопка и «Generate it
instead» на главной тоже переименовываются без ключа (`canGenerate`).

**Главная — поиск по банку.** `BankClient.list({ q, category, limit })` на существующий
FTS5-эндпоинт; дебаунс 300 мс только при непустом q; листинг и поиск — один
эффект-владелец `bankList` (два владельца — гонка ответов). Состояние поиска в React
state, не в URL: URL занят share-payload и auth-exchange. При серверном поиске
bank-записи не фильтруются локально по needle (FTS матчит prompt/tags, которых нет в
name+source) — проп `serverFiltered`; чипы при живом банке показывают все категории.

**Запись с микрофона → Fit** (`lib/record.ts`): getUserMedia с выключенными
echoCancellation/noiseSuppression/autoGainControl (обработка под речь съедает именно то,
подо что фитится), `pickMimeType` вынесен чистой функцией (node-тесты без MediaRecorder),
дорожки гасятся на всех путях выхода. B-источник `record` + кнопка «record → Fit»:
второй клик останавливает и сразу фитит; `fit(refOverride?)` принимает буфер напрямую,
минуя stale state. Попутная находка: `onClick={onFit}` после расширения сигнатуры —
runtime-ловушка (React передаст MouseEvent как override, типы это пропускают), обе точки
вызова обёрнуты стрелкой с комментарием.

### 26.1. Что осталось

- **Не проверено в браузере** (агенты работали без запущенного стека): мастер-слайдеры
  и Variation на слух; серверный FTS-поиск с живым банком; весь микрофонный поток
  (разрешение, пульсация чипа, record → Fit, отказ в доступе, Safari/mp4); вид пресетов
  в галерее. Тесты и tsc зелёные (1504 теста, 71 файл), `pnpm build:web` собирается,
  `seed --strict` на чистой базе: 50/50 пресетов stored, refused только helicopter.
- Переводы всех новых строк — только en+ru, остальные семь локалей падают на английский.
- Открытый follow-up из фазы 1: вынести span-replace в общий helper трёх мест
  (optimizer/slots, web/slots, core/transform/scale) — отдельным изменением.
- `corepack pnpm typecheck` на этой машине падает на последнем шаге: скрипт внутри зовёт
  голый `pnpm --filter @txt2sfx/web typecheck`, а pnpm не в PATH. Это особенность машины
  (CI не задевает); гейт гоняется шагами tsc по отдельности.
- Комментарий у seed-шага в `.github/workflows/ci.yml` обновлён: рендерится шестьдесят
  рецептов, не десять.

## 27. Мастер-слайдеры: jog → якорь, и кнопка «в новый» (2026-08-09)

Проверка в браузере (п. 26.1) закрыла первый пункт списка и сразу же отменила решение
1.2 из `.plans/no-key-playground.md`. **Jog оказался неотменяемым.** Обоснование было
про честность: между жестами источник правды — текст, пользователь мог отредактировать
рецепт руками, и ползунок с «×2 от замороженной базы» начал бы врать. Верно, но цена
выше риска: ползунок, возвращающийся в центр, нельзя *отыграть назад*. Увёл высоту вниз,
услышал, что перебрал, — вернуться к исходным числам можно только новым перетаскиванием
на глаз, и каждый раз это другие числа. Ровно на это и пожаловался пользователь
(«сбрасываются в центр после изменения значения»).

**Теперь мастер якорится.** `MasterAnchor { base, positions, source }` в
`apps/web/src/lib/master.ts`: `base` — рецепт, каким он был на момент якоря, каждое
движение пересобирает текст из базы со *всеми тремя* ratio (`moveMaster`), позиция читается
как абсолютная. Центр = рецепт, каким он открылся; вернул ползунок в середину — вернулись
и числа, байт в байт (тест `master.test.ts` это пинит). Округление не накапливается по
более сильной причине, чем у jog: `round(base × ratio)` держится всё время жизни якоря, а
не внутри одного жеста. Два следствия, оба зафиксированы в шапке модуля и в тестах:

- **Порядок композиции фиксирован** (`MASTER_KINDS`: pitch → length → brightness). Pitch и
  brightness пересекаются (cutoff у `lp` — это `freq`), brightness умеет *вставлять*
  фильтр, значит они не коммутируют; без фиксированного порядка пара позиций означала бы
  разный рецепт в зависимости от последовательности перетаскиваний.
- **Мастер в покое пропускается, а не применяется с ratio 1.** `scaleBrightness(src, 1)` —
  не тождество (слой без фильтра получает вставку `>> lp`), так что композиция «всех трёх
  всегда» правила бы рецепт при движении любого *другого* ползунка.

Ложь, от которой защищался jog, снимается сбросом якоря, а не позиции: `App` держит якорь
в ref и сравнивает `anchor.source` с текущим `source` — правка руками, Fit, Variation,
сгенерированный рецепт, открытие другого — всё это оставляет тексты разными, и ползунки
возвращаются в центр на новой базе. Ни один из этих путей не знает про мастера. `onMasterCommit`
и три обработчика конца жеста (`pointerup`/`keyup`/`blur`) удалены — отпускать больше нечего.
Появился readout `×1.41` у каждого ползунка (у jog его сознательно не было — нечего было
показывать) — это и есть ответ на вопрос «насколько я увёл», которого старому контролу
нельзя было задать.

**Кнопка «⎘ в новый»** (в шапке блока мастеров, не панели: якорный мастер — это *вид* на
один рецепт, кнопка — как вид становится вещью). Текст копируется байт в байт в session-запись
под именем от `recipeName` (заголовок `sound "…"` не трогается — суффикс получает только
запись каталога, тот же раздел, что и у сгенерированного рецепта), копия становится
выделением, мастера якорятся на ней и встают в центр. Оригинал остаётся со своими числами —
без этого «сделай ещё и на октаву ниже» означает отказ от исходного звука. Не файл и не банк:
и `Save`, и `Publish` — отдельные осознанные действия со своими кнопками (как `adopt`).

Ключи `master.fork*`, `master.forked` — en+ru, остальные семь на английском (как весь п. 26).
Проверено в браузере на `pnpm dev`: позиции держатся после отпускания, readout совпадает с
числами в рецепте, возврат в центр восстанавливает исходный текст, «в новый» создаёт запись
и обнуляет ползунки.
