node_modules/
dist/
coverage/
*.tsbuildinfo
.env
.env.local
apps/server/data/*.sqlite
apps/server/data/*.sqlite-journal
# Generated by `pnpm bench --out`; the committed snapshot lives in bench/README.md.
bench/results.md
# Reference recordings you are matching against. Never committed: they are someone
# else's audio under someone else's licence, and the point of this project is that
# the *code* is the deliverable. `vite dev` will serve them (they are inside the
# repo root), so the playground can load one with /@fs/<path> or the file picker.
.refs/
apps/web/.vite/
.DS_Store
